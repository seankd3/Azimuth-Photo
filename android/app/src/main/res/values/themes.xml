<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.PhotoArchive" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0B0B0D</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <style name="Theme.PhotoArchive.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0B0B0D</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_fg</item>
        <item name="postSplashScreenTheme">@style/Theme.PhotoArchive</item>
    </style>
</resources>
