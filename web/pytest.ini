[pytest]
# No waiting. A test needing more than ten seconds is testing the wrong thing,
# and a stuck one costs more than it protects.
timeout = 10
timeout_method = thread
markers =
    bench: opt-in timing, never part of the default run
