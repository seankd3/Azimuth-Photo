import { createFindSimilarAction } from '../library/similar.js';

export function createLegacyLibrarySimilarBridge({
    documentImpl,
    fetchImpl,
    getLightboxIndex,
    getLibraryImages,
    setLibraryImages,
    setRankingsOffset,
    setRankingsExhausted,
    getThumbHeight,
    getCompareStats,
    setCompareStats,
    setSearchQuery,
    bumpLibraryRequestGeneration,
    getLibraryRequestGeneration,
    closeLightbox,
    clearWarmups,
    clearPersistedSearchState,
    updateDateScrubber,
    clearBatchSelection,
    updateCompareProgress,
    openLightbox,
    showToast,
    createFindSimilarActionImpl = createFindSimilarAction,
} = {}) {
    return createFindSimilarActionImpl({
        documentImpl,
        fetchImpl,
        getLightboxIndex,
        getLibraryImages,
        setLibraryImages,
        setRankingsOffset,
        setRankingsExhausted,
        getThumbHeight,
        getCompareStats,
        setCompareStats,
        setSearchQuery,
        bumpLibraryRequestGeneration,
        getLibraryRequestGeneration,
        closeLightbox,
        clearWarmups,
        clearPersistedSearchState,
        updateDateScrubber,
        clearBatchSelection,
        updateCompareProgress,
        openLightbox,
        showToast,
    });
}
