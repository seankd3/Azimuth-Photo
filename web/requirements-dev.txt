# Test/lint-only dependencies (not imported by the production app).
# Install into the shared web/.venv:
#   uv pip install -r requirements-dev.txt
pytest>=8
pytest-xdist>=3.5
ruff>=0.8
# starlette's TestClient backend; starlette >=1.3 requires httpx2 (older
# releases used httpx, which remains fine in already-provisioned venvs).
httpx2
