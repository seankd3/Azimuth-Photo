# Azimuth Photo Master Plan

The owner document for **what Sean asked for and whether it is done**.

Section 1 is the authority: Sean's own words, verbatim, dated, from his prompts.
Nothing in it is paraphrased, summarised, or improved. When a plan and a quote
disagree, the quote wins. Section 2 is the derived work queue. Section 3 says how
to keep this file honest.

Product doctrine lives elsewhere and is not duplicated here:
[product vision](docs/product-vision.md) ·
[roadmap](docs/product-roadmap.md) ·
[perf budgets](docs/PERF_BUDGETS.md) ·
[agent rules](AGENTS.md).

## Status vocabulary

- `live` — checked against the current tree or disk today; it is there.
- `partial` — checked; some of it exists, the quoted ask is not fully met.
- `open` — checked; not done.
- `unverified` — recorded from the transcript, **nobody has checked it**. This is
  not a soft "probably done". It means unexamined.

Statements below were mined from 53 session transcripts (2026-06-05 → 2026-07-31).
Only Azimuth statements are included; statements about other projects, and Sean's
general working preferences (TLDR length, delegation, screenshots), live in his
global `CLAUDE.md`.

---

## 1. Sean's direct statements

### 1.1 What the product is

- `unverified` "I want to build this into a product not just for myself but for any photographer out there" — 07-06
- `open` "The photographer like you" — 07-31 · **the second user.** Shoots RAW, runs Lightroom Classic, owns a NAS, wants out of Adobe. Generalize only where that person differs from Sean — different camera, different drive layout, different OS. Not the self-hoster escaping Google Photos; that is Immich's audience and chasing it pulls Azimuth onto their ground.
- `open` "Import, browse (rank/cull/sort/search), edit, export (Once edits are finished they should be exported into the edits folder), share (links/website/galleries n shit)" — 07-31 · **the core loop, five named stages.** Export is not the end: finished edits are written into `Edits/`, which makes export the act that *files* an image rather than merely emitting it. Share then draws from `Edits/`. No single stage wins a tie — the seams between them are the product, so work that improves one stage by worsening a handoff is a regression.
- `live` "lets always just work off prod, this is a private app" — 07-09 · **reaffirmed 07-31** when asked whether going public ends prod-first development: it does not. Azimuth is a public product that in practice has one user, and prod-first iteration stays.
- `live` "lets keep it all open source and free." — 07-06 · AGPL-3.0 free/open-source license with no product paywall in settings or routes; greps for stripe/paypal/require_license found only false-positive stripEnd identifiers.
- `partial` "all of the custom solutions im building for me, ideally id like to be built in such an elegantly generalized way that they can benefit anyone who wants to use this software." — 07-19 · `web/core/runtime_paths.py:72`, `web/settings.py:36`, `web/settings.py:90` — Portable XDG/AppData paths and empty publish defaults show generalization; camera Adobe profile pack is still a small set (Canon-heavy plus a few others) so not every custom solution is generalized for anyone.
- `partial` "we are building this for me, but in a way that anyone can use it" — 07-21 · `web/features/pages/routes.py:128`, `web/features/pages/routes.py:149`, `web/settings.py:105` — Anyone can hit /setup on a fresh install, but public-release hardening (device token default, commercial face models) is incomplete.
- `runtime` "I want it to feel similar to lightroom classic yet much stronger and better" — 07-06 · `web/static/js/desktop/develop/panels.js:253`, `web/templates/desktop.html:37`, `web/static/js/desktop/state.js:10` — LRC-like Develop panels and Library/Map/Develop views exist in code, but "feel similar yet much stronger" is a subjective UX bar; settle with a timed side-by-side LRC task walkthrough (cull→develop→export)…
- `runtime` "I really want to eat lightrooms lunch." — 07-10 · `web/static/js/desktop/develop/panels.js:253`, `web/static/js/desktop/lenses.js:16`, `web/features/stacks/builders.py:21` — Competitive aspiration cannot be proven from source; settle with a comparative speed/feature preference bake-off vs Lightroom Classic on the same catalog.
- `partial` "I want full 100% feature parity with lightroom classic, with all the same editing tools and locations, aswell as all the other features I have and a really brillant way to unify RAW editing workflow with exported edits, and linking them in a good, way." — 07-10 · `web/static/js/desktop/develop/panels.js:253`, `web/features/stacks/builders.py:322`, `web/features/sync/export_relation.py:1` — Develop has LRC-ordered tools, virtual copies, and RAW↔export version stacks; missing 100% parity — greps found no color_label, tether, /api/print, or Book module; VALID_LENSES has no print/book/slideshow;…
- `partial` "rather than bolting on lots of differnt yet similar features Id like an elegant feature set that has a few powerful multi use features rather than tons of differnt yet similar ones." — 07-09 · `web/static/js/desktop/scope_data.js:12`, `web/features/stacks/builders.py:21`, `web/static/js/desktop/lenses.js:16` — Multi-use primitives exist (shared scope loader, one stacks system with four kinds, shared lens shell), but the desktop surface still has many specialized modules so "few elegant multi-use features" is only…
- `runtime` "I want this to all feel like magic." — 07-15 · `web/static/js/desktop/state.js:10`, `web/templates/desktop.html:37` — "Feel like magic" is a subjective quality bar; settle with timed first-interaction tasks plus error/blank-state counts on a warm library (not static review).
- `runtime` "everything about the app needs to be best in class" — 07-15 · `web/static/js/desktop/develop/panels.js:253`, `web/static/js/desktop/lenses.js:16` — "Best in class" cannot be settled from source; settle with explicit quality-bar benchmarks per surface (library, develop, search, mobile) against named peers.

### 1.2 Speed

- `open` "Speed is the bar" — 07-31 · **the release gate, above feature parity.** A missing feature is acceptable; a slow one is not. Nothing ships that misses its latency budget, even if scope must be cut to make it. Directly bounds "100% Lightroom parity" (1.1): parity never justifies shipping something slow.
- `runtime` "the data should all be there somewhere we really want to focus on extreame preformace too, LRCs fatal flaw is how slow it is, our biggest pitch is lightnight fast speed that feels like magic, this is 2026 and you are AGI, we shouldnt be afraid to use low level code if we need to to accelerate and hyper optomize the program." — 07-10 · `web/desktop.py:1`, `web/static/v2/lens/library.js:1`, `docs/REWRITE_LEDGER.md` — quality bar; the V2 native path now has viewport-near tile loading and stable cells, but still needs standing p50/p95 comparison with Lightroom on the same catalog.
- `runtime` "I want every part of this app hyper optimized like VLC levels, it should be wayyyyy faster than anything else, most software is horribly inefficent." — 07-16 · `web/test_perf_budgets.py:30`, `web/perf/baseline.json`, `scripts/bench.py:50` — "VLC levels / way faster than anything else" is comparative and subjective; settle with a same-machine browse/open latency bakeoff against peer apps using the standing fixture metrics.
- `runtime` "no bloat, no slop, just extreamly good engineering to make this app the fastest photo app ever made." — 07-16 · `web/perf/standing.py:1`, `scripts/perfbench.py:1`, `web/test_perf_budgets.py:30` — "Fastest photo app ever / no bloat" cannot be proven from source; settle with end-to-end wall-clock and RSS on a large catalog versus named competitors, not internal budgets alone.
- `runtime` "I want this running as fast and as snappy on any system as its physically possible." — 07-16 · `web/core/host_profile.py:37`, `web/settings.py:345`, `web/thumbnails/demosaic_pool.py:35` — "As fast as physically possible on any system" is an unbounded ceiling; settle by measuring interactive p50/p95 and resource use on 8GB, 16GB, and 64GB hosts after host_profile tuning.
- `partial` "as you work overnight I want you to continously idenitify the limiting factor of large work, find the slowest part of the app the bottleneck and agressively optomize it, until something else is the bottle neck, and then do the same for it. repeat this process over and over and over" — 07-20 · `scripts/perfbench.py:1`, `scripts/bench.py:1`, `web/perf/baseline.json` — Bottleneck measurement tooling exists (perfbench, standing bench, baseline); missing is any continuous overnight identify→optimize→repeat automation (no perf cron/timer/CI loop found under scripts/,…
- `partial` "I want to squeeze every possible drop of preformance out of any and all hardware we have acess too, and not be silly with how we do it, such to not waste cycles, reads writes etc" — 07-20 · `web/core/host_profile.py:75`, `web/core/hdd_governor.py:1` — Host-adaptive CPU/RAM/VRAM budgets and single-flight bulk HDD reads exist. The GPU-ownership evidence is withdrawn: `work_coordination.py` was deleted 2026-08-17 after measurement showed its one production caller set state no production code read — 341 lines, 24 functions, nine module globals, zero effect on the running app. "Every possible drop of all hardware" is not demonstrated and remains unbounded.
- `partial` "SSd space, ram, cpu, gpu, all of it keep track of benchmarks over time and log them in the commits as you go, I want to be able to really track progress and watch as the app gets faster and faster" — 07-20 · `web/perf/baseline.json`, `scripts/bench.py:18`, `scripts/perfbench.py:37` — One committed baseline snapshot and tools that tag git SHA exist; time-series history is written outside the checkout (runtime state dir / AZIMUTH_PERFBENCH_HISTORY), not logged into commits as continuous…
- `open` "dont be afraid to use lots of my ram to accelerate the xps app, I have 64gb, and I want the app to be super super snappy as much as possible" — 07-21 · `web/desktop.py:1`, `web/tiles.py:1` — the V2 desktop and persistent tile store are real, but V2 has not yet derived or measured an adaptive RAM policy; inherited satellite/settings behavior is not credited.

### 1.3 Laptop and server split

- `live` "Im currently in california and my home server is in austin, so we should have some elegant way to work import cull and edit work locally, some sort of local cache system or something that keeps everything very preformant." — 07-11 · `web/features/sync/satellite.py:76`, `web/features/sync/satellite.py:87`, `web/features/sync/mirror.py:76` — Satellite/standalone keeps a local catalog, imports, and SSD caches while syncing to a hub; request-path thumbs/develop do not block on the hub.
- `partial` "I want to be able to work on my latop, impor photos quickly and edit off my laptops fast SSDs while also backing up to the omarchy server and freeing up space on my laptop." — 07-16 · `web/features/imports/staging.py:94`, `web/features/imports/staging.py:517`, `web/features/sync/sync_worker.py:192` — Local import plus background hub upload exist; freeing laptop space is a manual Free up space job, not automatic after backup.
- `runtime` "the server needs to run at max speed as much as possible, its doing the grunt work, my xps will be the nice pretty ui thats super fast and responsive." — 07-19 · `web/core/background.py:538`, `web/core/background.py:540`, `web/core/background.py:549` — Hub-vs-satellite grunt split is coded (hub pregen/AI, satellite skips bulk workers); whether the server runs at max speed or the XPS UI stays snappy needs live load/latency measurement.
- `runtime` "it would be nice if everything I did on my laptop was super fast and never really had to wait on the server." — 07-20 · `web/features/media/routes.py:312`, `web/features/sync/readthrough.py:315`, `web/features/trash/routes.py:94` — Local-first non-blocking paths exist for thumbs, develop base, and trash; “never really had to wait” is only settleable by measuring interactive actions with the hub unreachable or slow.
- `partial` "Id like to use my laptops SSDs as staging (and keep them there for while as I work through them/edit them) and they get quietly moved to the server while being ready to work as quickly as possible." — 07-20 · `web/features/imports/staging.py:94`, `web/features/imports/staging.py:517`, `web/features/sync/sync_worker.py:125` — Local library staging and quiet background upload exist; quiet move off the laptop only happens when the user runs Free up space (not automatic reclamation).
- `partial` "when the images are going from the laptop to the server, they should be processed on arrival, and written to the HDD." — 07-20 · `web/features/sync/hub.py:708`, `web/features/sync/hub.py:775`, `web/features/sync/hub.py:828` — On complete upload the hub verifies hashes, places into the configured library root, and catalogs the file; no on-arrival thumb/AI process path in hub.py, and HDD is deployment config not enforced by code.
- `partial` "we should really only ever read the image from the HDD once, or preferable do it all on ingest off the SSD and then just archive it away on the HDD" — 07-20 · `web/thumbnails/harvest.py:229`, `web/thumbnails/harvest.py:258`, `web/features/sync/hashing.py:14` — Bulk harvest has a read-once-into-RAM path under the HDD governor; freeup confirmation and other hash paths re-read originals, so a single-touch guarantee is not universal.
- `partial` "I want to work in the desktop windows app, and the omarchy box runs a headless sever that acts like a NAS and remote compute." — 07-21 · `web/desktop.py:1`, `docs/CORE.md` — the one-process Windows V2 app is real; folders on the NAS fit the drive model and helpers fit owed work, but the omarchy helper flow is deliberately not rebuilt yet.
- `partial` "The software should be smart enough to seemlessly handle all of this elegantly behind the scenes, not going to differnt ports for differnt interfaces, Infact the only time id ever goto the hub web interface is if I didnt have the desktop app installed." — 07-30 · `web/desktop.py:1`, `web/static/v2/net/index.js:1` — the V2 desktop now uses a direct in-process bridge with no port or browser address. A separate headless/hub surface is parked rather than preserved as a compatibility mode.
- `live` "its a tanuri app or whatever not a web app, I want a native desktop experieance" — 07-30 · `web/desktop.py:1`, `desktop/BUILD.md` — the frozen V2 product launches in one native WebView2 window with the operating system folder chooser; Tauri, the loopback server, and the child engine are deleted.
- `partial` "I want one canocial archive on the omarchy server (dont rearrange the files more than you have too) data safety is important, and on the laptop is just a small chache, with one super easy elegant UI, with good product design." — 07-31 · `web/features/sync/hub.py:134`, `web/features/sync/hub.py:655`, `web/features/sync/hub.py:855` — Hub places durable originals into one library tree and satellites mirror/cache; laptop “small cache” depends on manual freeup plus thumb budgets, and “super easy elegant UI” cannot be graded from source alone.
- `open` "the app should have two modes, a local only mode like lightroom classic, and a Desktop + NAS/Server/mode like lightroom CC but self hosted. either way, to the the user its always canonical set , if theres a NAS its the NAS. (assuming the nas is bigger) (or user can select on onboarding which device to use as the server" — 07-31 · **the storage architecture.** Resolves the same-drive/canonical-archive contradiction: canonical is a role, not a fixed place. Local-only mode is a first-class product mode, not a degraded state.

- `open` "I really just want to work off the laptop, and use the hub for the heavy batch processing, and the deep/cold stroage of raws." — 08-03 · **the division of labour, stated plainly.** The laptop is where work happens, not a thin client onto the archive: it must never block on the hub for anything interactive. The hub is two things and only two — the batch engine (AI, faces, captions, preview pregen) and cold storage for RAW originals. Sharpens the 07-31 two-mode row: canonical may be a role, but the *working* surface is always the laptop. Read it as the acceptance test for the sync work — if the laptop waits on the hub to do something a person asked for, that is a bug.
- `open` "local first we should treat it like aftershoot, we have alot to learn from them too" — 07-31 · **V2 (08-21, `e3943728`):** semantic search runs on this machine — the query embeds inline only when the model is already warm, words answer instantly either way, and the proof watched "woodworking machine" surface the CNC frames with no matching filename. · **ML is local-first, not a server feature.** Faces and semantic search run on the machine the user is sitting at; a hub accelerates them, it does not own them. Aftershoot is the named reference for locally-run AI culling and is worth studying the way Immich was. Bounds the two-mode design: local-only mode does not lose capabilities, only speed.
- `open` "cahce of all the thumbnails we can fit, plus the most recent orignals we can fit, (esply raws)... so its all automatic but still gives the user the mbest UX for most usecases and the SSD space useable? lets workshop it" — 07-31 · **the laptop storage model, deliberately unsettled.** Direction: all thumbnails that fit, plus the most recent originals that fit, RAWs prioritised, sized automatically to the machine. Sean asked to workshop the policy rather than have it decided for him, so **do not implement an eviction policy from this row alone.** Resolves the cache-versus-working-set contradiction in principle: one automatic tiered budget, not two user-managed places.

- `open` "whats the ideal shape for the laptop sending data to the hub processing data and then handing that data back to the laptop? this is something we need to do alot in multiple places? Thumbnails, Embeddings, captions, face AI, etc as we want the laptop to feel as fast as possible." — 08-04 · **names the repeated shape.** Measured 08-04: those four are the same operation under five names, and the tree implements it six times — `readthrough.py`, `preview_mirror.py`, `prefetch.py`, the on-miss task in `media/routes.py:232-271`, `embedding_sync.py`, and the ZIP path in `publishing/downloads.py` — with five separate cursor tables and three near-identical AI workers (2,567 lines, ~52% duplicated). Answer designed as one content-addressed key `(content_hash, kind, recipe)` over one table, generalising the 08-03 rendition-key row in §1.5 by one axis (tier → kind).
- `open` "lets try to come up with the most elegant shape for all this sort of stuff, one nice simple unified system. not a whole bunch of differnt systems" — 08-04 · **the standing constraint on the answer, not a separate ask.** A design that leaves the old path in place is a seventh system, so each step must delete what it replaces in the same commit. Enforced mechanically rather than by intent: net lines must go down per commit, and a check asserts the counts stay at one.
- `open` "im not sure if my laptop can handle the AI models though can it? we should just have 1 simple button for if the hub is offline ask to process locally or not. even if theres no embeddings, I should still be able to rank a new set of images and then when they go to the server to get their embeddings they will get updated and propigated?" — 08-04 · **the law that makes the split simple: a derivation is enrichment, never a precondition.** Every surface renders from what exists and improves when more arrives — ranking needs a thumbnail and nothing else, Diverse degrades to random-within-scope without embeddings and sharpens silently when they land. Because nothing blocks, *who computes* stops being architecture and becomes one boolean (`process_locally_when_hub_offline`, default off). It needs no VRAM intelligence: the existing `memory_pressure` gate and CUDA-OOM circuit already handle a model that does not fit by backing off and leaving the want unsatisfied. Sharpens the 07-31 aftershoot row — local ML is a switch the user owns, not a default. Note the hardware question is answered by measurement, not by design: CLIP and InsightFace fit the 3050 Ti's 4 GB, the caption VLM will not reliably.

- `live` "currently the server is offline so Id like to focus on the local only/local first functionality and get that all working very nicely." — 08-13 · Walked the whole app live against the real laptop catalog with the hub dead; fixed what the outage exposed (Taste disabled by a swallowed 400 + a timed-out probe, a 3s poll storm that could never converge, a dishonest "still sharpening" notice, nine days without a catalog backup from an 04:00-alarm scheduler). Recorded in `docs/archive/SIMPLIFY_LOG.md` 08-13.
- `open` "keep working to refactor and elegantly shrink the code base, buying down techdebt." — 08-13 · restated twice same day ("rather than just gutting shrink the codebase by making the code simpler and more elegant" lineage; "we were drowning in tech debt"). The standing mode of work: every capability lands by deleting or absorbing the machinery it replaces.
- `open` "just focus on getting it to all work locally and the app actually being ready to use while cutting out bloat and shrinking tech debt and code volume, the less lines the better this project has gotten too big, and it also needs to play nicely right along side LRC as im not sure im ready to give up LRC just yet" — 08-13 · **the consolidated directive, three clauses**: (1) local-first and actually usable outranks everything speculative — the film-ready desktop app is the deliverable; (2) net lines keep going down; (3) **LRC coexistence is a standing constraint, not a transition detail** — shared originals stay where LRC expects them, XMP stays LR-compatible (write what LRC reads, read what LRC writes), Azimuth never moves/renames/duplicates anything LRC manages, and the import flow must not fight LRC over the same folders. Closes the executor research thread: embeddings stay 8B on the hub's schedule, no API, no 2B migration; search quality routes through OCR + captions later.
- `live` "I plugged in the external hardrive the hub was using if that is of use to you to continue your work." — 08-13 · The archive's expansion disk attached directly to the laptop. Built `photo/location.py`: one rule resolves archive paths against attached volumes (size-verified), so hub-only photos serve tiles, full images, and Develop bases straight off the disk while the hub is down. Proven live the same day.
- `open` "what work can get the azimuth photo .exe on my desktop working and functional for importing new film photos and editing through them with the current hub offline and the HDD plugged in directly? can we get that workflow/functionality ready as we recfactor and shrink the codebase? Id like to use the actuall app sooner" — 08-13 · **the acceptance test made concrete**: a current desktop build, launched from the Desktop, importing a real film scan folder and editing through it — hub offline, archive HDD attached. The installed build is known stale (its mirror aborts on a UNIQUE constraint fixed since). Work = rebuild/refresh the installed app from main, then prove the film loop end to end on it.
- `open` "can we do the embeddings and thumbnail generation and all that stuff locally too?" — 08-13 · followed in the same conversation by the 4GB constraint ("my 3050ti only has 4gb not enough for the bigger model and I dont want to scarfice the quality of the AI features though") and then "or maybe we should use online API based AI for embeddings and captions as I really want the highest quality search possible" · **the executor question, opened three ways in one sitting**: local (same 8B model, CPU/hybrid, slow but identical quality), hub (when it returns), or API (highest ceiling, works with the hub dead, photos leave the machine, real spend). The derivation design makes this one decision, not three systems — an executor serves wants; `model_key` keeps spaces from forking; migration to a better space is legitimate and reversible. Standing gates: no external API wiring or spend without Sean's explicit go; benchmark on his own photos (search_eval.py) before any library-wide re-embed; never mix two spaces in one active search index. **Researched 08-13, same session ("search online for what is the SOTA ai for this, maybe something like googles model?"):** the SOTA family is the one already in use — 2026 benchmarks put Qwen3-VL-Embedding-2B FIRST on hard cross-modal retrieval (0.945), above Gemini Embedding 2 (0.928) and Voyage (0.900), with a far smaller modality gap (0.25 vs 0.73); the 8B sits at/near the top of MTEB multilingual. The 2B fits the 3050 Ti's 4GB. Direction: no embedding API; candidate move was a deliberate migration 8B→2B — **overruled by ground truth the same hour**: "I used the 2b version before and I wasnt impressed atleast comparing the search to google photos search." His eyes beat the benchmark; 8B stays the canonical space. The comparison standard he actually holds is Google Photos search, which is fusion (embeddings + OCR of text inside photos + people/places/dates + query understanding), not a raw embedder — Azimuth's two missing levers against it are OCR and caption quality (the one surface where an API is a real jump). The search-quality want routes there, not at the embedding model.
- `partial` "also please reogranize the raw folder once and for all please theres still some outliers" — 08-14 · First read (same hour) called it eleven misfiled originals needing hub-side moves; the drive itself disproved that the next day — **every visible outlier's file already sits at its canonical path with the recorded size**. The debris is 17 stale catalog rows: 14 ghost doubles of files whose canonical row exists (three of them 8–16-byte junk "jpg"s worth Sean's own delete) and 3 lone rows under retired spellings (`RAWS/`, `RAWs/`, top-level `Film Scans/`). `scripts/repair_raws_catalog.py` (replaces the disproven mover) classifies and repairs — dry-run proven on the laptop catalog, no file ever touched; ghost keyword/batch links migrate to the canonical row first. The laptop `--apply` awaits Sean's one command (agent permission gate, correctly). The hub runs the same script at reconciliation before its service scans again. The 2,069 missing-marked `Raws/2021–2022` rows (astro/timelapse, bytes not on this drive) and 30 Google-Takeout-Trash rows are hub-truth questions, left alone and invisible in the folders panel.
- `open` "I think I imported the new photos but I dont see them in the libary, it should be light lightroom classic where theres a previous import collection thats selected after importing" — 08-14 · The behavior exists (commit closes the canvas and scopes the grid to `#import_batch=N`); his click had landed on a session killed by an engine swap, so nothing ran. The first real roll (40 frames) was recovered from its intact staging and committed cleanly the same hour. Standing lesson for engine swaps: never while an import canvas is open.
- `open` "maybe dont even worry bout the hub, just the laptops SSDs and the external harddrive as the bulk cold storage" — 08-13 · **a "maybe", not an order — but it names the Lightroom Classic shape**: catalog + working set on the laptop's SSDs, originals on a directly-attached drive, no server in the loop. Sharpens the 07-31 two-mode row (local-only is first-class) and re-weights the queue: local derivation (the H2 machinery + `photo/location.py`) is the architecture for this mode; the hub-transfer waves (H3/H4) serve the other mode and can wait. Do not delete hub code on the strength of a maybe. The concrete next want it implies: the ~50k thumbnails still owed on the laptop should derive from the attached drive in one orderly HDD-friendly sweep, not on demand.
- `live` "forget about the hub thats honestly too complicated, lets just focus on laptop + the external harddrive we can worry about the hub stuff later, but for now lets just do this simplifed workflow. of laptop and big pjgeed in external drive" — 08-14 · **the 08-13 maybe upgraded to the operative order.** Laptop + attached drive IS the system; the laptop's catalog is the operative truth (which unblocked the Raws repair locally); hub code stays but the hub-transfer waves (H3/H4) park, and hub reconciliation becomes a future project whose first step is re-running `repair_raws_catalog.py` against the hub's catalog before its service scans again.
- `live` "continue to fix the app through radical simplifcation." / "we really need to gut and slim down the codebase to make it stupid simple otherwie we will burn tokens like mad just trying to navigate it" / "I want the codebase to be 5% the size it currently is" / "smaller if we can get away with it" / "I feel like the bloat is quite excessive" — 08-14, four messages in one sitting · **the gutting order: target ≈5% of ~196k lines ≈ 10k, smaller if possible.** Fixing and shrinking are the same act — the first kill proved it: the offline service worker (the machinery that served "504 Offline" over a healthy engine after every swap and blanked his roll) died the same hour it was diagnosed. The demolition proceeds subsystem by subsystem, app booting and core loop green at every commit; git history is the archive, the working tree is the product.

### 1.4 Import and storage

- `live` "we can have the photos on the SSD and organize them by the new Azimuth folder strcuture and then have that same structure on the HDD that we use for cold storage" + "D:\Pictures\Raws\[film],[digital]\YYYY\YYYY-MM-DD\Roll (for film) / D:\Pictures\Edits\YYYY\YYYY-MM-DD\ / D:\Pictures\Snapshots" + "you can move the photos just be careful to not delete them" — 08-15 · **the two-tier architecture, owner-specified**: one Azimuth taxonomy at two temperatures — hot `D:\Pictures\` (NVMe: Raws/Digital, Raws/Film Scans, Edits, Snapshots, plus LR's catalog under Lightroom\) and cold `E:\Photos\` (same shape). Executed the same hour: Azimuth's local originals (42) moved C:→`D:\Pictures\Raws\…`; Lightroom's 19,062-file year moved `D:\Pictures\2026\<date>` → `D:\Pictures\Raws\Digital\2026\<date>` (zero collisions, zero losses, per-file verified); all rows + source + import_root repointed; skeleton Edits/Snapshots created. Identical tails between tiers mean archiving later is a pure move. LR side: one relink of its "2026" root to `Raws\Digital\2026` (+ film root to `Raws\Film Scans`) reconnects everything. Leftover: ~74 Holland duplicate TIFs at `C:\Users\smast\Pictures\Azimuth Imports\Film Scans\` (E: holds the canonical copies) — Sean's delete when ready.
- `done` "for film can we do YYYY/YYYY-MM-DD/RollName? (also an easy way to rename the roll folder) id like them organized like that" — 08-14 · Shipped same day (7d1239d1): film lands at `Raws/Film Scans/<YYYY>/<YYYY-MM-DD>/<RollName>/` — the date read from the lab archive's name, else the import day; the canvas grew a Roll-name field whose typing repaints the destination live, sanitized for Windows; the folder tree grew "Rename roll…" for landed rolls (ec4ddd9c). His first roll (40 frames) migrated into the new layout the same day, files + catalog together.
- `partial` "when I import I really dont want to have to click too much or where the photos go, I want it to all be simple and elegant, no managing folders of images or whatnot, the app should just know, 1 click, and the photos are magically imported to my computer and organized neatly into the main archive on omarchy." — 07-16 · designed in [INVISIBLE_IMPORT.md](docs/INVISIBLE_IMPORT.md), not confirmed shipped
- `open` "maybe just 1 option, RAW, personal, film scan, or exported edit, but really the software should be able to figure that out, from the source no?" — 07-16 · **confirmed current 07-31** when asked directly: collapse to one import action and make detection carry it
- `runtime` "I want to get this to the point where I never have to think about where or how my photos are stored." — 07-16 · `web/features/imports/taxonomy.py:15`, `web/features/imports/staging.py:95`, `web/settings.py:90` — Subjective end-state; settle it by a user session where imports and browsing never surface folder choice or path decisions.
- `runtime` "Its kinda all a mess and I want to make it so its not a mess and I never have to think too much about it again" — 07-16 · `web/features/imports/taxonomy.py:15`, `web/features/imports/taxonomy.py:387` — Subjective archive cleanliness; settle it by auditing the live library for misplaced trees and unrepaired mess, not from source alone.
- `superseded 07-31` "person photos is for cellphone shots, Raws are for digital camera raws, exported edits are for well exported edits, and maybe we need another one for film scans." — 07-13 · shipped as four destinations (`web/features/imports/taxonomy.py:39`), superseded by the three-root directive below
- `open` "I think Edits/ Raws/ and Snapshots/ is the right top ordering these define the image state and desire, Edits are edited images ready for sharing, RAWs/ are camera raws and film tiff scans or astro tiffs etc, and snapshots is cellphone pics that may be raws or jgp or random memes or family photos or facebook takeout data, etc that arent inteded to be edited or shared, but are still nice to browse through quickyl search and manage" — 07-31 · **the taxonomy.** Three roots, not four. The axis is *state and intent*, not file format: `Film Scans` folds into `Raws/` (a scan is a negative), and `Personal Photos` becomes `Snapshots/` with a wider remit (takeout dumps, memes, family photos). Supersedes the 07-13 four-destination split.
- `live` "Snapshots" — 07-31 · one-word confirmation when offered Phone vs Snapshots for the third root; Snapshots stands.
- `open` "whatever is the most grmatically correct" — 07-31 · **canonical root names are `Edits/`, `Raws/`, `Snapshots/`** — ordinary title case, not shouty caps, and "Exported" dropped as redundant. Measured 07-31: the hub archive volume is exFAT — case-insensitive, case-preserving — so `RAWS` and `Raws` resolve to the same directory there and cannot split. The never-normalise/never-create rule stays: it protects any install whose archive sits on a case-sensitive filesystem (ext4 NAS), and code must match whatever spelling readdir reports.
- `open` "Leftover — should go away" — 07-31 · `_intake/` is an artifact of earlier work with no remaining job. Retire it rather than building support for it; prove it holds no originals before removing it.
- `open` "Ill just rename the roof folders myself" — 07-31 · **Sean owns the one-time root migration; no agent moves these files.** He renames `Film Scans/` → under `Raws/` and `Personal Photos/` → `Snapshots/` by hand. The agent-side job is therefore *not* a destructive pipeline: it is making the app correct against roots that have already changed underneath it — update the taxonomy constants, never re-create the old roots, do not choke or re-file when it meets the new names, and repair catalog paths that still point at the old ones. Supersedes the earlier "physically migrate" answer from the same day.
- `open` "Ask me when ambiguous" — 07-31 · how import decides between `Snapshots/` and `Raws/`, given the axis is intent rather than format. Auto-file the confident cases from source provenance (`classify_source_kind` already does this), and hold genuinely unclear ones — a DNG from an unrecognised source — in a small review queue. Filing something wrong silently is worse than asking.
- `superseded 07-31` "on desktop I want 2 simple import buttons, import from card, (raws on external cards get important and sorted into raws) and import film scans (opens file picket to import film scans, often tiffs in a zip or rar file, imported and sorted under film scans automatically)." — 07-29 · shipped (card + film routes in `web/features/imports/routes.py`), but superseded as the destination: asked on 07-31, Sean chose one button now. The two routes stay as the mechanism behind it.
- `live` "id like the catlog and images to be organized like this D / pictures / lightroom / catolog file and D / pictures / YYYY / (current year images organized yyyy-mm-dd)" — 07-19 · `D:\Pictures\{2026,Lightroom}` verified on disk 07-31
- **`open`** "id like to retire and remove c/pictures once we have propperly moved everything to its new home" — 07-19 · `C:\Pictures` still holds `2025\`, `Lightroom\`, and loose files. Restated 07-31: *"you never fixed the folders what are pictures?"*
- `open` "lets keep the catlog and the images on the same drive, that seems cleaner to my mind." — 07-19 · `web/core/runtime_paths.py:184`, `web/features/imports/staging.py:95`, `web/settings.py:36` — Defaults split catalog to app data_dir and originals to Pictures/Azimuth Imports with no same-drive co-location policy (searched same drive/co-locate in web/desktop product code).
- `runtime` "just make sure ALLL of the photos are organized into their propper places and nothing is lost please." — 07-19 · `web/features/imports/staging.py:517`, `web/features/imports/taxonomy.py:387` — Verified copy and repair tools exist, but whether every real photo is organized and none lost requires a live disk inventory of the user archive.
- `runtime` "i want it to be super elegant and automatically maintained and sorted. Edits, (film, digital and edited phone shots) Raws, (Digital Raws and Film Scan Raws), and Phone (facebook photos, google photos, etc combined into one date sorted) all nice and elegant and organized automatically" — 07-31 · `web/features/imports/taxonomy.py:15`, `web/features/imports/staging.py:483` — Subjective polish bar; settle it by continuous live use that new imports stay sorted without manual folder work or reclassify repairs.
- `unverified` "everyphoto should have metadata or an exisiting folder that tells you what is is." — 07-31
- `unverified` "photo is already in a folder. the only ones with issues are the personal folders" — 07-31
- `unverified` "when files are imported they should be imported and sorted by the structure I said." — 07-31
- `partial` "i dont want to do all this through you, I want the app to support this so users can easily and seemlessly migrate from LRC to this." — 07-12 · `web/features/develop/import_routes.py:78`, `web/features/develop/lrcat_import.py:417`, `web/static/js/desktop/drawer.js:761` — LRCAT scan/import API and importer exist, but no desktop/static UI calls /api/develop/lrcat (searched web/static, web/templates, desktop, android); Connect Lightroom is a bridge, not seamless full migrate.
- `open` "azimuth photo should read seemlessly across both reguardless of how I move them bertween these two locations" — 08-15 · the two locations being hot `D:\Pictures` and cold `E:\Photos`. **This ask is what produced Azimuth 2.0.** Root cause found: an absolute path is a drive plus a tail and they were stored fused, so every workaround in the tree — `photo/location.py` probing, `hub_remote`, `missing_at`, the mass-missing breaker, `rebind_moved_source` — is an attempt to recover a tail from a path that swallowed its drive. Answer: store the tail, compute the path. `docs/CORE.md`.
- `open` "Ive worked on lots of photos recently in lightroom withing the D/pictures structure, azimuth should auto update as photos and folders update and move" — 08-15 · measured the same hour: reconcile matches strays by `basename+size`, calls the rest *gone*, and `apply()` refuses the whole plan above 5% — raising *before* `_remember_directories`, so a large reorganization stalls it permanently and silently. Under 2.0 there is no rename code path at all: a file at a new tail with a known hash simply gets a copy row.
- `open` "I want to be able to offload work to omarchy or have my files live on that server, without totally complicating everything into a bloated mess" — 08-15 · answered without a new concept: a share is a drive, and a helper is another machine doing owed work that writes results this machine sweeps up. ~200 lines where the hub took 17,000. The rule that keeps it honest: this machine never waits for a helper, and a helper's absence is indistinguishable from a slow day.
- `decided` "Park the phone" — 08-15 · desktop only for the MVP; the whole network layer (auth, unlock, device tokens, origin checks, CORS, remote access, Tailscale serving, pairing) is deleted and the Android client stops working until a transport is deliberately rebuilt.
- `decided` "Leave it for omarchy" — 08-15 · the Qwen3-VL-8B embedding space stays; the remaining ~131k embeddings stay *owed* rather than being recomputed with a weaker model that fits a 4 GB laptop. Semantic search is honestly partial (25%) until the box is powered on. Consistent with 1.3's ground-truth ruling that the 2B was not good enough.

- `open` "for the main folders, personal photos should be undersnapshots, thats phone shots and old social media shots and stuff like that." — 08-19 · **the taxonomy has three roots — Edits, Raws, Snapshots — and phone and social-media photographs live under Snapshots.** A fourth root in a test library is the test library's mistake, not a fourth root.
- `open` "for importing I want it to work similar to lightroom classic, and how ive described before, just the folders on d with some sort of background worker to check for new photos (or manually triggered like lightrooms sync folder feature as a right click option on the folder tree. so I can move photos around in the files, or have other programs import or cull them and azimuth can place nicely with all that." — 08-19 · **the folders are the truth and Azimuth follows them.** Import is Lightroom Classic's shape: attached folders on the fast disk, a background sweep that notices new, moved and removed files (and a *Synchronize folder* on the tree for a manual one), so other programs may add, move or cull files and the catalog keeps up rather than owning them. Design brought for discussion before building (the 08-19 import design note).

- `open` "sounds good build it all. but lets drop the video requirement for now too much of a rabithole" — 08-19 · **the 08-19 import design is approved as written; video is out of scope.** The working drive is swept every minute and the archive on attach or request with *Synchronize folder* on the tree; a move is recognized by name, size and time and confirmed by identity; changed bytes carry their decisions; a file gone from every drive shows as missing with a Forget verb; card and folder import put by the one destination rule into Raws/Snapshots/Edits with the camera's own date, verify every byte, clear the card only after, and never overwrite. Video files are neither imported nor catalogued until asked for again.

- `open` "to anserw your questions 1, on attach/request." — 08-19 · the sweep cadence question. **Read as:** drives are swept on attach and on request (*Synchronize folder*), with the working drive's minute sweep kept as the "background worker to check for new photos" asked for the same day — 0.5 s for 1,874 files, invisible. If "on attach/request" was meant to exclude the minute sweep, say so and it goes.
- `open` "2 no videos" — 08-19 · restated; video files are not catalogued or imported.
- `open` "3 show as missing with forget missing options added to the folder tree rick click menu? similar to LRC" — 08-19 · **missing photographs are shown, and *Forget missing photos* is a folder verb on the tree's right-click menu** (Lightroom's remove-missing, scoped to a folder), beside the per-photograph Forget.
- `open` "4 mostly google photos takeout." — 08-19 · **phone photographs arrive as folders, not cards.** Import-a-folder is the phone path; a Google Photos Takeout folder is guessed as Snapshots.
- `open` "5 yeah sounds good to me, default rolls to 1. 2.. 3... with options to name or if film stock metadata is present" — 08-19 · **film scans: rolls default to 1, 2, 3… under the day, nameable, and named from film-stock metadata when it is present.** Built after card and folder import.

### 1.5 Editing and colour

- `runtime` "the raw thumbnails look great, but the as soon as i go into the edit/develop tab the colors get super ugly, this is absolutely critical to get right." — 07-15 · `web/thumbnails/generation.py:120-126`, `web/raw_thumb_ops.py:57-82`, `web/features/develop/rawproc.py:613-621` — Thumbs use embedded JPEG or LibRaw demosaic, not Develop; anti-vomit `develop_default_render` exists, but “super ugly” needs a live thumb-vs-Develop A/B on real RAWs.
- `open` "rendering raw images for their thumbnails sounds like a good idea but if it's a brand new unedited raw the camera provided data should be good" — 08-03 · **the thumbnail rule: a thumbnail shows what the photo currently is.** Unedited RAW keeps the camera's embedded JPEG, which is both the fastest path and the one Sean says already looks great. Once a photo has develop settings, its thumbnail is Azimuth's render of those settings. This settles the grid-versus-Develop mismatch without making thumbnails worse: the two surfaces only ever differ when there are no edits, and there the camera's rendering wins. Measured 08-03: nothing currently refreshes a preview when an edit is saved — `purge_image_cache` is called only from `features/catalog/routes.py:505`, never from the Develop save path — so an edited photo keeps showing its unedited camera JPEG indefinitely. Implement as part of the one rendition key: `(photo, tier, recipe_version)`, where an edit changes the key.
- `partial` "I'm treating Develop color fidelity as P0: we should establish a measurable reference pipeline before adding more editing features, because every slider is downstream of that foundation." — 07-15 · `web/eval/fit_camera_profile.py:1-6`, `web/eval/fit_camera_profile.py:302-338`, `web/features/develop/render.py:642-661` — Offline LR-pair fitter, DNG goldens, and default-render twin exist; no continuous product/CI colour-fidelity (ΔE) gate, and feature work was not blocked on it.
- `runtime` "lets do propper super accuate canon color science (and other cameras too) but we really need to nail this 100% like a professional photo editor, colors are kinda the most important part" — 07-12 · `web/features/develop/pipeline.py:1134-1206`, `web/features/develop/dng_pipeline.py:1-12`, `web/features/develop/camera_profile.py:85-99` — Adobe DNG + fitted Canon residual paths ship (R5/R7/RP fitted; only R5 tone_trusted), but “100% pro editor” accuracy is a visual/metric bar vs reference exports.
- `runtime` "all the raw images have a wierd purply color when opened fully, lets make sure we really nail the color science." — 07-10 · `web/static/js/desktop/loupe.js:358-387`, `web/raw_thumb_ops.py:57-82`, `web/features/develop/rawproc.py:519-541` — Full-open loupe uses lg thumbs (LibRaw/embed), not Develop; no purple-cast fix found (only DefringePurple); settle by opening full RAW loupe and checking cast vs embedded sm.
- `partial` "it would be great if we can do soemthing better than just film presets too, like what if we also had the very best most accuate film emulation?" — 07-10 · `web/features/develop/film.py:1-13`, `web/features/develop/film.py:202-238`, `web/static/js/desktop/develop/film_panel.js:16-47` — Physical film engine (H&D/halation/grain/DIR) plus 14 stocks and UI is more than LUT presets; “very best most accurate” needs scan/reference comparison, not source alone.
- `live` "we need to add a elegant way to handle RAW vs edits stacks, (I talked about this before in a differnt session) see if you can find that info" — 07-10 · `web/features/stacks/builders.py:322-397`, `web/data/repositories/stacks.py:447-530`, `web/features/develop/routes.py:1011-1013` — RAW↔edit version stacks match, persist on export, show as RAW+N, and toggle via V / Edit RAW end-to-end.

### 1.6 Refine and ranking

- `runtime` "I want the refine mode running on my laptop to work super well be really polish, instant click responsiveness on ranking the photos and good algos for the differnt modes for refining." — 07-21 · `web/static/js/desktop/refine.js:24`, `web/static/js/desktop/refine.js:690`, `web/static/js/mobile/refine.js:212` — Strategies, optimistic pick UI, and mode code exist, but “super well / polish / instant / good algos” is a subjective quality bar—settle with click→visible-replacement p95 and a live polish pass on a real… · **V2 (08-19, `170b9f32`):** Refine rebuilt on the V2 core — a round is one whole decision, sets are drawn only from photographs whose tile exists and shown at equal area, the next photographs are decoded before they are needed, the pick holds 120 ms and leaves with the longest-standing other, digits/arrows/Enter pick, Ctrl+Z takes the round back; proven natively maximized.
- `live` "in refine in the dual mode, both images should be replaced each round." — 07-21 · `web/static/js/desktop/refine.js:489`, `web/static/js/desktop/refine.js:493`, `web/static/js/desktop/refine.js:696` — Desktop duel `replacementIndices` returns every index so both cells are replaced; mobile pick advances to a full new set. · **V2 (08-19, `170b9f32`):** falls out of one rule — the pick and the longest-standing other leave each round; in a pair that is both.
- `partial` "dual should only compare two images of similar aspect ratio, gotta control for aspect ratio in duel" — 07-21 · orientation pairing pool exists in `web/features/compare/service.py`; aspect-ratio matching itself not found · **V2 (08-19, `170b9f32`):** a pair is one orientation (the companion shares the anchor's landscape/portrait/square); larger sets are mixed at equal area, so shape never votes there either.
- **`open`** "Im on the diverse mode but its showing me images that all look the same, in this mode it should show images that are very differnt unless not otherwise possible (this is to take max advantage of the elo propigation system to sort clusters of images relative to eachother quickly)" — 07-21 · `diverse_sample()` exists; the quality complaint about its output is unaddressed · **V2 (08-19, revised 08-21 `f51d08bd`):** there are no modes; one candidate query (least-judged anchor, nearest in rating) over the current scope — and rating is the taste-blended sort index, so an unjudged set is drawn from where the prediction says the anchor lives. What diverse/compete/explore reached for, as one ORDER BY.
- `unverified` "when using the refine mode, can we somehow limit it to the files that already have thumbnails and are ready to serv so we never get blank spots like this?" — 07-21 · **V2 (08-19, `170b9f32`):** built as the tile store's `ready` scope composed into the candidate query; a card is never blank.

### 1.7 Mobile and Android

- `partial` "I want to build a native andorid app, like google photos, with super deep rich intergration, not a pwa but a rich native android experience so that I can entirely replace google photos, so that every photo I take on my phone gets saved into this, and I can use it as my phones default photo app." — 07-12 · `android/app/build.gradle.kts`, `android/app/src/main/AndroidManifest.xml:34`, `android/app/src/main/AndroidManifest.xml:44` — Native Kotlin/Compose app, MediaStore backup, and gallery/viewer/picker intents exist; full "entirely replace Google Photos" is not demonstrated from source alone.
- `partial` "photos (and videos?) should be quietly moved off my phone and onto the main sever." — 07-12 · `android/app/src/main/java/app/azimuthphoto/mobile/backup/BackupWorker.kt:55`, `android/app/src/main/java/app/azimuthphoto/mobile/backup/BackupWorker.kt:59`, `android/app/src/main/java/app/azimuthphoto/mobile/data/Settings.kt:58` — Quiet photo+video upload exists; free-up off the phone is opt-in (default false), needs MANAGE_MEDIA, and worker FreeUpSpace.runIfEnabled is a no-op.
- `live` "it should be two way too, for isntance the photos from yesterday imported from my camera on my computer should show up there." — 07-12 · `android/app/src/main/java/app/azimuthphoto/mobile/data/ArchiveApi.kt:72`, `android/app/src/main/java/app/azimuthphoto/mobile/data/UnifiedTimeline.kt:73`, `android/app/src/main/java/app/azimuthphoto/mobile/data/UnifiedTimeline.kt:86` — Timeline All-scope merges hub rankings with device media so computer-imported archive photos appear on the phone.
- `partial` "I want to be able to share to other apps, I mean really just make it like a total copy of google photos." — 07-13 · `android/app/src/main/java/app/azimuthphoto/mobile/ui/ViewerScreen.kt:815`, `android/app/src/main/java/app/azimuthphoto/mobile/ui/TimelineScreen.kt:558` — Outbound ACTION_SEND / SEND_MULTIPLE share exists; "total copy of google photos" is not met.
- `live` "For editing we should add a button to edit RAW in snapseed or other installed editing apps" — 07-13 · `android/app/src/main/java/app/azimuthphoto/mobile/ui/ViewerScreen.kt:360`, `android/app/src/main/java/app/azimuthphoto/mobile/ui/ViewerScreen.kt:824`, `android/app/src/main/java/app/azimuthphoto/mobile/ui/ViewerScreen.kt:829` — Edit RAW (.dng) uses ACTION_EDIT with a system chooser ("Edit with"), so Snapseed and other installed editors can handle it.
- `superseded 07-31` "lets keep the phone app simple and elegant for now." — 07-12 · asked directly on 07-31; Sean chose full Google Photos replacement as the scope
- `superseded 07-31` "most of the real work should be done on a computer, we can worry about those types of features later." — 07-12 · same resolution; heavy editing still belongs on desktop, but phone scope is no longer deferred

### 1.8 Sharing, publishing, website

- `open` "i haven't sent any yet" — 08-03 · **no client gallery link is live**, said when told that merging the sharing surfaces would invalidate URLs already sent to clients. That objection is withdrawn: token stability is not currently a constraint, so the four sharing packages (`share`, `publish`, `publishing`, `shared`) may be consolidated on their merits. The remaining differences are real and must survive any merge, not be dropped: proofing/favourites exists only in `share`; layout, theme, cover image, download size and per-image sized download only in `publishing`; `client_galleries` allows many galleries per collection while `collection_shares` allows one active share, enforced by a CHECK constraint and a unique index; only `collection_shares` carries expiry and revocation.
- `live` "I want to publish a collection to my website, www.seankennethdoherty.com and share a link that way" — 07-08 · `web/features/publish/routes.py:171`, `web/features/publish/routes.py:296`, `web/features/publish/routes.py:778` — Snapshot-to-website, static export, optional deploy hook, and `{publish_site_base_url}/g/{slug}/` links exist; domain is settings, not hard-coded.
- `partial` "To be clear I want very clear manual approval for website publishing." — 07-08 · `web/static/js/desktop/shared.js:430`, `web/static/js/desktop/shared.js:625`, `web/static/js/desktop/panel.js:828` — Publish is user-initiated (drag / Export / Publish button) with republish confirm only; searched publish+shared+panel for approv|pending_publish|manual_approval — no approval queue or first-publish gate.
- `partial` "in the publishing page it should be extremely clear which collection is for the website and other personal/private/shared privately/shared with clients" — 07-09 · `web/templates/desktop.html:197`, `web/features/publish/nodes.py:16`, `web/static/js/desktop/panel.js:234` — Shared page clearly separates Collections / Website / Private links; client galleries live only in Deliver tabs, not a publishing pane — no personal or shared-with-clients columns on that page.
- `live` "maybe there's something where you can update it and you can see what photos you wanna add, but it should be static once it's moved into there. And it should be a very deliberate thing to move that into the public" — 07-09 · `web/features/publish/nodes.py:79`, `web/features/publish/nodes.py:195`, `web/features/publish/nodes.py:553` — Snapshots freeze into `published_node_images`, diffs show added/removed with Apply update, and Export site is a separate deliberate public write.
- `partial` "I want to tie my website and this app together in an elegant way that also is friendly to other people using this software in their own workflows and websites." — 07-09 · `web/settings.py:91`, `web/static/js/desktop/drawer.js:1032`, `web/features/publish/deployer.py:28` — Any install can set publish_dir, site base URL, brand name, and hook; deeper elegant productized multi-site integration is not in code, and “elegant” needs a live UX review.
- `partial` "I want the the images on the website to be generated from the code, and, preferably, have little interactive sections based off the real code of it." — 07-12 · `web/features/publish/builder.py:102`, `web/features/publish/builder.py:160`, `web/templates/share_gallery.html:91` — Site JPEGs are generated by the app builder; interactivity is a self-contained lightbox in share_gallery.html, not desktop/app UI modules.
- `open` "the app and website need the same style and the website needs to show bits of the actual UI, not something totally differnt." — 07-14 · `web/templates/share_gallery.html:13`, `web/features/publish/builder.py:160` — Searched share_gallery.html and web/features/publish/ for desktop.css, static/js/desktop, iframe/embed of app UI — none; gallery owns its own CSS/JS and does not show app UI chrome.

### 1.9 UI and UX bar

- `runtime` "I really want all the agents to focus on improving UX, finding and fixing bugs, improving preformance, hyper otpomizing everything, and just working hard and thoughly to make this app feel like absolute perfected magic, like factorio levels of polish and bug free high preformance, extreamly user friendly, perfect UX." — 07-16 · `web/templates/desktop.html:141` — subjective bar; settle with one number: drive all 15 `.view` surfaces end to end, require console-error count 0 and zero open P0/P1.
- `partial` "I dont like overlays for large UIs, it should be a pannel or a page, not something that depends the whole app every time its open." — 07-09 · `web/static/desktop.css:1272`, `web/static/js/desktop/panel.js:244` — every large surface is a `.view` page and the old scrim modal is dead code, BUT the system drawer is still a 400px panel behind a full-screen scrim, and Deliver is still a `modal-scrim` that dims the whole app.
- `partial` "all pannels should be collapseable" — 07-09 · `web/static/js/desktop/panel.js:1618`, `web/templates/desktop.html:242` — left/right panels and all 23 sections collapse with persisted state, but the Develop right rail `#develop-right` and `#develop-filmstrip` have no whole-panel collapse control at all.
- `runtime` "I want a UI refresh/improvement on Azimuth with a very subtle yet elegant aerospace theme." — 07-15 · "(dont over do it)" — 07-14 · `web/static/desktop.css:7` — the entire theme layer is one accent token `--accent: #7aa8d4` plus uppercase labels and tabular numerals; the string `aerospace` appears nowhere. Settle it by putting Grid + Develop screenshots beside the azimuthphoto.com hero for a same-family / not call.
- `runtime` "I want each UI to be as elegant, intutive, and clear as possible, yet professional and powerful, every features in its right spot, everytool where it should be, configurable etc." — 07-06 · `web/static/js/desktop/drawer.js:1089` — configurability is real (7 settings sections), but settle “elegant, every tool where it should be” by timing one unaided photographer through import→cull→develop→export and requiring 0 wrong-surface detours.
- `partial` "explore how the app looks at differnt screen shapes ... lets make sure its all very responsive." — 07-09 · `web/static/desktop.css:1584`, `web/static/desktop.css:1590`, `web/static/desktop.css:1633` — **live bug**: at ≤880px `#panel-right` is `display:none` while `#btn-right-panel` still renders and its handler only toggles a class, so Info/Metadata/Keywords/Ranking are unreachable behind a dead button. `.develop-layout` has no breakpoint below 1050px.
- `runtime` "the smarter search is a big priority for me, the search needs to really be as smart as possible, (also keep in mind, we want to build in a way that the features we could work elegantly and symbiotically with other features) that's were the real power comes from." — 07-09 · `web/features/search/planning.py:49`, `web/features/search/fusion.py:83` — intent planning plus metadata/caption/embedding fusion is genuinely in the live query path and a search scope saves straight to a Smart Collection. Settle “as smart as possible” with `web/search_eval.py --mode fused` nDCG@10.
- `partial` "the smart collections should be a very powerful set of filters that drive it." — 07-09 · `web/features/collections/smart.py:24`, `web/static/js/desktop/state.js:598` — 13 whitelisted facets captured from the live scope and re-resolved on read, but the filter language has no AND/OR/NOT, no numeric or date ranges, one value per facet, and `folder` is silently dropped unless exactly one folder is selected.
- `partial` "suggested collections are pretty bad, just going by date, we should be much smarter about suggested collections and organize it better, lets refernce lightroom classic alot more here." — 07-09 · `web/features/collections/suggestions.py:487`, `web/static/js/desktop/suggestions.js:147` — the date-only complaint is fixed (folder hints, capture events, k-means embedding clusters, caption themes, people), but “organize it better” is not: review is one flat ranked rail and `kindLabel` collapses shoot/event/people into a single “Shoot” badge.
- `live` "when I right click sources, it should give an option to open in explorer." — 07-15 · `revealFolder()` in `web/static/js/desktop/api.js`

---

- `live` "when testing the UI interactively it might be best to make it fullscreen as that will by my main viewing size. and you can deisgn around that (fitting nicely on 1080p screens is good too but not a priority for me atm)" — 08-19 · **the design size is the full 3456×2160 window at 175%.** The app opens maximized and every native proof photographs it maximized; 1080p must still fit but is not the size decisions are made at.

### 1.9b Codebase and architecture (08-15, the 2.0 order)

- `open` "I want the codebase to have zero tech debt, just clean dead simple code. small codebase." — 08-15 · measured honestly rather than agreed to: 146,178 lines, of which 39,135 is UI. Cutting the UI removes the product, not the debt, so the target is stated against the backend — **107k → ~12k**, tests → ~5k, UI → ~25k. One mechanical gate holds it: a line budget that fails the build. Advisory doctrine decays; this is the lesson from three previous cleanup waves growing back.
- `open` "can we also dtich the web shit? I just want a desktop app." — 08-15 · separated into three tiers: delete everything that exists because it was reachable over a network (doing), replace HTTP with direct calls once one API client is the chokepoint (next), rewrite the UI natively (**refused** — 39k lines of grid, loupe and keyboard polish is the product, and HTML/CSS is a rendering layer, not a liability).
- `open` "the simpler our architecture the more powerful it will be" — 08-15 · borne out the same session: every capability gained came from a deletion. Storing the tail bought cross-tier reads, folder-move following and one-row letter changes. "Owed is a query, not a queue" deleted a 93,220-row backlog *and* is why a helper needs no protocol. Remote helpers cost 17,000 lines in the hub design and ~200 here — complexity does not merely look bad, it makes capabilities unaffordable.
- `open` "fully implement core.md rewrite nd the related implementation work extending outwards from there until the radical simplication rewrite is totally complete." — 08-16 · the 2.0 build order is not advisory and does not stop at the model layer; the rewrite is finished when the product runs on the core, not when the core exists beside it.
- `open` "focus on gutting the app and the nrebuilding it with clean elegant and smarter primatives from the inside out." — 08-16 · **the method, and it overrides incremental wiring.** Not: thread the core through 100k lines of machinery. Instead: finish the primitives, rebuild each surface on top of them, and delete the machinery it replaces in the same commit. Inside out means the core is written first and the callers are changed to suit it — never the reverse.
- `open` "propogration via the vectors is a key mechanusm for mulitplying the ranking power" — 08-16 · **V2 (08-21, `f51d08bd`):** the mechanism is live on the core — the worker grows vectors from the grid tiles as a cache kind, `rerank` blends fitted strength with the taste prediction and rewrites the whole order after every round, and the native proof watched photographs never judged sort by it. · **corrects a same-day reading of the catalog.** Measured that 29,260 photos carry an Elo no recorded comparison explains, and called it contamination; it is not, it is the taste model spreading 2,532 real judgements through the embedding space to photographs that look like them. Propagation is a designed feature and the reason a few thousand comparisons can rank 157,000 photos. Design consequence: Elo is still derived rather than stored judgement, but its derivation is *comparisons + vectors*, so it is a cache kind that improves every time either input grows — and embeddings become load-bearing for ranking, not only for search.
- `open` "we can display just a green dot if all the photos in that folder are stored locally. no need for both. i suppose it may come in handle when local photos are not backed up to the drive though idk whats the most elegant way to handle this ? I want it super simple and elegant not overly busy" — 08-16 · **the right instinct, and it resolves to one dot answering one question.** Two dots report *which disks hold this*, which is trivia. One dot can report *is this folder safe*, which is judgement criterion #4 in `docs/CORE.md` — "it says what isn't backed up". So: green = every photograph here has a copy on the record drive; amber = some exist only on the working disk and would be lost with it; hollow = the archive is not plugged in, so the honest answer is that we cannot say. One mark, three states, and the only one that asks for action is the amber one.
- `open` "lets use green and yellow red dots, green for local, yellow for harddrive and red for offline" — 08-16 · refines the folder indicator: **colour, not initials.** Green = on the working disk, yellow = on the archive, red = that drive is not attached. Better than the letters it replaces because it is read rather than decoded, and it says the thing that actually matters at a glance — *can I open this right now, and how fast* — which is exactly the `is_record` bit plus whether the drive answered.
- `open` "also in the current web view the folders arent scene (this should be combined and mergded view from both drives) with maybe alittle subtle indicator of whwere they are?" — 08-16 · **a bug report and a design in one.** The folders panel reads "No folders yet" because the rebuilt `/api/folders/tree` returns a flat list where the UI wants sources with nested children — my regression. The design half is better than what it replaces: a folder is a prefix of a tail, so there is **one tree**, not one per drive, and which drives hold a folder is an attribute of the node rather than the axis the tree is built on. Lightroom cannot do this; the tail model gets it for free.
- `open` "can we handle missing and find photos similar to how lightroom does it? with the syncrhonize folder button and such?" — 08-16 · Lightroom's Synchronize Folder finds new files, removes missing ones and asks first. Azimuth already has the halves: `sweep()` walks a drive and `state()` says available/away/lost. What is missing is the *conversation* — showing what a sweep would change and letting the owner approve it. Note the standing constraint: away is not lost, so a synchronize must never offer to remove a photograph whose drive is simply unplugged.
- `open` "also when I rotate photos in lightroom or apply edits in lightroom those should appear here too. come up with an elegant way to do that aswell" — 08-16 · Lightroom writes edits and orientation to XMP sidecars. The elegant reading: **an XMP sidecar is a decision made in another application**, so it belongs in the log with the rest — read on sweep, filed as a `develop` decision whose label says where it came from, and losing to anything the owner did here afterwards. `develop_settings.origin` already distinguishes `xmp` from `user` (79,482 against 5), which is exactly the precedence rule this needs.
- `open` "how can we keep the codebase as simple as possible, as short as possible while being as powerful as possible? I want it to be super nimble and clean to work in, elegangcy of code is the ultimate goal." — 08-16 · **the standing question, and it now has a measured answer**: see `docs/CORE.md` § *How it stays small*. Short version, from the session that took 107k lines to 74k: you do not get there by writing less code, you get there by deleting **questions**. Nearly every large deletion followed noticing that something had stopped being asked.
- `open` "continue working to create new elegant first princples solutions across the app. focus on the core and the basics just browsing and handing the files on disk neaty and generating the thumbnails for them. this is the first feature of the app so lets focus here" — 08-16 · **names the first feature, and it is not a feature list — it is one loop.** Browse, keep the files on disk tidy, make the tiles for them. Everything else in the product is downstream of those three working well, so they get the attention rather than the surfaces that merely have the most lines. Practical effect: Develop's remaining wiring is parked mid-flight, and the bar for browse/disk/tiles moves from "answers correctly" to "is genuinely good".
- `open` "I mean its ine to have stuff running in the background so long as we propperly take advanteage of multithreading in a way that keeps the interface super responsive" — 08-16 · **corrects the politeness law, and raises the bar.** The 2.0 draft had chores *stop* while the owner was using the app, which is a truthful reading of the 23 ms-versus-minutes measurement but the lazy fix: it leaves the machine idle exactly when someone is sitting in front of it. The requirement is concurrency done properly — chores keep running, and responsiveness is protected by where the work happens rather than by whether it happens. Consequence, and it is the appendix lesson made load-bearing: `rawpy.postprocess` holds the GIL, so decode belongs in a *process*, not a thread; the request path must never share a connection or a lock with a chore.
- `open` "take your time and be very elegant and thoughtful about the implementation, v1 was a rough draft v2 is a well edited final copy" — 08-16 · **the standard, and a better metaphor than any line count.** Editing is not shortening: it is making every part earn its place, cutting what restates, and finding the one sentence that replaces a paragraph. Applied here it means a route is not repointed at a new module when the question it answers has stopped existing — it is rewritten to say the true thing. Speed is explicitly not the measure; the second draft is allowed to take longer than the first.
- `open` "target is as small as possible focus on elegant clean simple code" — 08-16 · **answers the open question, and answers it away.** The ~12,000 figure was a guess made before Develop's colour maths was measured; the instruction is not a number at all but a direction, so the line budget stops being a target to hit and becomes a thing to keep pushing down. Consequence for the two remaining subsystems: the thumbnail cluster is not ported, it is *deleted and replaced* by `work.owed` + `render`, because that is smaller than adapting it; and Develop keeps only mathematics that changes pixels, with every scheduler, cache and history table around it gone.
- `open` "we want the smallest and most dead simple codebase possible. simplicty is the hardest thing to engineer after all, but the most beautiful and delightful when done well" — 08-16 · **smallest is the goal, not a side effect of tidying.** Simplicity is a design output that costs the most effort, so it gets the effort: the answer is not a smaller version of the old shape but a shape that makes most of the old code unnecessary. Measured the same day, the whole point of this: a 2.4 GB catalog of 84 tables holds about **89,000 rows of actual owner judgement** — every other row in it is recomputable.
- `open` "dont hegde its a lie that the app works and I use it daily, i dont use it daily and it never worked well. so dont worry about it" — 08-16 · **corrects the premise `docs/CORE.md` opened with**, and removes the constraint that premise implied. There is no working daily-driver to protect, so the rewrite does not have to keep every commit runnable, does not owe the old surfaces a migration, and must stop citing current behaviour as evidence that a shape is right. A thing that never worked well is not a requirement.
- `open` "gut it all and rebuild it from first princoples with little reguard for the ways of old, just learn from them and move on with new more legant solutions to the same problems" — 08-16 · **the old code is not the starting point, and it is not owed compatibility.** A surface is rebuilt on the core and its machinery deleted in the same commit, rather than adapted, wrapped or threaded through. What carries forward from the old tree is the *lessons* — the appendix of `docs/CORE.md` — never its shapes. Where a lesson and an old implementation disagree, keep the lesson and write the implementation new. The one thing this does not license is a dead app: each surface lands whole, so the app runs at every commit.
- `open` "do we really need so many tests? lets try to avoid tests and these sort of burrcratic hedges" — 08-16 · **tests are not the default deliverable.** A test earns its place only when it is the cheapest way to hold a property that already broke once — the appendix lessons, the perf budgets, the safety refusals. Behaviour-per-method coverage, restated assertions and "does the setter set" are the bureaucracy being refused. One file for the whole core, not one per module; the 2.0 test target moves from ~5,000 lines to as few as hold the line.
- `open` "now some landscape images appear in portrait / I edited the rotations in lightroom classic / since the film scans dont carry orentation metadata" — 08-16 · **a bug report that condemned the fix that caused it.** `/api/folder/rotate` turned a whole 40-frame roll on the reasoning that a lab scans a roll the same way round. It does — and then the photographer turns individual frames, which is the only reason the roll needed attention. Lightroom's own answer for that roll was 18 turned, not 40, so the blanket verb was wrong exactly when it was used. Deleted, not fixed.
- `open` "the latest film edits were the most recent ones with the orientation changes idk how those get saved, maybe in lrc I should save metadata to files?" — 08-16 · **the right question, and the answer settled the whole integration.** Yes: Save Metadata to Files is the bridge, and it is the *only* bridge Azimuth needs. Measured — Lightroom rewrote all 40 files, putting orientation in the TIFF/DNG header where every decoder already looks. Turn on Catalog Settings → Metadata → *Automatically write changes into XMP* and the manual step disappears for good.
- `open` "I commanded lightroom to save metadata to files for the 2026 raws" — 08-16 · this is what made the measurement possible and it deleted a module. `decode()` was then found to agree with Lightroom on **40 of 40** frames with no Lightroom-specific code at all, so the half-built `.lrcat` reader was removed rather than finished.
- `open` "we want to keep the solution as well thought out and elegant as possible. not just bloat. working seemlessly and nicely along side LRC is a major selling point, lets be smart careful and considerate for this sort of intergration." — 08-16 · **names an integration as a selling point, and sets its shape.** The considerate version is not a Lightroom adapter: it is reading the photograph properly. Adobe's catalog schema is private and serves one application; the photograph is a published format and serves all of them, so the code that reads the file also works for darktable, Bridge, Capture One, a restored backup and a re-scan. The integration is the *absence* of an integration — see `docs/CORE.md` § *Alongside Lightroom*.
- `open` "I apperciate the deep investigation but remeber to always come back to an elegant and simplest solution, not the quickest solution but the solution that solves the most problems with the least code." — 08-16 · **the acceptance test for every investigation.** Measuring is allowed to be long; the thing it produces must be short. Applied the same hour: four separate defects (blanket rotation, stale dimensions, stale digests, orphaned decisions) collapsed into **one word** — `synchronize` gained `changed` beside `new` and `missing` — and a 136-line module was deleted rather than debugged.
- `open` "now make sure this fix is elegant and not just a bandaid." — 08-16 · **the check that turned a fix into a refactor.** Versioning the tile URL made the grid right and left the concept wrong: the same fact was spelled in three sets of places, so it only worked where I had edited. `thumb_url` was built server-side in two places and read in three and *could not* carry the version; "how wide is this photograph" was answered four times, all four preferring a stored `aspect_ratio` that disagrees with the dimensions on 12,127 photographs. The standing form of the question: after a fix works, find every other place that answers the same question, because a fix that only works where you looked is a bandaid by definition.
- `open` "constantly think about refactoring and simplfying. no code is sacred. this codebase is a block of rough marble to be carved and molded into the platonic ideal" — 08-16 · **carving is the default activity, not a phase.** Every fix is an opportunity to find the shape underneath: one address, one shape, one ETag. Corollary learned the same hour — carving needs the chisel checked. A `grep` for callers used `[^)]*`, which stops at the paren inside `dict(row)`, missed a caller and 500'd `/api/trash`; the console caught what the search did not.
- `open` "there are still aloy of older film photos that are all being displayed in landscape but these photos should all have rotation data from lrc" / "C:\Pictures" — 08-16 · **a bug report that overturned a design decision, and the second message is why.** I searched three roots for `.lrcat`, found one, and concluded the rotations did not exist. There are **three** catalogs; the others sit under `C:\Pictures\Lightroom\Catalogs\`. 2,953 photographs are turned in Lightroom with no orientation in the file at all, so "read the photograph, not the catalog" was losing real decisions. The reader returns as a one-time *adoption* rather than a runtime coupling. Standing lesson: **a search that covers three of four places reports absence with the same confidence as a complete one.**
- `open` "astro awards is not astrohphotography" — 08-16 · **a name is not a namespace.** The astrophotography exclusion is the `Astro/` tree specifically; "AstroAwards" is an event and ordinary photography. Matching the substring dropped 1,770 rows and 460 real rotations from the adoption. A standing rule expressed as a keyword match will eventually exclude something that merely shares a word.
- `open` "lets keep refining the ideas for elegance and first principles thinking" — 08-15 · the standing mode for 2.0. Design is adjudicated by whether a feature *falls out* of the shapes or has to be built: Develop history stopped needing a table, move detection stopped needing code, the 10,750-duplicate merge stopped existing.
- `open` "we dont have to do all the embedding work right away, I want to focus on the erwrite and refactoring the codebase into very elegant, simple and robust forms, the old version was a buggy nightmare and this version has to be the polished finished product, that we take our time on every single line." — 08-18 · **the current priority and release bar.** Embedding coverage is not a prerequisite for the rewrite. The work is the deliberate core-outward reconstruction of a dependable finished product; speed of implementation and background completion counts do not compete with architectural clarity, robustness, or product polish.
- `live` "sounds good but also take a look over all the .md files and the learning etc including claudes internal notes and memories." — 08-18 · repository documentation, Claude's plans, and the Azimuth/refactoring memories were audited before further implementation. Their contradictions are being resolved into the ownership model in `AGENTS.md` and `docs/README.md`.
- `live` "theres also an elegancy study somewhere" — 08-18 · located and reviewed; its operative correction is that elegance is fit rather than polish, coupling rather than size is the useful risk measure, and static-reference deletion is unsafe evidence by itself. Those conclusions now live in the authoritative `AGENTS.md` simplicity rules.
- `open` "great lets consolidate and unify the documentation" — 08-18 · **documentation is part of the rewrite.** Four documents own current truth: this ledger owns Sean's words, `docs/CORE.md` owns invariants, `docs/ARCHITECTURE.md` owns code shape, and `AGENTS.md` owns working rules. Other documents are current references only when the documentation index says so; historical V1 specifications remain evidence, not authority.
- `open` "how can we keep track of what parts of the codebase have been rewritten and what parts still need work? dont want to leave old sloppy code, everything has to get rethunk from first principles" — 08-18 · **the rewrite needs an exhaustive completion gate, not a progress estimate.** `docs/REWRITE_LEDGER.md` records every product area and exact code file, including tests and build tooling. Anything not individually proven or deliberately removed remains legacy by default; a directory-level claim cannot hide an unreviewed file.
- `open` "the new code doesnt have to interface perfectly with the old code, as I said I want it all to be ground up rebuild" — 08-18 · **compatibility with V1 is not a requirement.** A surface does not become rewritten by wrapping, adapting, or dual-running old machinery. Its V2 behavior lands whole on the new core and the V1 implementation leaves in the same change. Compatibility code is permitted only for user-owned data or an explicitly named external format, never to preserve internal V1 shapes.
- `live` "codex isnt active currently so you can continue where it left off, also take note of its ledger of what has been rewritten and proved and what hasn't" — 08-19 · **one session owns the rewrite at a time, and the ledger is the handoff.** Claude resumes from Codex's last commit and its uncommitted Pick/Reject/Undo work; `docs/REWRITE_LEDGER.md` is read before any surface is touched and updated in every rewrite commit.
- `open` "we want to slowly and deliberately re-write the whole app from first principles with elegance learned from the past version, and radically simplified" — 08-19 · **the pace and the bar, restated.** Not fast, not feature-count: each surface is re-derived from the problem, carries only lessons the old version paid for, and lands radically smaller — `product-roadmap.md` order, `REWRITE_LEDGER.md` gate, `AGENTS.md` simplicity rules.
- `open` "no bandaids no stop gaps, no adapters, we should take all the ideas from v1 and rewrite them with hindsight and first principles in mind so we can get the highest quality code that is elegant and preformant. v1 was the rough draft v2 is the product." — 08-19 · **the standing rule for every surface, said while the tiles rewrite was being scoped.** V1 is mined for what it learned (the ideas, the measurements, the failure modes), never for code to wrap or bridge; each V2 surface is the finished form of the idea, and anything shaped as a patch over a V1 shape is rejected at review.

### 1.10 Non-goals

What Azimuth deliberately refuses. A non-goal is worth as much to an agent as a
goal — it is permission to stop.

- `open` "No cloud, no accounts, no telemetry" — 07-31 · nothing may require an external service or account to work. Constrains dependencies, not features.
- `open` "No general media manager" — 07-31 · photos and the photographer's workflow only. Not video editing, not documents, not a Drive replacement. Stay narrow exactly where Immich went broad.
- `open` "ignore it, im keeping the astro shots out of this program sope they are too special leave them out" — 07-31 · **astrophotography is out of scope.** The `Astrophotography/` root is not a library root, not a taxonomy destination, and not Azimuth's business. This is also a **data-safety boundary**: no scan, index, migration, cleanup, dedup or free-up path may read, move, rename or delete anything under it. Stacking, calibration frames and session structure are why — that workflow is not what this app models, and half-modelling it would damage it.

### 1.11 Snapshots

- `open` "Everything, just not by default" — 07-31 · the full app works on `Snapshots/` if you go looking, but snapshots never enter Refine queues, Develop scopes, or ranking surfaces unless explicitly scoped to. Default-off, not absent — 40k phone pictures and memes must never dilute the professional surfaces, and must never be unreachable either.

### 1.12 Ranking and RAW support

- `superseded 07-31` "Real stars alongside Elo" — 07-31 · recorded as two independent axes, corrected the same day by the row below. Kept because the *requirement* it captured still holds: stars must be real, stored, filterable and sortable, not a dead write like the current mobile rating sheet.
- `open` "Stars should be applied by the photos elo" — 07-31 · **stars are Elo's readable face, not a second signal.** One quality signal earned from pairwise comparison; the star is its projection, computed and stored so it can be filtered, sorted and exported to Lightroom as a real value. This supersedes the independent-axes design above — there is no competing user-asserted score, which keeps the propagation system on the single signal it was built for. · **V2 (08-19, `f93a7e91`):** `rerank` writes the star column beside the order — top 2% five, 10% four, 30% three, 60% two, the rest of the ranked one, unranked none; earned after three rounds; `STAR` is no longer a projected decision family.
  - `open` "Keeps filtering" — 07-31 · **1–5 filters, it does not rate.** Asked directly, Sean kept the keys as "show me 3+ and better". This settles the override question left open above: there is no manual star gesture, so **the star is purely computed and Elo is the only way to change it**. You change a photo's standing by ranking it in Refine, which is the entire point of the earned signal. The earlier inferred "remembered correction" override is withdrawn — do not build it.
  - The cost Sean accepted knowingly: every Lightroom migrant, himself included, will at some point press 3 expecting to rate and watch the grid filter instead. If that friction proves real in daily use it is a UX problem to solve with affordances, not by adding a second signal.
  - Follow-on for export: every photo has a computed star, so Lightroom always receives a real value and never an empty rating. There is no authoritative-versus-inferred distinction to carry — every star is computed, so every star exports the same way.
- `open` "Widen now" — 07-31 · **multi-vendor RAW becomes real support this cycle.** The importer already files `.arw/.nef/.orf/.raf/.rw2` (`web/features/imports/taxonomy.py:55`) into an archive the scanner never catalogs and XMP write-back never touches — a silent trap for exactly the second user named in 1.1. Scanner acceptance, catalog, thumbnail decode, Develop and XMP write-back all widen together; a format is not supported until it survives the whole chain. Acceptance coverage is the gate, and today it passes for 1 of 3 fitted Canon bodies, so the suite widens before the format list does.

### 1.13 Interaction shape

- `open` "Keep Develop as a mode, make it instant" — 07-31 · **Develop stays a mode you enter, and the 1,377 ms becomes a hard latency budget rather than an architecture change.** Familiar to every Lightroom user, keeps edit controls out of the culling surface, and turns the slowest interactive operation in the app into a number to beat (`develop_open_202_to_200_ms` p50 ≤ 400, `web/perf/baseline.json:43`). Rejected: editing in place in the loupe.
- `open` "something the remebers by source if data doesnt make it ovbious" — 07-31 · **import learns per source.** Photos appear immediately and file themselves from provenance. When the data genuinely does not settle it, ask once for that source — then remember the answer and never ask again for the same source. The ambiguity queue is therefore self-emptying by design: each question asked is one that never recurs. Pairs with the one-button import directive in 1.4.
- `open` "Quiet chip only when something is owed" — 07-31 · **offline is invisible until it costs something.** No connection status, no permanent indicator. A small chip appears only when work is genuinely waiting on the server, and is gone when nothing is owed — so the indicator always means something instead of being decoration. Reuses the existing peek-chip vocabulary. Requires that "nothing owed" be *provable* rather than inferred, or the chip becomes a liar.

- `open` "import need default move setting" — 07-31 · the import canvas offers Copy/Add only; Move must exist and be the default for card and staging imports. A photographer pulling from a card wants the card drained, not duplicated.
- `open` "the folders I see on the side appear pretty arbitary" — 07-31 · the import source rail is a filesystem dump: user dirs, duplicate names ("Pictures" twice), raw drive letters, OneDrive, a UNC path. It should present *sources that mean something* — cards, remembered import locations, recents — not a drive tree. Coordinate with the sidebar rework the review session shipped (c419b128) so the two rails share one philosophy.
- `withdrawn 07-31` the "film TIFs misclassified as Personal" observation — Sean had set the file-source dropdown himself; the classifier was not at fault. Kept: the "That source couldn't be read" error bar shows no actionable detail and needs a real message.

- `open` "Also on the top row lets make invites a top row icon, and fold the collections into a panel in the grid page." — 07-31 · two IA changes to the top nav: sharing/invites gets promoted to a first-class top-row destination, and Collections stops being a separate page — it becomes a panel inside the grid, consistent with the folders-and-collections-are-two-lenses law (1.15). Answered 07-31: `"sharing" destination that holds both` — one top-row Sharing icon opening a single surface with friend links and client galleries together; Galleries/Deliveries stay the vocabulary inside it.

- `partial` "ALways update the desktop version isntalled on my laptop after every change, how am i supposed to test it if its not updated withthe changes??" — 07-31 · **honoured 07-31 20:05**: rebuilt and reinstalled at `C:\Users\smast\Programs\Azimuth Photo` (1.0.0-rc.1), and the shipped engine was checked by behaviour rather than by timestamp — it reports `skipped_conflicts`, a field that exists only in the merged mirror fix. Still `partial` because this is a manual step; it closes properly when client auto-update ships. **standing rule: every merge to main ends with a rebuild + reinstall of the installed desktop app** (scripts/build_windows_desktop.ps1), so what Sean clicks is never behind what shipped. A change is not "done" until it is running in the installed app on the XPS. This closes the loop that burned today: he reviewed one-button import on a frozen day-old bundle. Longer-term this row is satisfied properly by an installer with updates (`docs/DISTRIBUTION_SPEC.md` owns that; the hub-served auto-update spec is archived); until that ships, the rebuild is a mandatory merge-gate step, not a courtesy.

- `open` "yes thats correct" — 07-31 · **the shape inside Raws, confirmed:** two shelves, `Raws/Digital/` and `Raws/Film Scans/`, each holding date folders (`YYYY/YYYY-MM-DD`). The loose `2025` currently beside them is unfiled, not a third shelf. Import files digital camera RAW into `Digital/<date>`, film batches into `Film Scans/<batch>`. Supersedes the code's current `Raws/Film Scans` + bare-date assumption; `web/features/imports/taxonomy.py` and the relocation tool must learn the Digital shelf.

- `open` "Edits / Raws / Snapshots needs to be inforced as the archive folder structure (note many phone pics have raws but leave them in snapshots)" — 07-31 · **the three roots are enforced, not suggested** — the archive is kept in this shape by the app, and nothing may file outside it. And the sharpest classifier rule yet: **a RAW with phone provenance stays in Snapshots.** Intent beats format — a phone DNG is a snapshot that happens to be RAW, never a Raws candidate. The Digital-shelf taxonomy work must encode this: provenance decides the root, extension decides nothing on its own.

- `open` "Make sure that by the time I get home 4pm ct, everything is fixed and operating how Ive asked. ready to use the software (focusing on the taxonomy, organization, local laptop cache that quietly syncs with server etc) so I can use the app nicely and productively without frustrations over things Ive already asked for." — 07-31 · **the 4pm CT acceptance bar.** Priority order for the day: catalog matches the renamed archive everywhere; imports file into the enforced three roots (Digital shelf, phone-RAW-stays-Snapshots); laptop cache syncing quietly with honest status; installed app current. Done means running in the installed app on the XPS, screenshot-proven, before 4pm.

- `open` "the orignals should be the only thing stored on the big hard drive, caches, databases, and other program files should be on the SSD in one neat and tidy directory, to keep things fast and clean, with the same database copied and quietly synced in the background on my laptop to keep local querries super fast, Ive been pretty clear on this no?" — 07-31 · **storage placement law.** The archive HDD holds originals and nothing else; every cache, database, and program artifact lives in one tidy SSD directory per machine; the laptop keeps a synced copy of the database for instant local queries (the mirror sync now does this). Violations found today: hub develop cache + backups configured onto the HDD (`/etc/azimuth-photo/azimuth-photo.env` PhotoArchiveCache lines), and Azimuth program folders cluttering the drive root. Note: one backup copy staying on a second medium is a data-safety exception worth keeping — flagged, not silently decided.
  - Measured 07-31: the hub develop cache is **409 GB on the archive HDD** — larger than the SSD's 319 GB free — because it has no size budget. The fix is not a move but a *budgeted* SSD cache with age eviction (same law as the laptop cache), sized to the machine. Backups (38 GB) stay on the HDD as the second-medium safety exception. Queued as a build; the placement law stands, physics scheduled it.

- `superseded 07-31` "RAWs is the most correct imo" — 07-31 · read as canonical `RAWs`; asked again the same evening and answered `Raws`. The row below wins.
- `live` "Raws" — 07-31 · **the canonical spelling is `Raws/`**, which is also what is on disk. Roots are `Edits/`, `Raws/`, `Snapshots/`; shelves are `Raws/Digital/` and `Raws/Film Scans/`. (The hub archive is exFAT — case-insensitive — so `RAWs` and `Raws` are the same directory there; the spelling matters for code constants and for an archive on a case-sensitive filesystem.)
- `open` "they should be moved into the taxonomy" — 07-31 · the laptop-local sources cluttering the folder list (`C:\Pictures` 10,689 and the `Azimuth Imports/Film Scans` staging 74) are not to be hidden or folded away — their contents belong **filed into Edits/RAWs/Snapshots** like any other import. Hiding them would leave photos living outside the archive; the archive is the one place photos live.

- `open` "its all pretty straight forward, its litterally just 3 folders on a hardrive on omarchy" — 07-31 · **the simplicity bar, stated after a nine-hour repair.** The product is three folders on one disk; when the code makes that hard, the code is wrong. Today truth about a photo lived in four places that could disagree — `images.filepath`, `images.missing_at`, `catalog_sources.path`, and the `watched_folders` list — so a rename left three of them stale and every fix looked partial. Standing requirements from this: **(a)** a folder rename is ONE operation that updates every place at once, never a sequence of independent repairs; **(b)** the laptop is a projection of the hub and must be able to re-derive itself wholesale rather than drift; **(c)** when a design needs four synchronized writes to stay correct, simplify the design instead of adding a fifth.

- `open` "yes, like lightroom with right click menu options for sychronize folder" — 07-31 · **the disk is authoritative, and Synchronize Folder is how you say so.** Answering whether "the disk wins" may be a law: yes. When the catalog and the disk disagree the disk is right, and the catalog is re-derived rather than hand-repaired. Lightroom's model is the shape: right-click a folder → Synchronize Folder, which reports what it found (new files to add, catalogued files no longer there, moved files) and applies it. Explicit and legible, not magic. This is what makes a folder rename stop being an operation at all: identity is content, location is whatever the walk found, so the next sync simply *is* the truth. Retires the class of bug that cost 07-31 — four places holding location truth that could disagree.
- `open` "idk, lets focus on the basics. just organzing and storing the photos." — 07-31 · **scope discipline, asked what data counts as precious.** Do not design the earned-data tier (Elo, picks, develop settings, keywords, collections, faces) yet. Get organizing and storing right first: photos land in the right root, stay there, and the catalog tells the truth about where they are. The preciousness question is real but premature — answer it when the storage layer is solid.
- `open` "prove it on a super small test libary" — 07-31 · **the fixture is small on purpose.** New behaviour is proven against a few dozen photos on fast local disk before it ever meets the real archive. Chosen over rebuilding the live hub catalog first. Pairs with the 30-second test rule: if proving something needs the 150,000-photo HDD, the proof is designed wrong.

- `open` "it should feel identical away from home, just fewer originals cached" — 07-31 · **no trip mode, ever.** Away from the archive the app behaves exactly as it does at home; the only difference is how many originals the local cache happens to hold. No separate offline UI, no mode switch, no degraded surface — browse, search, cull, rank and Develop all work the same, and a missing original is a cache miss to fetch, not a different state to explain.
- `open` "other people" — 07-31 · **local-only mode exists for other users, not for Sean.** He always runs desktop + omarchy. Local-only must therefore be genuinely first-class for a stranger on one machine, but it is never the mode to optimise his own daily experience around when the two conflict.

- `open` "for the grid rendering I want it to be liek google photos where theres not much dead space but the images are minimally cropped." — 08-19 · **the grid is a justified layout, not square tiles.** Rows fill the width; each cell keeps close to its photograph's own aspect ratio so little is cropped and little is empty. Owned by `web/static/v2/kit/virtual-grid.js` and the library lens; the virtual window and density slider stay.

- `open` "we should be using the pre-computed thumbnails for the grid, not decoding raws on the fly no?" — 08-19 · **the grid reads finished tiles; it never pays a decode.** The V2 core already says tiles are a cache kind the worker makes ahead — the proof on 08-19 showed the two missing wires: the grid's tile call still decoded on the interactive lane when a tile was absent, and the worker never learned what was on screen. Both are owed by the tiles rewrite below.
- `open` "the thumbnail generation, caching and servering is vital to the catalog, we should ensure that is fully rewritten in the ideal form" — 08-19 · **tiles are a first-class surface of the rewrite, re-derived rather than patched.** Generation (one decode per photograph, viewport first, embedded camera previews where the bytes already carry one), caching (the content-keyed store with one keep/evict policy per size), and serving (the window reads a finished file; nothing blocks on a decode) are designed together and proven on the real library before the area is recorded as rebuilt in `docs/REWRITE_LEDGER.md`.
- `open` "should we discuss the redesign/rewrite of the tiered thumbnail strategy? the idea is to precompute thumbnails on import (and backfill old images) with sm md lg thumbnails that are each served as quickly as possible where relevant to their display size to make that app feel as lightnight fast as possible. this was something worked on alot durring V1 but there is probably a more elegant shape in retrospect to the same ends." — 08-19 · **the tiers are the ask; the shape is open.** Precompute per display size on import and backfill, serve each at the speed its surface needs; the V2 design is discussed and settled before it is built (see the 08-19 design note in the session and the tiles rows in `docs/REWRITE_LEDGER.md` once landed).
- `open` "lets consider deeply the size and numbers of thumbnails per image that makes the most sense. having 1:1 thumbnails sounds nice for full screen viewing if the size/speed difference is resonable, but Im really not sure. its all about what will give the best user experience with the given hardware the software is running on and how much ram cache and ssd cache budget." — 08-19 · **tiers are derived from the display and the machine, not fixed by habit.** Settled in the 08-19 design note: a 1:1 tier is ruled out by arithmetic (≈12 MB × 157k ≈ 1.9 TB) and served by decoding the original on demand; the grid tier and a screen-sized loupe tier are the stored answers, with the loupe evictable under a disk budget.
- `open` "I have 64gb of ram so I imagine we can use that to accelerate large parts of the app? idk we have to think about it more." — 08-19 · **RAM is used, not managed.** The OS file cache and the browser's image cache already hold hot tiles; V1's user-space RAM caches were two caches in series. The deliberate uses are SQLite's page cache/mmap sized to the machine and a small decoded-pixel cache for the loupe/Develop neighbourhood — each measured before it is kept.
- `open` "taking the imbedded jpgs seems wise, dose that work well for lightroom edited DNGs if meta data is saved to the images?" — 08-19 · **answered in the 08-19 note:** saving metadata writes XMP, not a new preview; only *Update DNG Preview & Metadata* re-embeds an edited preview, and proprietary RAWs never carry one — so an embedded preview is the camera's rendering, and an edited photograph's tile must come from a render of the edit.
- `open` "for azimuth edited raws Id like the thumbnails to update after the images are edited to reflect those edits right? weather externally or internally the thumbnails should reflect the latest non-destructive edits" — 08-19 · **the edit is an input to the tile.** A tile's recipe names the photograph's current edit, so a new edit owes a new tile and the old one becomes stale cache; an external edit is a changed XMP the sweep notices. Falls out of *recipe is a pure function of its inputs*; built with Develop, not before.
- `open` "ideally edits from lightroom should translate 1:1 to azimuth and vice versa but ik thats a huge ask." — 08-19 · **held honestly:** metadata (rating, flag, keywords, crop, rotation) translates 1:1 through XMP; develop settings translate partially; exact parity is not promised. The cheaper route for *tiles* of LR-edited photographs — reading Lightroom's own rendition rather than re-implementing its engine — is measured before it is chosen.
- `open` "1 fixed size, 4k~ sounds resonable, (maybe settings options for 4k vs 1080p)." — 08-19 · **the loupe tier is 3840 on the long side, fixed; a 1080p option may follow as a preference.** The size is the loupe kind's one constant, so a preference later is one value, not a policy.
- `open` "2, for my use case everything is fine. the app frees up so much space by offloading raws that even a few hundred gb is a small price to pay." — 08-19 · **no loupe budget for the owner; a few hundred GB of previews is accepted.** The loupe tier is made ahead for everything; eviction stays a ceiling the owner may lower, defaulting to the disk.
- `open` "3, D yes, in the main catalog folder where all the other app data is stored, so its all neat and organized under one master path. (similar to a lightroom classic catalog)" — 08-19 · **one master home folder holds catalog, previews, and every other runtime file — Lightroom's catalog shape.** `AZIMUTH_HOME` is already that root; choosing and remembering it is the first-run surface, and nothing product-level may put runtime data anywhere else.

### 1.14 Culling outcome, video, first run, search

- `open` "Explicit reject → trash, you empty it" — 07-31 · **reject is a real verb with a real destination.** Culled frames go to trash and the user empties it, so storage stays honest and culling feels decisive rather than merely re-sorting. **Dependency, not optional:** this promotes trash to a load-bearing surface, and trash is exactly where this project has shipped its worst defects — "emptying the trash still doesnt work" (07-13) and the VC-trash P0. Reject must not become a primary gesture until emptying, restoring and counting are provably correct on a large selection. A reject the user cannot undo is data loss wearing a feature's clothes.
- `open` "important for import to support unloaded and storing and backing up video but dont worry about UI support at all yet, maybe way down the line" — 07-31 · **video is custody-only, deliberately.** Import, store, back up and never lose a video; build no browsing, scrubbing or playback UI on desktop. This is a stated asymmetry against the phone, which does play video. **Immediate consequence:** the flat grey 320×180 rectangle currently generated as a video preview (`web/features/imports/staging.py:290`) reads as a bug to every user and every agent — replace it with an honest video affordance. Not a non-goal, a *deferral*: the files must be safe now so that UI can arrive later.
- `open` "Ask which, then commit" — 07-31 · **first run forks once, at the top: existing library, or a card.** Someone escaping Lightroom arrives with fifteen years of photos; a fresh shoot is the demo. Both are real arrivals and onboarding must not assume either — today it silently assumes the first (`web/templates/setup.html:138`).
- `open` "all of them with both words and rich buttons filters and context based suggestions" — 07-31 · **one search that resolves people, places, visual meaning and technical facets, in one box.** Typed words and rich filter chips are the same system, not two; suggestions are context-aware rather than a static list. The hardest thing here is not capability but *latency* — under 1.2 speed is the release bar, so every added modality has to stay inside the search budget or it does not ship.

### 1.15 Storage policy, vocabulary, export, collections

- `open` "Evict by age only" — 07-31 · **the laptop cache policy, settled.** One ceiling the user sets, filled in priority order: every thumbnail first, then originals by recency with RAWs prioritised. Eviction is **pure age** — not export state, not sync state. Rejected deliberately: evict-once-exported, because predictability beats reclamation. The user should always be able to answer "what is on my laptop?" with "the newest N GB", without modelling what the app knows about export. Supersedes the "workshop it" hold in 1.3; this row is now buildable.
- `open` "Rethink the words entirely" — 07-31 · **the vocabulary is being redesigned, not merely harmonised.** Today one concept carries several names — Refine is the ranking game on desktop but a filter sheet on Android, Map versus Places, Picked/Favorited/Favorite for one column, and desktop draws `picked` with a star that collides with the Elo star. Sean rejected "make Android match desktop": the words are confusing on every client, not just inconsistent between them. **Blocked pending the proposed vocabulary** — one noun per concept, agreed with Sean, then applied across all three clients in one pass.
- `live` "1 b" — 08-21 · **the chip language:** chips AND across, values OR within (camera: RP *or* R5), any chip negatable. Built as `model/criteria.py` + the contextbar chip bar; typed query syntax can arrive later as the same criteria spelled in text.
- `live` "2 fixed and dynamic (smart) collections. both are useful." — 08-21 · both exist; smart collections hold crisp criteria only (no semantic terms — a search saves as a *fixed* collection via Save results, so membership is always explainable).
- `live` "3 a b and c?" — 08-21 · all three capture gestures: **B** tosses the selection into the pinned Quick Collection (toggles back out when everything is already in), right-click offers Quick / every fixed collection / New-from-selection / Remove-when-inside, and cells drag onto sidebar collections (smart rows refuse the drop).
- `live` "4 Freeze makes a dynamic collection into a fixed collection? with manual adds and subracts? useful for albums that are to be shared elsewhere" — 08-21 · exactly: Freeze writes the current answer as membership and drops the rules; from there add/remove by hand. There is no hand-removal from a live smart collection.
- `live` "5 a" — 08-21 · collections use the standard sorts; manual drag-order waits for Galleries, and the membership decision can carry a position later with no migration.
- `live` "6 … thoughts on nested and intersecting collections? … want it to be powerful without overcomplicated" — 08-21 · **the tree is a shelf, the algebra is chips.** A collection lives in one place (names nest by slashes; a parent browses as the union of its descendants, per the 07-09 ruling) while a photograph belongs to many; "Utah landscapes" is a saved intersection — two `in` chips — not a graph edge. Multi-parent graphs are dead.
- `open` "7. keywords seem difficult and too variable/under&overdefined." — 08-21 · **keywords are parked.** The sets primitive keeps the kind, no tagging UI is built, and search already finds photographs through collection names.
- `open` "Both equally, always interchangeable" — 07-31 · **folders and collections are two lenses on one surface.** Any folder view can become a collection; any collection browses like a folder. Neither is the "real" one. This is the most powerful option and the hardest to keep simple, so it constrains the UI: there must not be two different browsers, two selection models, or two vocabularies for the same act.
- `open` "Right, but Pick not Keep" — 07-31 · **the canonical vocabulary, one noun per concept, applied across all three clients in one pass:** Refine = the pairwise ranking game · Filters = narrowing what you see (Android's "Refine" sheet renames) · **Pick / Reject** = the cull verb, replacing Pick/Picked/Favorited/Favorite (Pick kept over Keep for Lightroom muscle memory) · Stars = computed quality · Places = the geographic page, Map is only the widget · Galleries = public website output (today `features/publish/`) · Deliveries = client, token-gated output (today `features/publishing/`).
  - **Two hard rules fall out.** The star glyph means quality and nothing else, so Pick/Reject use flags (which also matches Lightroom) and the desktop grid stops drawing `picked` with a star. And `publish` versus `publishing` must not survive: two different products with near-identical names, where grepping "publishing" lands you in the privacy-sensitive one by accident.
- `open` "I dont want images to get dubted... it would be nice if the exported where cleanly sorted into something more meaningful than just dates but idk what he right taxonimny there is" — 07-31 · **one edit, one file, filed by date; meaning comes from collections.** Exports land at `Edits/YYYY/YYYY-MM-DD/` with the filename derived from the original. Collection-named export folders were considered and rejected precisely because they duplicate: a photo in three collections would export three times. Instead the same law as 1.15 collections applies — the edit exists once and "Hollands Wedding" is a collection *containing* it, not a folder copying it. Meaningful grouping, zero duplicated bytes.
  - **Re-export never duplicates.** If an edit already exists for that original, export versions or replaces it and never writes a second file. This is the actual guarantee behind Sean's "I dont want images to get dubted".
  - **Client delivery is not archive structure.** Sending a wedding is a bundle generated on demand to a chosen destination, transient and outside `Edits/`. Deliveries never reshape the archive.
  - Recorded as Fable's recommendation answering Sean's direct "what do you think?" — the date default is his, the no-duplication architecture is the proposal. Reversible if it feels wrong in use.

### 1.16 Polish standing order, clustering, search cards, brand carry-through

- `live` "before we move onto the develop mode, I want to really focus on what we already have a dig alittle deeper into polushing and refining exsisting features" — 08-22 · **polish wave one executed the same day**: five parallel audits over every existing surface, 58 findings verified against source, the confirmed high-impact set fixed natively (one self-expiring toast for every message, cull verbs live in the loupe and the right-click, in-place multi-select culling, the Esc ladder, loupe zoom that survives background refreshes, filmstrip that glides instead of rebuilding, chip-editor lifecycle, honest shelf counts, rename that carries children, import dialog typing/keyboard/error fixes, first-sweep honesty, drive re-scan). Deeper polish remains a standing order, not a finished task.
- `open` "can you make the logo the shortcut, windows icon, window favicon etc? so its all pretty." — 08-22 · **done same day**: the compass mark rendered to a multi-size .ico (needle-only at 16 px), worn by the title bar and taskbar via WinForms icon + AppUserModelID, and by the desktop shortcut.
- `open` "suggested dynamic collections based off clusters, like best landscape photos, best nightscape photos, best of YYYY, best portraits of eris. and similar, but done in a smart super context and cluster away way?" — 08-22 · suggested smart collections from embedding clusters × ranking. Design brief owed before build.
- `open` "same for search like some sort of suggestions and little search cards for metadata, portrait/landscape, film/digital and other little cards that pop up in a drop down when search is hovered over or something like that you know? kinda like google photos but more professional and powerful." — 08-22 · search suggestion cards: metadata facets + clusters offered under the search box. Same system as suggested collections, per Sean: "the intelegent clustering and labeling of distinct clusters could be a very powerful feature for both search and dynamic collections (these two systems are very deeply linked)" — 08-22.
- `open` "perhaps this is  good time to start brainstorming about tagging or how to limit search/filter to more defined catoglories." — 08-22 · tagging/defined categories brainstorm requested; keywords stay parked (1.15 #7) until this design lands.
- `open` "also the search/filters and folders should limit the set for the refining (not sure if the search/filter currently works that way)" — 08-22 · **verified: folders, collections and chips already scope Refine** (one `viewing()` answer). A text search does not — Refine is hidden while searching. Whether a search should be refinable is an open design question tied to the clustering brief.
- `open` "id also like to subtly lean a bit more into the angualar modern areospace look just a tad." — 08-22 · first tad shipped same day: radii sharpened one step, one cut corner on the launch button, 2 px accent edge on active rows. Further leans stay subtle.
- `live` "face-crop strips on people rows. and what else can we work on around these sytems keeping elegantcey and simplicity in mind" — 08-22 · **face crops shipped same day (`02d29558`)** with zero new rendering: samples carry the tile plus a CSS view box from the detection box already stored — people rows wear the face as a round mark, drop strips show the person not the place, and the info panel gained a Names row (every name a photograph wears). The "what else" menu delivered in chat: sessions-as-clusters recommended next, rename/dismiss housekeeping, calibration parked for E:.
- `live` "is there a way to ensure that the thumbnail image for the person is a good image? like a centroid/mean of that person with good lighting?" — 08-22 · **shipped (`7d735d8d`): centrality picks the face.** Among the group's larger half, faces nearest the identity centroid come first — and because poor light/blur/turned heads embed *atypically*, centrality quietly favours the well-lit frontal look with no light meter, no blended score, no knobs. If dark avatars still appear on the real library, the next honest step is a stored per-face luma fact filtered at pick time — build the meter only when the need is measured.
- `live` "also dose it work well for images with multiple people? and searching for photos with combos of people?" — 08-22 · **yes by construction, and proven on real detections:** every face is stored and clustered independently, so a two-person photo wears both names; combos are the existing chip algebra — `≈ Ada` + `≈ Ben` chips AND (proved: exactly the shared photo), values within one chip OR, and the exclude toggle gives "Ada without Ben" (proved: 0, correctly).
- `live` "can we work on the people and faces part next? seems like a natural next step" — 08-22 · **shipped same day (`7dc04476`): people.** InsightFace ArcFace on the CPU (the card stays SigLIP's; resemblance ≠ identity, measured 51-68 pts apart). Faces are a worker cache kind from the grid tile; the rank lane groups them precision-first and rewrites whole; a person's name is one decision on the group's exemplar face, so it survives every re-grouping. Named people ride the same rows/chips/shelf/search-cards as tags; the unintroduced show as ◉ Someone and clicking asks their name. Proven end to end with the real model: six portraits → ◉ Someone ~5 through the pulse alone → named Eris in the popover → browsable ≈ Eris chip. "Best portraits of Eris" now composes from existing chips. Full-archive people arrive as the E: backfill feeds faces overnight on CPU.
- `live` "live smart/dynamic, until frozen, perhaps we can just call this clusters? data driven." — 08-22 · **ruled:** an accepted suggestion is a live smart collection until frozen; the sidebar section is named **Clusters**.
- `open` "rather than just how similar is this vector to dog does this image actually have a dog in it or not for certain? all these sytems need to be tied together elegantly" — 08-22 · **the labels-as-decisions design, approved** ("that sounds like a very clever and ingenious solution so long as we can make it intuitive"): the space proposes candidates with scores; a label becomes a *fact* only by confirmation or by crossing a threshold calibrated on Sean's own Y/N answers (a 60-second Refine-style flow per label, tuned to ~99% precision). Facts are decisions with provenance; corrections are decisions too. Below the line a candidate ranks but never counts or filters.
- `live` "on vocab, can we have the labels be suggested by clustering and then refined by the user?" — 08-22 · **ruled:** the vocabulary is bottom-up. Clusters form unnamed, each proposes its own name from a naming palette auditioned against it, and Sean refines by renaming / merging / rejecting — each refinement a decision. No vocabulary management screen exists.
- `live` "the labels should not be exlcusive they should be more like tags" — 08-22 · **ruled and built that way** (`33019587`): a photograph wears every name nearly as strong as its strongest, capped at its best few — one JSON row per identity. The margin is interim by design; calibration replaces it with per-label thresholds.
- `live` "I also really wanna keep it bottom up data dirrived and as smart as possible" — 08-22 · the vocabulary is what the library turns out to contain: only names whose groups actually form are proposed, and kept names leave the proposal list. **Deepened same day (`679c0a9d`):** k-means over the space itself surfaces the groups no single tag caught — named by their two nearest terms, gated by whether the tags already told the story, riding the same rows as every tag — and proposals carry faces (their three best-ranked tiles) on the search drop. Real-library read-only check: the misty-hike trip cluster (~52), interiors · architecture (~56), children the tags missed (~14); 22 of 31 groups correctly skipped as already-told.
- `live` "when I begin to search something I want it like google photos where it auto suggests based on likely clusters or something?" — 08-22 · **phase 1 shipped same day (`7cebd3fe`): search cards.** Focusing the box offers the library's shape (years, cameras, kinds, orientations, collections, recents); typing narrows the offers while the meaning search runs underneath; every card applies as chips (countable, saveable, scopes Refine); Enter always means the meaning search. Orientation joined the chip language as a closed-vocab field. Phases queued: 2 = cluster proposal cards + Clusters sidebar section with tilde-counts (needs vectors; whole archive after E: backfill), 3 = calibration flow → labels become facts and counts drop tildes, 4 = faces (ArcFace) → people as labels.

### 1.17 The organizing-model reset (08-22 evening, from live use of the real library)

- `open` "this seems a bit much we at the very least need collapsible sections and only show a few unnamed people, also the images for the people are too small and clicking on them doesnt filter to show images of them so its hard to even name them. also the clusters seem a bit too much, maybe theres a better place or view for it? idk what do you think?" — 08-22 · the first live-use verdict on the Clusters shelf: sidebar overload, 18 px faces too small, click-should-browse-before-naming.
- `live` "also for some reason the app wont come to the foreground I have to minimize claude to seee it" — 08-22 · **fixed same day**: the icon code set a WinForms property from pywebview's event thread; WinForms is thread-affine and the cross-thread write corrupted window activation. Rewritten as WM_SETICON SendMessage — Win32 messages marshal to the window's own thread by design.
- `open` "also once a collection is selected how to I remove or add images to it, that dont fully fit it?" — 08-22 · fixed collections: right-click Remove exists when inside; adding happens from anywhere (B / drag / right-click). Smart collections: **exceptions designed, not yet built** — membership decisions already store true/false rows, so smart = (rules ∪ forced-in) ∖ forced-out is one change in `criteria.resolve`; drag-in and Remove become legal on smart. LRC cannot do this; part of "smarter".
- `open` "whats the workflow for creating a new smart collection? when I click create it tells me to select a folder, I cant just drag selected images into it? also would be nice to have a feature that recomends similar shots to add to a collection" — 08-22 · three findings: (1) the "select a folder" he saw is the generic empty-state showing inside an empty collection — the empty state must know it is an album view ("Drag photos here, press B, or right-click any photo"); (2) the intended smart flow (build chips → Save view) is undiscoverable from the + button; (3) recommend-similar has a clean path — `find()` already accepts a query *vector*, so "more like this" is search seeded by photos instead of words, and an album's suggestions are its member-centroid's nearest non-members.
- `open` "I think we need to still keep thinking about quite how the collections and clusters work and appear to the user. lets take heavy inspiration from LRC but just make it smarter" / "or im confused, labels vs smart collections etc" / "I think we sitill need to work on that whole feature design, collections clusters, smart/fixed, maybe we call them albums? labels, people, how these all mix together, whats the real elegant shape here? not just for code but for good UX, it should be simple to understand yet powerful" — 08-22 · **the design reset order.** Build paused mid-flight (half-built Clusters stage reverted; server-side survivors kept green). The proposal on the table: kill the proposed/kept state entirely; **Albums** (plain + smart — LRC collections, friendlier word), **Labels** (keywords that write themselves — LRC keyword list, auto-filled, tilde until calibrated), **People** (LRC People view: named in sidebar, a face wall to introduce Someones), chips unchanged as the one filter language. "Clusters" dies as a user-facing word.

- `open` "Can I create smart album using a keyword, (target vector) and then as I refine what is and isnt, it learns via the vectors? … much like the ranking system, where we have predicted, and earned. lets keep brainstroming hard on this. I really love google photos and lightroom classic … I want to find the middle ground and research other photo tools and apps … also think about sessions and how they relate and how all these differnt ways of searching and organzing can play together nicely and elegantly." — 08-22 · **the organizing design, researched and written: [docs/ORGANIZING.md](docs/ORGANIZING.md)** (`06cd7655`). Ten tools surveyed (Google/Apple/LrC/C1/PM/Excire/Peakto/PhotoPrism/Immich/Mylio/ACDSee); two probes measured on the real library (teach loop: 0/20 → 9/20 in fifteen keystrokes on a word-blind concept; sessions: 173 dated photos → 11 legibly auto-named shoots). The market gaps the design claims, per the research's own words: no tool lets a smart set take exceptions ("a decade-old workaround"), and no rules engine learns from corrections ("genuinely novel"). Awaiting Sean's read before the build resumes.

- `live` "I do think labels are a good primative, that can be a part of smart collection filtering. tagging/labels are great but only if they can be dynamically adjusted. say I create the label starbase, it could be rockets, beach construction sites or other stuff that might be hard for the embeddings to figure out without any help but if I can refine what that keyword/tag means over time that could be really helpful and make search stronger. smart albums can be created by filtered sets and keywords/search terms. but if we treat the labels like normal search terms and give them a place to learn and get better that would be really good as this type of search is good but not perfect. So lets say I search cat and theres 90 cats and 1 dog, I should be able to hit no on the dog and next time that keyword is used anywhere it will be smarter. with this we can removing the clustering suggestions, and make the smart collections just simoled a saved query" — 08-22 · **the locking rulings.** Labels = learnable primitives born by teaching (first ✗/✓ on a term's results creates the label); one formula unifies labels and smart albums: (derived ∪ pinned) ∖ denied; smart album = simply a saved query; **clustering suggestions deleted** (palette auto-tags, k-means groups, proposals shelf, Keep). People and Sessions survive — identity and time are facts, not suggestions. docs/ORGANIZING.md updated with the consequences.

- `live` "this sounds great. I really like it. lets move on, as far as refining the labels, maybe you click on a label or search it and we have a button to refine it. (lets change the other refine verb to rank) as you are ranking the photos and refining is refining what the labels mean and theres a way to both confirm/anchor and reject/exclude photos from that label" — 08-22 · **the UI design blessed, and the vocabulary settled: Rank = the pairwise game (was Refine); Refine = teaching a label what it means**, entered from a label's view via a Refine button, with anchor (✓/Y) and exclude (✗/N) as its two verbs. Supersedes the 1.15 vocabulary row's "Refine = the pairwise ranking game".

- `live` "when names change change the code names too otherwsie it gets super confusion, ux terms should always match internal terms" — 08-22 · **the vocabulary law.** Executed the same night: Collections→Albums, Refine(game)→Rank, clusters→People across verbs, stores, CSS, selectors and files (`collections.js`→`albums.js`, `refine.js`→`rank.js`, `clusters.js`→`people.js`). The one deliberate seam: *stored* strings never rewrite (the decision log is append-only), so `sets.ALBUM = "collection"` and `sets.LABEL = "keyword"` — constants speak the product word, bytes keep their history.

- `live` "take care of all of that stuff, and then work on browsing preformance for queryies, grid loading scrolling, ranking latency, image loading, etc. its all got to feel instantly fast. thats where the magic is, as soon as a ranked photo is pressed the next one should be there. likewise for loupe mode, film strips, navigating around the grids, clicking, every interaction has to be as fast as possible. (and please do so in a clean simple elegant way, not a bloaty mess)" — 08-22 · the speed directive. Measured at 155k synthetic rows before touching anything: folder tree 30 s (correlated GROUP_CONCAT per image → one streamed LEFT JOIN, 0.5 s), deep scroll at offset 60k 290 ms (→ covering id-walk, 5 ms), counts 1.1 s (partial indexes carried only half of IN_LIBRARY's predicate → respelled with both halves, 10 ms), shelf re-counts on every worker tick (→ only when a sweep or lane rewrite moved them). Every count on the browse surface now counts rows, one unit everywhere; the one deliberate DISTINCT left is Rank's total, which must speak `judged`'s identity unit.

- `live` "continue working on all the scopped work without stopping or inturepuptions. just work on your own for awhile" — 08-22 · the autonomy window this wave shipped in.

### 1.18 The second live-use wave (08-23, from working with the shipped organizing build)

- `live` "when I exclude/anchor a label, when that label is filtered can it update right away and remove those shots/recompute?" — 08-23 · **yes, now**: teach() recomputes the one word synchronously against the memoized space before returning (74 ms at 150k rows, measured), and the view refreshes right behind it — an excluded photograph leaves the word's view at the keystroke, proven in the window (4 photos → 2 at N). Three deeper faults surfaced and fixed on the way: a collapsed head (contradictory teachings) flooded the label — a head that ranks anything denied above an anchor is no longer trusted to extend; searching a taught word now omits its denials everywhere (the "search cat, N the dog" ruling made literal); clicking a photograph now moves keyboard focus to the grid so Y/N always land.
- `live` "How can I merge two people together?" — 08-23 · **name them the same name.** The backend welcomed it but the shelf showed two rows; `repeople` now folds same-named groups into one person (union photos, one avatar from the combined faces), and renaming a healed person propagates to every face that answered to the old name — it can't be split back apart by renaming one half.
- `live` "in the import dialog we need a way to multi select via cntrl, shfit etc same keybindings as lightroom and as the grid. and to only import certain dates etc." — 08-23 · the stage now groups photographs under day headers whose own checkbox imports exactly that date; click selects, Ctrl adds, Shift ranges, Space toggles, and a checkbox ticked on a selection answers for all of it (Lightroom's import hands, the grid's one grammar).
- `live` "also the x on the import dialog to close it doesnt work" — 08-23 · the X (and Esc) now always closes: it puts the conversation away, never the work — a running import continues and its outcome arrives as a toast. (The old rule refused to close while running with a message Sean's eyes were not on.)
- `live` "the latest photos I just imported from my sd card say undated? can we sort that out? then next I want to build out the editing/develop I want it to be nearly a 1:1 to lightroom classic, (ideally compatible with loading and editing lightrooms non destructive edits too. (LR timelapse does this well) this is the other half of the app, organizing/culling/searching, and editing. so lets take our time and do it right, with elegancy, grace and beautiful simplicity. i really want it to be 1 to 1 from lightroom classic if possible." — 08-23 · **undated fixed** (`242a684b`): in CR3, Canon's CMT2 box *is* the Exif IFD — the reader walked it with IFD0's map, so all 657 CR3s in the library knew their camera and never their date or lens; fixed against the real R5 file, dateless answers drain at next open. **Develop designed**: [docs/DEVELOP.md](docs/DEVELOP.md) — the census of Sean's 580 real sidecars is the 1:1 target (569 crop-only; 11 full PV15.4 **HDR-mode** edits, zero masks), edits = decisions in the crs dialect, renders = cache rows, one NumPy pipeline (V1's harvested), LRTimelapse's three sidecar rules, acceptance = before/after pairs against his own edits. Build order: crop → read-path → tone (measured) → color → detail/lens/effects → round-trip → export. Awaiting Sean's read.
- `open` "also btw for the taste ranking, can we use the face embedding model aswell to more granualy learn what facial expressions make great photos? is there a way we can elegantly work that in? what do you think?" — 08-23 · design answer given: ArcFace vectors are the wrong ingredient (identity embeddings are trained to be expression-*invariant*); the right one is SigLIP over the stored face *crops* — no new model, boxes already stored — added as a second feature block in the taste fit. Per the 08-17 taste-probe doctrine, measure lift on Sean's own duels before wiring it in; probe queued behind the E: backfill.
- `open` "also for the ranking mode I want to add selection modes, like tournament, random, diverse images." — 08-23 · queued; V1 precedent exists (roadmap §4 "selection modes are small pure strategies over one candidate contract"; the dev-noise lane's diverse-spread fix). Lands as strategies in rank.candidates + a mode control in the Rank stage.
- `live` "once import starts can I close the import menu? or have it auto close" — 08-23 · **auto close.** Clicking Import ends the conversation: the dialog puts itself away, progress rides the status line ("Importing 34 of 120 · 2.1 GB · ~3 min left"), the outcome arrives as a toast, and Import… while it runs is the door back to the details (and Stop) with no folder picker in the way.
- `live` "lets build out all 3... also for the tags, once its searched could we save those tags into the metadata in some way visable to lightroom? (also stars based on ranking and pick/reject flags supported across lightroom?" / "I also want a filter chip for color photos, b&w and serpia photos." — 08-28 · **shipped as four laws** (f6340ef6, 758ca1b8, 8855c2f7, bbc9d99b): (1) a stack is a run of frames at one cadence — >=4 frames on one exact interval collapse behind their first frame; images.stack_of projected like every decision column; cover badge = the door in via a stack chip; Rank's default draw collapses too; midnight dates carry no cadence. (2) The camera knows when, the phone knows where — GPX tracks place dated photographs by interpolation as place decisions; EXIF GPS now reads (raw GPS IFD + Pillow path) and always wins; "Add GPS track (GPX)…" in the folder menu; inspector shows Place. (3) The photostats kind measures what the embedding throws away (lit-pixel chroma, hue focus, tone, sharpness — calibrated on real tiles: shadows are neutral in any photograph, and sepia must be warm) and its derived word is the Look chip: color / b&w / sepia. (4) The sidecar carries the verdicts Lightroom reads: stars = xmp:Rating, taught words + people = dc:subject ∪ the file's own keywords, a pick = the Green label — XMP has no flag field LR reads, so that is the honest stand-in; rejects are Trash and do not travel. Proven in-window: a real burst collapsed behind "▤ 4", the chip stepped in and out, Look=bw matched 3, all suites green (152).
- `live` "great, next hwo can we minimize the deadspace?" / "is there a good way we can compare images of differnt aspect ratios seperately?" / "on the ranking mode when the mouse is hovering over an image can I hit r to quick rotate it 90 degress like in the browse mode?" — 08-28 · **shipped**: the equal-area law kept, the uniform grid gone — cards shelf-pack (widest first, rows as tall as their tallest member) at the largest per-card area whose packing fits the stage, found by bisection; the black goes to the photographs. Aspect separation is one sentence, not a mode: *a set wears its anchor's orientation when the scope can dress it* — a stable partition after each mode's own ordering, which also deleted the pair-only special case (and an orientation chip + Rank already scopes to one shape exactly when wanted). R over a hovered (or selected) card turns it 90° in place and the shelf re-packs. Proven in-window: swaps still 122–135ms, all modes deal, data-turn=90 after R.
- `live` "x and p should also work in the ranking screen on over over an image" — 08-31 · **shipped**: the grid's per-photograph verbs all follow the mouse in a round now — one card-under-the-mouse lookup (falling back to the keyboard's selection) serves R, P, U and X. P and U mark and stay; X is out of the running, not just marked: the card leaves the round, its seat goes to the next photograph in hand, the rail's counts take the same arithmetic nudge the grid gives them, and Undo puts the set back as it was. Proven in-window with a real cursor: hover beat the keyboard selection, picked/cleared/trashed read back from the catalog, the stage repaired to 9, Trash 0→1→0.
- `live` "Id like you to come up with a new selection mode for the ranking system, I want this to be the smartest mode yet, and not by guessing but by simulation testing and math... what gets us to the best ranking as quickly as possible?" / "also can we make the quick rotate hotkey rotate it the other way? (more common)" — 08-29 · **shipped: Learn, the new default mode, chosen by tournament of strategies** (scripts/sim_learn.py: 7 strategies + syntheses, 3 seeds, 600 rounds of 9 over 2,000, softmax-consistent clicker, scored on whole-order rho AND top-decile recall). The winner is a mixture with one sentence per half: teaching rounds deal *the most uncertain photographs that are most evenly matched* (a window slid over the rating-sorted pool to maximum total 1/sqrt(1+rounds) — information theory's answer for a softmax model, and coverage falls out free because the unseen are one flat window); one round in three, once everything has been seen, *finds among the bubble* — the least-worn nearest the top band's cut, because membership of the top is decided at its boundary. Measured through the real pipeline: rho .396 / top-decile .442 — within noise of each specialist at its own game (window .406 order, diverse .455 finding) while beating every mode decisively at the other's; also best at 100 rounds on both. The port was verified against the sim head-to-head (a tiebreak spread over the band's top instead of its bubble cost 7 points of recall — found and fixed by construction), and the hunt surfaced a real pool bug: the wrap-around could seat the same frame against itself (deduped for every mode). R now turns left, Shift+R right, everywhere.
- `live` "on the compare mode can we work on the latency between click and new image? it needs to feel instant. also... the diverse mdoe seems to repeat lots... all the modes should take in to account what photos have already been rated lots and what havent" / "maybe you can run some simulations of the differnt modes and explore the dynamics" / "also the side panels need nice big buttoms to collapse and reopen them" — 08-28 · **shipped, tuned by simulation** (scripts/sim_rank.py, 300 rounds x 9 over 2,000): diverse repeated 42% of its shows because farthest-point orbits the embedding hull's corners — one law fixed every mode: *a round spends its seats on the least-judged photographs the mode's own idea can find*. Random and diverse now walk the scope in perfect epochs (repeats at the theoretical floor, max wear 2), tournament's crown circulates its top-3n court (learning rho 0.86→0.92), close breaks rating ties by wear. Latency: the compare log was JSON-parsed twice per ask (memo: 11ms→1.8ms warm on the live catalog; full diverse ask 13ms) and the round's write sat on the click path — it now records underneath while the next set comes from decoded-in-hand photographs. Measured in-window: ten picks, every swap 122–130ms (the 120ms hold + one frame). Panel tabs grew into real buttons (24×120px, bordered, visible).
- `live` "when I goto import a folder selection window opens, I select the card, and it just says looking at photos at the bottom then acts like nothing happened. wheres the import UI?" / "it currently requires scrolling all the way to unselect old dates, could we have a small feature to import only the newest since last import?" / "can we make the import UI like lightroom and have it use the main grid and layout? like make it its own workspace and reuse most of the exisiting UI?" — 08-28 · **shipped**: Import is its own workspace — the stage lives where the grid does (sticky day bands, the grid's grammar), the panel sits in the inspector's seat, Esc steps back, the view-title says Import. "Nothing happened" was a 16s+ card scan behind a static status line: staging now counts as it reads ("Looking at E:\ — 1,000 photographs…"). "Newest since last import" was a bugfix, not a feature: suspicion matched name+size but the import renames files, so nothing ever looked suspect — it now also matches capture second + size, and the fresh-only default just works. Plus: the card chip never appeared for the CFexpress reader (mounts as a fixed disk; DCIM is the signal, not the bus). Proven on the real card: 2,535 staged in 10 day bands, 3 imported (card untouched), second look suspects exactly those 3, re-import of them skipped by identity.
- `live` "lets put the develop stuff aside for now and focus on the other parts of the app, specifically synicng folder, with dng files. I just imprted many photos in lightroom that I want to sync, also alots ofthem were shot with a cool in camera custom profile how can we see that? and how should/can we handle photos in differnt edits or styles in camera?" / "okay nvm ill delete those photos from lr and reimport them via the card into azimuth directily once thats working." — 08-28 · **shipped the sync half**: the 2,086 LR-converted DNGs sat undated because Lightroom's DNG writer puts the Exif IFD after 38 MB of image data and the raw reader stopped at a 4 MiB header — the reader now maps the file and follows the IFD graph wherever it points (CR3's box scan keeps its bound); a boot drain re-reads every dateless raw. A DNG is now its own sidecar: adoption reads the XMP packet inside unheard and rewritten DNGs, with one empty receipt for a settings-less file so sweeps never re-read megabytes forever. The sweep already recognized LR's in-place rewrites (same tail, new bytes → carry decisions). Profile truth: Copy-as-DNG discards the camera rendering (Adobe re-renders previews; style name survives only as numeric maker-note IDs) — reimporting CR3s directly is right, and Azimuth's tiles come from the camera's own embedded JPEG, so the in-camera profile is exactly what the grid and loupe show.
- `live` "can we add multi slecting via cntrl and shift to the folder tree? for example selecting two days for a shoot that spanned across multiple days?" — 08-23 · **shipped**: the tree speaks the grid's grammar — click browses one folder, Ctrl adds/removes (a two-day shoot is their union), Shift ranges across the visible rows; both rows light, the title names both ("2026-04-18 + 2026-04-19"), day chapters and counts cover the union, and Save view folds the whole selection into one folder chip with several values (the criteria language already spoke plural). The view's folder became `folders` end to end — one noun, plural — and the tree data was renamed `tree` so the words stay honest. Proven in-window: Edits (12) + Ctrl Personal Photos → "Edits + Personal Photos" (24), toggle back to 12.
- `live` "complete the developing/editing workspace." — 08-23 · **DONE, proven in the window** (`dedcb928`): the whole RENDERED crs subset renders through V1's pipeline (display base; identity-exact when untouched), each surface at its own size — scrub on the grid proxy, instant grid answer from the verb, the worker's loupe at LR's standard-preview 2048. The edit panel: D over the loupe, Light/Color/Presence/B&W in LR units, drag = a look-file preview (data URIs died silently in the WebView2 bridge past an unnamed size — a found-and-fixed real bug), release = one decision, double-click/Reset rest sliders, panel rides the arrows. Write-back merges the crs surface into sidecars keeping every other block — the LRTimelapse loop closed both ways. The tone-parity gap (0.0445 L, lighter/flatter than Adobe) is the phase's remaining refinement; the workspace inherits every future pipeline improvement for free. 138 tests green.
- `live` "I trust your judgement, build out develop and implement those other asks. carefully and with great elegance, clean modular code. and take care of any other housekeep or if you spot a good opotunity for refactoring somethings. I just want the codebase neat and tide so development goes as smooth as possible. im heading out today so ill leave you to it good luck!" — 08-23 · the day's autonomy window. Landed: **Develop phase 1 whole** (`2d58766a` — sidecar adoption on the sweep, edits as file-authored decisions in the crs dialect, the `keyed` rendition recipe so an edited photo's tile is its own answer while the plain one survives for the embedding, the C crop surface with draw/resize/move/apply/full-frame, synchronous cut-from-loupe so the edit is instant; proven in-window 1.78→0.99 on the exact rectangle), **Rank selection modes** (close/random/diverse/tournament as pure orderings over the one pool, segmented control), the CR3 date fix + drains, and the phase-3 probe receipts written into DEVELOP.md (pipeline+xmp_write import clean; profiles/dng chains and the acceptance harness need de-V1ing first — that is the tone phase's first task).
- `live` "in the ranking mode, sometimes it seems like portrait shots are cropped to landscape, and also in the libary there are several images that aren't rotated upright… Id like a nice simple rotate button for it" / "i used r to rotate the film scans propperly" — 08-23 · root cause found on the live catalog: **27 photographs (the Eris film scans among them) had a transient "database is locked" stored as their permanent metadata failure**, so they never learned their shape and Rank cropped their assumed-landscape cards. The law is fixed — a projection failure never touches the computed answer (reindex re-projects; the moment is retried, the poison rows drain at next open) — and Rank cards letterbox rather than crop while a shape is still unknown. A visible **R Rotate** button joined Pick/Clear/Reject in the cull bar (Shift-click turns left).

### 1.19 Finishing the deletion, and two live-use asks (09-08)

- `live` "finish the deletion, get the gates and ledger green, and maybe its time to move this branch to the main branch? v2 has far surpassed v1 by now right?" — 09-08 · **the order that closes the rewrite.** Every inherited file leaves the tree or is registered as Proven with its evidence; the gates and `scripts/rewrite_status.py --check` go green; then `v2-carve` becomes `main`. Done on branch `v2-finish` in the canonical checkout while the app kept running from the sandbox tree. · **done 09-08 (`8254ba03` … `b177a9b1`): 368 files left, gates 0/0/0/0/0/0, 264 tests green, `main` fast-forwarded and pushed; the ledger reads Proven or Removed for everything but `android/`, which is one decision away**
- `open` "btw the app is currently running, doing a bunch of real work, so be careful" — 09-08 · the running app imports lazily (the sidecar writer, the colour pipeline, the model loaders), so its source tree is not touched while it runs; the work lands in a second checkout and reaches the app at the next relaunch.
- `live` "can we work on the little status message on the left panel that displays what the app is currently processing and some stats about it so its nice and clear always?" — 09-08 · the status line is one writer over `pulse()`; the ask is that it always says plainly what the worker is doing now and the numbers that matter, never a blank or a stale line. · **shipped 09-08: the worker reports its kind and its own count of what is owed on the pulse; the line reads "Making tiles · 31 left · 30 / min" and, at rest, "Up to date" (the count left the line 09-09, F14: three other surfaces already say it) — proven natively**
- `live` "there is a small issue with the stack mechanic, when I use interval timer is is often with a variable shutter speed, and that plus the autofocus changes the timing between shots, can we account for that a bit and still reliably detect those sort of interval and full auto burst stacks?" — 09-08 · the cadence law (1.24, "one exact interval") is too strict for real intervalometer runs: exposure and autofocus jitter the gap by up to a shutter length. The fix must tolerate that jitter without gluing separate shoots together. · **shipped 09-08 (`86fdd0be`): a gap keeps the beat within two seconds or a third of it, sized from the working catalog's own jitter (285 → 367 runs); a stroll still is not a set**
- `live` "opening a stack and then closing it, looses your place in the orignal grid, should be saved" — 09-08 · a stack is a chip lifted over the view; leaving it must land on the cover you came from, scroll and focus intact. · **shipped 09-08: stepping in remembers the cover, its index and the scroll; the × and Esc land back on it, selected**
- `live` "also when sorted by newest/oldest along the side should be a timeline similar to google photos based of the dates of the photos in that view" — 09-08 · a date scrubber on the grid's right edge for date sorts, built from the view's own day chapters (`library.days` already answers the index arithmetic). · **shipped 09-08: a rail at the grid's right edge on newest and oldest, years and months placed by index from the chapters' own day list, a marker riding the scroll, press or drag jumps with the day named; proven in the harness and natively**
- `open` "gotta focus on the little tiny details that make all the UX magic, like notion levels of polish in all the smallest UI details, (while adhearing to the elegancy focus)" — 09-08 · the standing polish order restated for V2: every detail sized, keyed and remembered; elegance is the bar for how it is built.
- `open` "How can we make the codebase easier to work in?  Also I think Ive asked enough from you for tonight, continue working on all of these requests thanks!" — 09-08 · answered in the same session's report: the V1 tree is gone, the map in `AGENTS.md` names every file's job in one line, one command checks everything, and the gates make the dependency direction mechanical. The rest of the asks proceed unattended.
- `live` "is it possible for you to test the app headlessly in the background or some sort of virtual evnioment or something where it doesnt itterupt what im working on my computer?" — 09-08 · yes: the UI is one document, so a stub bridge runs it in a headless browser tab with fake data (`scripts/harness.py`); the engine is proven through the real bridge without a window (`test_desktop.py`); a native window is launched only for a final look and is moved off-screen the moment it appears. · **shipped 09-08: `scripts/harness.py` (the UI in a browser tab, no window) and `scripts/native_proof.py` (the real app off-screen, probed and captured)**
- `live` "yeah delete android, and remove the sandbox worktree, stacks should collapse and expand in grid. then hunt areas of UX improvement, UI refinement, and preformance, lets do a HARD polish pass." — 09-08 · android deleted (the ledger reads Proven or Removed for every file), the sandbox and every stale worktree removed and the app relaunched from `main`; stacks expand and collapse in place; **the polish pass shipped 09-09 (`9a21e696` … `de651a24`): every row of `docs/FINDINGS.md` is shipped, measured or rejected with its reason — performance (indexes, one projection writer, repairs behind the paint, viewport-only refresh), the grid's selection and cursor, the loupe, Rank, the sidebar, one palette and one row, the faces shipped in the document, and every document true at HEAD or archived.**
- `live` "is ther a way you can work faster across many agents without sacrificing code quality or creating a bloated mess etc?" — 09-09 · yes, with one rule: audits and reads fan out freely; fixes fan out only after the shared shapes are landed by one hand, one surface per lane, each with a frozen finding list and the harness as proof, every diff read before merge. Parallel lanes over one primitive produce N local answers, which is where bloat comes from.
- `live` "wheres the full list of findings and how are those all being tracked and adressed? we need elegant reliables processes to drive development to make this app as good as possible" — 09-09 · **shipped 09-09 (`3c4ba0e6`): `docs/FINDINGS.md`**, the twin of this ledger — every audit finding with its user impact, fix and rank; a row closes only in the commit that does the work, whose subject starts with the row's number; rows are never deleted.
- `open` "review ALL docs, and clean up the docs why are there so many docs? are they useful, up to date and accurate? or slop? (add this to the list of problems to fix. and fix all problems that ought to be fixed." — 09-09 · row D1 in `docs/FINDINGS.md`; three read-only auditors over every document, verified against HEAD, then one hand folds and deletes.
- `open` "keep fixing all problems that ought to be fixed. and if you run out of things to fix come up with more. dont stop." — 09-09 · the standing order for the polish pass: the ledger is worked in rank order, and when it runs dry a new audit runs against HEAD to refill it.
- `live` "also by default lets have all stacks expanded, and maybe add a border around stacks or something like that? I feel like the stacks as they are are way to agressive and overgroup? maybe just manual stack creation? whats the smartest way we can do that" — 09-09 · **shipped 09-09: a stack is a decision.** Nothing is stacked unasked; S on marked frames makes a stack, S on one frame stacks the burst the cadence law finds around it (the law lives on as the proposal, never the grouping), Shift+S takes one apart, every one with Undo. Stacks are open by default with a band around each; the badge folds one, a bar toggle collapses all (remembered). Rank still draws covers only, so a burst never floods a round.
- `live` "all of those other suggestions sound good, and lets continue to work on polishing the UI and UX, I want to really bring it next level polish in every interaction" — 09-09 · **the eight appearance moves shipped (`ad96aa18`, A1–A8)**, then a third audit of the feel of every interaction (F1–F44, U6–U14 in `docs/FINDINGS.md`) **shipped 09-09 in three commits (`8446f74b` the grid, `43db615c` sidebar/bar/rail/tabs, `0ae7a92f` loupe/search/toast/inspector/type/keys)**; F15 rejected (view controls right, verbs left, as in Lightroom). F44 was found on the way: the mark's ring and the hover line had been invisible since A1 (an inset shadow paints under the image) — now drawn on a veil above it. **A fourth audit (Rank, Import, People, Develop, Crop, strip, dialogs, drag, chips, Trash, toast copy: Q1–Q34, U15–U28) and a refutation (X14–X25) shipped the same day (`15a66791`, `7fbe93a6`, `527f70eb`)**: the keyboard is never stolen by a focused button, the cursor outranks the mouse in Rank, Undo wherever a set changes, one menu placer with the keyboard (`kit/menu.js`), a roving filmstrip, the crop takes the keyboard, Rank's settings in one shape with keys. Open: U16 (the rank stage's verbs through cull.apply) and KB1 (owner's call). Lands at Sean's next relaunch.
- `live` "also now the left panel button opens and clsoes the right panel, so be careful and diligent in each thing" — 09-09 · cause: for a few commits the fold tabs sat outside the shell that defines their seams, so all three stacked on one spot with the right one on top. **fixed `17643185`**, proven natively: left tab at the sidebar seam folds the sidebar, right tab at the inspector seam folds the inspector.
- `live` "keep going, run another audit and fix everything it finds" — 09-10 · **shipped 09-10 (`0f21a162` feel W1–W40, `758f42ee` perf P12–P20 with `scripts/bench.py`, `183ddb89` W14/W19/U36, and the craft rows N1–N4)**; W1 resolved KB1 (Tab folds from the photographs, traverses from a chrome control). Fifth round: a product-boundary performance audit on the 150k synthetic catalog (P12+) and a feel audit of the surfaces no round had covered (dialogs, search drop, topbar, loupe zoom, drag targets, folder tree, every toast string, screen-reader semantics: W1+), both read-only, then fixed by one hand and refuted before push.
- `live` "can you come up with a development strategy/process in order to get notion level UI polish without my feedback?" — 09-10 · answered and folded into `AGENTS.md` (How a polish round runs: one round in flight; audit → ledger → one hand → refute by another model → native proof and bench; a medium fix states its sentence and its kill-list; a dry ledger widens the lens). No new document.
- `live` "how can we make it even better ... without excessive bureaucratic rules ... at this size of app its important to keep things manageable" / "how else can we refine our development processes in line with the spirit of high quality clean nimble code and extreamly UI/UX and preformance quality?" — 09-10 · the lean version: the standard lives in the doc that already existed (`docs/ui-architecture.md`), journeys are probe files, reviewers are prompts, gates are greps, numbers live with the command that produced them (`scripts/bench.py`), the harness stub must throw rather than answer null, deletion has a quota, and every batch is refuted before push.
- `live` "can we study the development of notion and other apps know for their extreame quality and learn how to apply that to our app? how do we fill the app with love for the user in every little detail?" — 09-10 · studied (Notion, Linear, Superhuman, Raycast, Browser Company, Rauno Freiberg, Emil Kowalski, Nielsen, Lightroom Classic, Capture One, Photo Mechanic); the numbers and practices are now the Speed covenant and Interaction canon in `docs/ui-architecture.md`, and the first four rows it produced shipped (N1 a single flag's way back waits silently on Ctrl+Z, N2 Lights Out on L, N4 a press is felt; N3 already so).
- `live` "Great can we agressively apply these learnings?" — 09-10 · **shipped 09-10 (`edb11139`, `5f5359e8`, refuted in `6c97fb37`)**: nothing shows for a wait under 200 ms; the loupe is instant and one easing curve is a token; E opens the loupe and G is the grid from any stage; the filmstrip stays put while the frame is in view; the loupe teaches its keys on its first three opens; a toast waits under the hand; no text selection on the stages; tabular counts; N surveys the marked burst in Rank; `.` puts the next face at 100% (a `faces` verb over the face pass); the harness stub throws instead of answering null.
- `open` "thouse all sound great do all of them, and the diverse mode doesnt seem very diverse. and the learn mode seems to show me mostly the lowest rank images alot, I care much more about finding the top than the bottom? idk" — 09-10 · the six next steps (a real-shoot roast, persona audits, a journey gallery, the two-pass cull and first-run choreography, the owed shapes, the monthly numbers) all taken; and two Rank findings from his screenshot: Diverse dealt five near-identical night-lake frames of nine, and Learn keeps dealing the bottom of the ranking. The ask is to find the top: draws should spend their rounds among the leaders and the unsure, never re-litigating the floor.
- `open` "Also deeply research and study how we can make the taste algo smarter, more nuanced, and be able to really learn compositions face expressions, color harmoury etc and other photogenic quality. constantly think hot to make the app smarter, more elegant, more preformant, more filled with love and grace." — 09-10 · a research round on the taste head (what the rounds teach, what the encoders see, what a photographer's eye weighs: composition, expression, colour harmony, moment), measured against his own duels before anything ships; and a standing order: every round asks what would make the app smarter, more elegant, faster, and more loved.
- `live` "in tournament mode the ranking feels pretty high latency/laggy between click and next images" — 09-10 · two causes found in the code and removed: a 120 ms beat held before the swap (a Q9 audit suggestion; the frequency rule says a round has no motion), and the 4,096 px loupe rendition being decoded for every card on a 1.5× display past a pair (now the 1,024 px grid tile except for a pair). Awaiting his feel on the next sitting.
- `open` "Eye/face sharpness are key too, lets study aftershoot and lrc asssited culling and learn from them" — 09-10 · a study of Aftershoot, Lightroom Classic Assisted Culling, Narrative Select, FilterPixel, Photo Mechanic and Capture One: the signals they surface (eyes, focus on the subject, expression, duplicates), how they show them, their burst policies, and the trust failures their users report; the taste study already puts eyes-open and face-vs-frame sharpness first among the facets (E2).
- `open` "subject sharpness detection and more, lets really studdy how we can make it as smart as possible" — 09-10 · a technical study of subject and eye sharpness (the measures that survive real photographs, subject-vs-frame, motion vs defocus, the eye crops from the 106 landmarks, burst-best calibrated from his own picks, and the compute plan for 150k photographs), landed as `docs/taste-and-culling-research.md` with the culling-tools and taste studies; **the first slice shipped 09-10 (E2-1): a `sharpness` cache kind — the frame map, the subject ratio, a noise-aware measure per face crop — read by the inspector as numbers.** Eyes (E2-2) and the one-stage head (E1) are next.
- `live` "we also need a better UI for merging people in the people section, like a little inline card or like google photos where it asks if these two are the same people" — 09-10 · **shipped 09-10**: the wall proposes pairs of groups whose faces sit close in the face space as one inline card (two faces, Same person? Yes / No, Y/N keys); Yes heals the split as naming already does (a name is asked for when neither side has one), No is a decision that keeps the pair apart and never asks again.
- `live` "we want lots of photo anaylsis metrics that can inform the taste prediction and serving the assisted culling system and ranking, im not sure how to best blend that but I think you understand my intention enough to help" — 09-10 · the shape, settled: every metric is a stored fact (per frame, per face), never blended by hand; the one blend is the fit — a Plackett–Luce head over the embedding and the facts, its weights learned from his own rounds and picks — which serves ranking, the assisted cull (relative within a stack, with the why from the largest term) and Learn's draws alike. `docs/taste-and-culling-research.md` is the plan; E2-1 shipped, E2-2 and E1 next.
- `live` "im afk but take care of the next steps, continue working" — 09-10 · the open ledger in order, unattended: E2-2 (eyes), K9 (the > command line), BC5–BC9, BC11, HK3 and FR1 shipped; LP6 rejected (equal area is the law); BC13, FR2, FR3, FR7 deferred with their reasons in the ledger; the refutation of the day's people, sharpness and command-line work (X48–X72) shipped, with the people card's undo made a replay of the log. E1 waits for a copy of his catalog taken while the app is closed. Every batch refuted by another model before it is pushed; his app needs a relaunch to see any of it.
- `open` "what areas need the most attention next?" — 09-11 · answered in rank: the learned head (E1, E3–E5; needs a catalog copy with the app closed), his own sitting on what shipped, import staging (FR2/FR3/FR7), assisted-culling marks after E5, sharpness at scale, the concentration of change in boot.py/app.js, Develop parked.
- `partial` "those all sound great, do them all and also work on the people wall merging people seems slow/unresponsive, renaming doesnt update right away and theres no chip if the name matches another, that prompts merge, its deff better, but lets keep polishing the people area, aswell as all the collapse/expand buttons, they are better but their placement is a bit jank and feels unpolished. also the grid timelime needs lines for dates that have photos, and more love put into the little details of it." — 09-11 · all seven areas taken in order; plus: the People wall answers a merge and a rename at once (the groups rewrite on the rank lane after the verb returns, so the wall showed the old groups until it landed); a name that matches someone already introduced says so as you type and that saving joins them; the three fold buttons' placement redone from pixels; the grid's rail gets a mark for every day that has photographs, and its details. · **checked in the tree 09-12:** the wall's Same person? card and the name-matches chip are in `web/static/v2/shell/people.js`; the rail draws a line for every day that has photographs, longer for a fuller day (`web/static/v2/shell/timeline.js`); the fold buttons' placement is a look, not a grep, and waits for his eyes.
- `open` "I also keep getting this command window pop up, whats that about? CUserssmastAppDataLocalProgramsPythonPython312python.exe.txt" — 09-11 · a console window titled with python.exe's path: something launches python.exe with a console (the launcher, or a subprocess started without CREATE_NO_WINDOW); found and removed.
- `open` "for azimuth can we polish up the packaging so its all under Azimuth photo with the proper logo and everything in task manager and whatnot rather than it just saying python? want to make sure its all neat and professional in that regard aswell." — 09-11 · the app is one process named Azimuth Photo with its own icon in Task Manager, the taskbar and the window; no console; a shortcut that is the app.
- `live` "in the import dialog, lets work on polishing that aswell, with options to copy, move, and some clever way to no reimported old culled photos unless explicted desired, without keeping detailed logs of every image ever deleted/culled." — 09-11 · Copy and Move (the source removed only after the copy is verified) as the dialog's own choice; a photograph culled before stays out by default and the import says how many it left out with one click to bring them anyway — known from the decision log that already holds the cull by identity, so no second record of anything is kept. · **checked in the tree 09-12:** Copy and Move are the dialog's own choice (`web/static/v2/shell/intake.js`), and a photograph culled before stays out unless asked (`include_culled` through `bring`).
- `open` "how can we make the whole app more polished, professional, and intuitive?" — 09-12 · answered with a ranked diagnosis (one program identity and startup, first-run and empty states, one grammar for views and verbs, feedback on every act, the details a pro notices); the first rows of it shipped the same night (one instance, a log, the launcher).
- `open` "how can we make the code more optomized, closer to the metal and really make the program as fast as possible? I think theres lots of headroom in software, most software is wayyyyy slower than the hardware could really enable, all of this is stuff to keep in mind and address continuously. The app needs to be the very best in all aspecs." — 09-12 · a standing order: every round carries a performance lens against the hardware's own limits (disk bandwidth, decode throughput, the GPU, one bridge call per act, one paint per frame), measured before and after, never a timing widened to hide a wait.
- `open` "ok do e3-e5 and fr3 fr7 then bc13, keep going. im going to use the app a bit and import new photos. but I think yiu have a pretty good idea by now of the directions I want to continue development" — 09-12 · in that order, on the catalog copy while his app runs; then the ledger and the lens, as before.
- `open` "Can we build a feature that can automatically detect and merge shots that are shot as a panorama? that would be a really neat feature if we could do it really well. but it would have to be done extreamly well." — 09-12 · a study first (what makes a stitch excellent: detection of a swept sequence from the cadence and the overlap, alignment, projection, seam and exposure blending, the raw pipeline), then the shape: a panorama is a stack the library recognises, its stitched frame a rendition, and the merge a verb that is only ever offered when it will be excellent.
- `open` "how could we contribute/learn to/from open source photo tools to improve this app? how can we make photo software better for everyone and grow this project? (darktable just doenst excite me, it feels ugly" — 09-12 · answered: learn the pixel science from darktable/RawTherapee/libraw and the stitching from Hugin, never their surfaces; contribute upstream where Azimuth already leans (libraw/rawpy, exiv2's tags, lensfun profiles, insightface); grow through the ledger as a public roadmap, universal primitives at the boundary (sidecars, DNG, XMP, GPX) and a contribute page that reads like the Field Log.
- `open` "I want the very best professional photography app to be open source, and I want this to be that app." — 09-12 · the standing vision, restated: every round is measured against the best professional tool for that surface, not against other open-source tools.
- `open` "continue making the app better in all ways. lets get ahead of it all. and lets constantly consider how we can improve tha app both inside and out the codebase, professionalism, quality, preformance, love all the thousands of little details that show that we love the user, and really just pour lots of love into every single detail, and the big picture. how can we actually make this the best app, not in just 10 fixes, but across tens of thousands of big and small improvements, refactors, tiny details, big features, and small tweaks alike?" — 09-12 · the standing method, made explicit: the ledger never runs dry because every round widens the lens (a persona, a surface, a catalog size, a state of the machine, a time of day, a failure); every batch is refuted by another model and measured against the hardware; the details are audited surface by surface against one canon (copy, focus, cursor, hover, empty/loading/error states, keys, units, plurals, numbers, motion); his own sittings and the log are the input; the count of closed rows is the only progress that counts.
- `live` "the libary wasnt loading or me when I tired to boot it from the desktop, all the improvements are great but zero bugs and zero lag are #1 goals" — 09-12 · the cause was mine, an hour old: a kit helper named `count` shadowed by a local string in the main render, so the window drew nothing; fixed in the bundle (`c391dfeb`), and the linter now refuses any shadowed name (`no-shadow`), which would have caught it before the build. Zero bugs and zero lag are the first two goals of every round from here.
- `open` "keep working all night thanks" — 09-12 · yes.
- `open` "how well do you understand my development intentions and vision such that you can confidently procede autonomously and really impress me?" — 09-12 · answered in the report: the vision in my own words, where I am sure, where I am not, and how I decide alone.
- `open` "sounds like you get the gist, please continue working until I say otherwise." — 09-12 · yes: rounds until told otherwise, each refuted by another model before its push, each proven on an isolated home.
- `open` "keep working un attended till morning" — 09-12 02:12 · yes; the night ran to b2e654d5 (34 commits: PN4, BC13 second round, X129–X192, IA1–IA19, LR1–LR23).
- `open` "keep going" — 09-12 06:55 · yes: the rounds continue (audit, refute, prove, push); PN2 (the full-size merge as a stack cover) is designed in daylight with his eyes on the first result.

### 1.20 Working from the phone (09-12)

- `live` "This is a whole nightmare I just want it to be all fixed and everything set up so I can work on it from my phone without my laptop on but GitHub integrations are a nightmare on Claude" — 09-12 · said in the chat that moved the repository to `seankd3/Azimuth-Photo` after the old account was flagged; the whole tree was pushed there the same day, and the cloud workflow below is the rest of the ask.
- `live` "setup a clean and elegant workflow for working on the app in the cloud." — 09-12 · the shape: a task from the phone runs in a Linux container, builds the development archive from public-domain photographs, runs the one check, and comes back as a branch and a draft pull request to merge from the phone; CI runs the check on Linux and Windows both. Written down once, in `docs/development.md` (Cloud sessions) and the cloud-session exception in `AGENTS.md`.
- `open` "I want to continue development from my phone using Claude code and astra" — 09-12 · Claude Code on the web is the tool the workflow is built for; "astra" is not recognised and is asked about.
- `open` "Astra is gpt-6" — 09-12 · the answer: the second agent is Codex. The workflow is agent-neutral by construction -- `AGENTS.md` is the contract both read, the cloud-session exception names both branch prefixes, and both run the one check -- so nothing is built twice.
- `open` "keep working on what you can" — 09-12 · the standing order for the cloud: the open rows a session can prove without a window or his laptop, one task per pull request, merged as they go green. First: `scripts/cloud-setup` and its start hook, so every session begins ready.
- `open` "does your cloud machine have all the hardware to properly run the app?" — 09-12 · measured 09-12: 4 vCPU, 15 GB, no GPU, no display. The engine runs whole (sweeps, tiles, RAW decode, EXIF, search, the CPU face pass); the window cannot open, so the UI is proven in the harness; the embedding space (SigLIP through torch, 4.5 GB) is not installed and would be CPU-slow; nothing of his archive is here, so the fixture is the library.
- `open` "and to test its features and whatnot" — 09-12 · what a cloud session can test: the whole engine against the fixture, the UI's shape and behaviour in headless Chromium (`scripts/harness_proof.mjs`), the check on Linux and, through CI, on Windows. What it cannot: the native window's pixels, the Windows packaging, the GPU, and anything only 150,000 real photographs show.
- `open` "how can we continue to improve elegance and professionalism across this project?" — 09-12 · answered in the session's report; the standing method already in `AGENTS.md` (one of each thing, gates over prose, a refuter per fix, every batch refuted before push) applied to the cloud: every session proves what it says with a number or a capture.
- `open` "use many sequential very large meta workflows focusing on polish refinement, performance, UI QOL and general app improvement, focus on quality over buggy new features. goal is product maturity and professionalism. the app exists now we make it good." — 09-12 · **the standing order for the cloud rounds, and the opt-in to multi-agent workflows.** The repository's own rounds (`.claude/workflows/`: elegance, latency, seams) run in sequence from a cloud session, each fanning out readers over one lens, refuting the weak findings, and fixing what survives; every batch refuted by a second model before merge, one pull request per row cluster, merged as they go green; no new feature starts while a finding is open.
- `live` "the cloud test catalog should be a large catalog of real images, and we can reuse it for testing and development, and verification." — 09-12 · the manifest `scripts/test_library.tsv` carries a large tier: 6,105 public-domain photographs from five Library of Congress collections (FSA/OWI colour and black-and-white, Detroit Publishing, Bain, Harris & Ewing), 1860–1944, pinned by SHA-256, as Edits, Snapshots and 44 film rolls under Raws/Film Scans, dated in fives so the cadence law has bursts. Proven 09-12 in the container: `--large` builds 6,137 files (1.3 GB) in 332 s; a Library sweeps them in 0.5 s, reads 6,136 dates in 22 s, makes 12,272 tiles in 304 s; the cadence law proposes a set on 335 of 400 sampled frames; `scripts/bench.py` on the catalog has no row over budget. The suite keeps its synthetic frames so CI never downloads.
- `open` "I will be AFK for 16 hours, queue up enough work to cover 4x that amount of time" — 09-12 · the rounds cycle without a sitting: polish, latency, elegance, seams, then a new lens each time (personas, the codebase's own ergonomics, the documents' truth); each round's survivors land as one refuted pull request; an hourly heartbeat launches the next round if none is running.
- `open` "focus on elegance, rather than just bloating the codebase" — 09-12 · every round's fix agents are told the same: smallness as a consequence, delete in the same commit, no new machinery; a round is judged by what it removed and repaired, never by what it added.
- `open` "want to improve the app codebase as well such that it's more efficient to work in. as well as better experience for the user" — 09-12 · two lenses for the elegance round: the next agent's speed (where change concentrates -- `web/boot.py` and `web/static/v2/shell/app.js` -- the check's own time, names that have to be looked up) and the photographer's experience (the canon in `docs/ui-architecture.md`).
- `open` "going to be AFK so please work autonomously and automatically until Sunday morning at 10am. use many sequential very large meta workflows focusing on polish refinement, performance, UI QOL and general app improvement, focus on quality over buggy new features. goal is product maturity and professionalism. the app exists now we make it good. elegance, performant, world class professipnal UI/UX" — 09-12 · the standing order restated as the goal for the night and the day after; the loop runs until Sunday morning and then holds for his eyes.
- `live` "the rank game still lags esp on 2 image mode. lets make it instant. no lag no delay, no latency, and ditch the orange outline that shows on the next image on the previous pick" — 09-12 · two defects in Rank, fixed 09-12 (PR 16): the buffer holds two sets in hand so a pair pick never waits on the bridge, a card paints what is decoded now and swaps the loupe in when its decode lands, and no cursor survives a pick. Proven in the harness: the next pair paints in the same frame (0 ms), cursor 0 after the pick, focus on the stage; in a set the next arrow brings the cursor back. `docs/FINDINGS.md` RK1–RK3.
- `open` "also how does th enumber of images in the rank mode change how the descisons are recorded and processed? picking 1 image out of 20 via small thumbnails is a differrent choice than 1v1 large images, how can we be smarter about this and more effficent? as my picks may be very noisey and differnt day by day" — 09-12 · answered in the session's report from the code: a round is recorded as one winner over the whole set, and the Plackett-Luce fit already weighs a 1-of-20 as a weaker word about each loser than a 1-of-2; what it does not know is how big the cards were or which sitting the round came from.
- `live` "can we delegate to cheaper models where applicable?" — 09-12 · yes: the rounds' scouts run on Sonnet, the per-finding refuters and fixers on Sonnet at high effort (then Opus, below), and the strong model reads one round's diff once before it merges. `.claude/workflows/*.js`.
- `live` "i only have 22% fable ussage remaining till wednesday" — 09-12 · the budget rule for the rest of the week: the strong model does not scout, refute or fix per finding; it launches rounds, reads each round's result once, merges, and writes to him rarely and briefly.
- `live` "how effective is the workflow per token vs just doing everything yourself? is the work quality or slop?" — 09-12 · answered from the rounds so far: polish round one landed seven of eight findings and its fixes held; the latency round landed none of six (the lens, not the model: a stub harness has nothing left to measure), and was retired. The refuters earn their tokens: one Sonnet refutation of PR 16 found two real defects.
- `live` "So sonnet wasnt good? can we use opus sub agents then?" — 09-12 · Sonnet was good (above); the tier matters for judgment, not breadth. Routing from 09-12: Sonnet scouts, Opus refutes and fixes at high effort, the strong model reads each round once. `.claude/workflows/*.js`.
- `live` "also on rank every click causes all the images to flash black for a second, jarring and annoying. ick please fix." — 09-12 · done 09-12: Rank rebuilt every card on every pick, so the seven that stayed were new images painting black until their bitmaps landed and ran the arrival again; a card stays for a photograph that stays, and only the changed slots are touched. Measured in the harness: 9 rebuilt and animating before, 2 after. `docs/FINDINGS.md` RK4.
- `open` "what can we work on next? how can we continue to improve the app? notion/factorio levels of polish and QoL UX" — 09-12 · the ledger of findings is nearly clean (four rows deferred), so the next work needs a new lens rather than the same eight surfaces again: a journeys round that walks a person's afternoons through the app (first run, a cull, a Rank sitting, finding one photograph, organising, the second sitting, learning the keys, when things go wrong) and holds each moment to his bar, one key or one click, instant, remembered, undoable, taught, never a dead end. `.claude/workflows/journeys.js`; runs after the large fixture merges, then polish and elegance again.
- `open` "that sounds good, what else can we do? all the sutble UI/UX details that breathe love and life into the app, how can we make those types of improvements?" — 09-12 · the honest answer: an agent cannot feel the app, so the details are made findable three ways. A craft round scouts by family of detail rather than by surface (every act answered at once and in place; settling instead of jumping; one rhythm of space and type; the voice; hints that retire once learned; the rare moments that deserve a quiet flourish; the pointer and the cursor; and restraint, the scout that finds what is already too much), each refuted with the question "would a hundred-a-day act pay for this". Every rule a round finds missing is written into `docs/ui-architecture.md` so the next round checks it. And his own noticing: a line dictated from the phone for each wince at a sitting becomes a row here. `.claude/workflows/craft.js`; runs after journeys.
- `live` "one small example, Landscape photos vs landscape oreintation, how can we make the toast clearer? more toast? I really like toasts, esp in the people area and search, and other text feilds. little things like that, but everwhere" — 09-12 · done 09-12 as the craft round's seed: the shape words are "Wide (landscape)", "Tall (portrait)", "Square" in one vocabulary shared by the chip and the search drop, and a filter added from a text field is answered in a toast in the chip's own words ("Filter added — Wide (landscape)."), the mirror of the removal toast that already existed. The standing rule for the craft round: every act from a text field is answered in words, everywhere. `docs/FINDINGS.md` CR1.
- `live` "also the x on the filter toast isnt fully on the toast, thats the type of little details that we need to fix." — 09-12 · done 09-12: the chip's × inherited six pixels of button padding and its icon spilled onto the pill's curved end; measured in the harness before (1 px from the border, outside its button) and after (inside, 9 px in). `docs/FINDINGS.md` CR2. The craft round's rhythm scout measures every control's box against its content the same way.
- `live` "any of the slightest friction, ugly ux, lag, etc etc, how can we sniff those things out better?" — 09-12 · two instruments, since an agent cannot feel the app. A friction meter in the window records what every key and click costs — the wait to the first change and to the paint, the nodes rebuilt, the animations started, long tasks, layout shifts, errors — and keeps the last forty, with Ctrl+Shift+W to note a wince into the log the moment something feels wrong; and the harness now draws real photographs, so a proof pays for a decode instead of a black tile. `docs/FINDINGS.md` FR1.
- `live` "diverse rank mode still shows images from the same sequence on the same grid, all very similar, it should choose images that are all very differnt, hence DIVERSE" — 09-12 · done 09-12: before anything is judged every photograph is equally worn and the least-worn tier was cut in filing order, so it was the first shoot in the pool; the tie is broken at random and the tier spreads across days whenever it holds two. Refuted by a test: one day before, four or more after. `docs/FINDINGS.md` RK5.
- `open` "next I want to design a dedicated workspace/page for album creation, curration and culling, where the top shots from the sequences are currated, so that every shot is differnt, distinct, and strong. I want the program to help guide this process and really help automate the process in a very context aware way. how can we do this? lets think it out and come up with a really great and elegant design. learning from other programs to create the ultimate portfolio and album curation tool." — 09-12 · designed 09-13 in `docs/SELECT.md`: a story is an album with an order and chapters; scenes are the cadence law's sets widened by likeness, each shown once; the pass walks scenes one key at a time with a proposed lead; a distinctness guard and a budget keep count; the fit learns the person's leads inside the story. Six steps, each its own pull request; step one needs nothing the engine lacks.
- `open` "for instance I shot thousands of photos durring a long trip with my girlfriend we traveled all over the west coast, east coast, NYC, NH, NJ, and at the end we got engaged on mt washington, and I want tools in the software to help currate and put the story together. how can we build a bicycler for the photographer? wanting to help photographers tell the stories that lie in their archives." — 09-12 · the worked example of `docs/SELECT.md`: chapters from sessions and places (the coasts, the city, the mountain), the beat proposed from density and people and confirmed by him, the cover the highest-rated wide frame with both of them in it. The machine proposes and keeps count; the person decides and orders.
- `live` "for instance I care more about the best shot in a seqence of 20 shots than I do having 20 shots from the same sequence even if I on average rank that seqence above another one" — 09-12 · the one rule of `docs/SELECT.md`: a scene is worth its best frame, and a story is a sequence of scenes, each shown once. Two kinds of round follow, within a scene and across scenes, both in the decision log Rank already writes.
- `open` "also this can help us cull, for instance, something like Pick the top 3 shots and cull the rest? or something like that if theres a big delta between shots in a sequence/redundant shots with one subtly better." — 09-12 · in `docs/SELECT.md`, "The same rule culls": keep N of a scene and reject the rest, offered only where the fit is sure (the lead clear of the rest by more than the fit's own uncertainty) or where likeness plus rating makes one frame redundant; the offer wears the tilde, rejects are Rank's rejects (Trash, undoable), and a whole story's sure scenes can be culled in one act with one Undo.
- `live` "lets keep working on the story idea lets get creative and clever with it" — 09-13 · a first list of nine additions was written into `docs/SELECT.md` and then cut on his next word (below): three were the one rule again and were folded into the surfaces they came from (the spine drawn to shooting density, the second look as the pass's undo for omission, the post as the book's budget applied again); the rest are recorded as rejected bells.
- `live` "those are interesting gimmicks but i want elegance not occasionally useful bells" — 09-13 · done 09-13: the list is gone from `docs/SELECT.md`; "Nothing extra" keeps the three that were the rule, and the rejections name the rest with the reason each earned. The standing rule for Stories: a thing joins only as the one rule applied again, never as a feature beside it.
- `open` "what about creating albums around subjects? or like year in review? all time portfolio? wedding deliveries? conside all these usecases and lets design elegant versitile and powerful" — 09-13 · in `docs/SELECT.md`, "One pass, many shapes": every one is the same pass over a different scope (a chip sentence) with a different order and budget rule, and none needs a surface the story lacks. Story, Delivery (coverage, N per scene by length, cull the rest, export in order), Review (a floor per month), Portfolio (strength, the guard as the instrument, tournament rounds among leads), Subject (a smart album that has been walked; new matches arrive as unwalked scenes). Three budget rules, one setting per story.
- `live` "study how the best photographers currate their books albums etc and how we can make that process easier for everyone" — 09-13 · done 09-13: a sourced research pass (twenty sources: Frank's *Americans*, Goldin's *Ballad* edit, Steidl, Mack, Shore, Hido, delivery and portfolio norms, today's tools) became "What the practice says" in `docs/SELECT.md`, each rule with what it decides in the design: editing in rounds (cut again, the wall as the next round's pool), the whole set seen at once (the wall), a second pair of eyes (the contact sheet), sequence as composition kept in human hands, default budgets from practice, fewer beats more, speed at the pass and patience at the wall, machine picks visible and reversible.
- `live` "and more widely applicable design dont make it too specific to me consider all photographers" — 09-13 · done 09-13: `docs/SELECT.md` rewritten for any photographer; the shapes (a trip or project, a delivery, a year, a body of work, a subject) carry the design and his trip is one worked example inside them.
- `live` "so with all that research lets take another stab at the culling/album/stories/portfolio idea/workspace design" — 09-13 · done 09-13: the second draft is `docs/SELECT.md` (the first, Stories, is replaced). The noun is the photographers' own: a select. Four nouns instead of six (select, scene, lead, round). Two rooms instead of three (the pass, fast; the wall, slow, with the chapters as its margin). Rounds are the model, not a feature: round two edits round one's wall, every round kept. The rest holds: the one rule, the guard, the budget rules, the shapes, the cull, the tilde.
- `live` "lets think about it from first principles, how can we make this truly the best, most elegant, most powerful, and most inteligent yet simple to use?" — 09-13 · done 09-13: `docs/SELECT.md` opens with seven principles (the machine removes what is the same, the person chooses among what differs; judgment is comparative; ask only what the machine cannot answer; the machine remembers, counts and notices, the person composes; the log is the only state; never wait, never rush; three settings ever) and the pass changes to match: it asks in order of doubt, settles sure scenes under the tilde, and ends when the doubt does. The powerful part and the simple part are the same: attention spent only where it is worth something.
- `live` "Empty trash button i s very laggy and takes forever for the dialog box to pop up" — 09-13 · done 09-13: the dialog waited on a dry run that read every trashed file byte for byte; it opens at once now, the preflight (drives attached, files present) runs while he reads and arms the button, and the deletion still verifies every byte. Harness: 1,209 ms to open before, 5 ms after. `docs/FINDINGS.md` CR3.
- `live` "(1) In AGENTS.md add a cloud-session exception: agents may push a task/* branch and open a PR, never commit to main directly." — 09-12 · the first half of the task he submitted from the chat (its words were drafted there and sent as his). **Merged 09-12 (#1)**, with the check green on Linux and CI on both platforms.
- `live` "(2) Replace scripts/make_test_library.py with a portable fixture builder: download a small fixed set of public-domain photographs from Library of Congress or Internet Archive (hardcoded URLs, SHA-256 per file, a PROVENANCE.md listing source and license), write them into the Edits / Raws/Digital / Snapshots date-folder layout under a path from AZIMUTH_TEST_LIBRARY or --dest instead of C:\, and include controlled cases: known capture dates, a duplicate pair, a nested folder. Must run on Linux and pass scripts/azimuth-check." — 09-12 · the second half of the same submitted task; its own pull request, on top of the first. · **merged 09-12 (#2): thirty-one FSA/OWI Kodachromes from the Library of Congress, pinned by SHA-256, EXIF dates the fixture assigns, DNGs under Raws/Digital from the same frames, the duplicate pair, the nested `Fair` folder, one undated frame, `PROVENANCE.md`; builds in 2 s on Linux and the check is green**

## 2. Derived queue

> **Historical pre-V2 queue.** Statuses and evidence below are preserved because
> they explain product decisions and prior incidents. They do not set the V2
> execution order. Current rewrite order lives in `docs/product-roadmap.md`.

Consolidated 07-31 after the design pass. Ordered by leverage. `fleet-1` rows are
in tonight's overnight wave; each lands on its own branch and is merged only
after an adversarial review plus a human diff read. `runtime` rows stay out —
they need a measurement, not a build.

| # | Item | From | Wave |
|---|---|---|---|
| 0 | **DONE 07-31 16:50 — taxonomy live end to end.** Archive, hub catalog and laptop all read `Edits / Raws / Snapshots`. Root cause was `catalog_sources.path` still holding pre-rename names, so every repaired row failed source containment and the app answered `source_invalid` no matter how correct the filepath was. Fix sequence: disk `RAWS`->`Raws`; 97,426 rows repointed onto `Raws/Digital`; 89,026 stale `missing_at` flags cleared; source roots renamed with `Film Scans` folded in as a shelf inside Raws; laptop-local sources folded (1,045 adopted, 9,718 proven duplicates retired, **0 photos missing from the archive**) and excluded via `included=0` (the app retires by `included`, not `removed_at` — setting the latter alone let the scanner re-add the source). Verified by real image bytes served on hub and laptop, not by folder counts. **CORRECTION 07-31 20:30: this row was claimed too early.** The catalog was right when it was read, and wrong again after the next restart — see row 0b. Read it as "the paths were repaired", not "the taxonomy held". | 1.4 | **superseded by 0b** |
| 0b | **DONE 07-31 20:28 — the repairs finally hold, and here is why they did not before.** `web/data/schema.py` `normalize_legacy_image_state` runs on **every** startup (`db.py init_db`, schema-is-current branch, no version gate) and rewrites any `images.status` outside `kept/maybe/trashed` back to `kept`. Repair scripts across several sessions had parked stale rows under an invented `removed` status — 6,059 of them — so every hub restart silently resurrected the lot and each fix looked like it had never applied. Proven by experiment, not inference: service stopped, 29 rows retired, 0 verified remaining, service started, all 29 back. **The durable fix is to stop inventing statuses:** a row leaves the library by `missing_at` (the file is not at that path) or by an excluded source, both of which survive a restart. Applied by triggering the app's own source rescans, which mark unseen files missing through `mark_source_missing_files_on_conn` under the >5% circuit breaker — hub strays went 29 -> **0**, and hub `/api/folders` now returns exactly `Edits 20,466 / Raws 100,480 / Snapshots 26,552` with shelves `Raws/Digital 97,541` and `Raws/Film Scans 2,939`. Startup now logs what it rewrites (`01cc0da4`) and the rule is in AGENTS.md, so the trap cannot be walked into silently again. | 1.4 | **live** |
| 0d | **DONE 07-31 — the library follows the folders, and the laptop can search offline.** Four things landed on the approved server/client plan. **(1)** Camera-profile lookup tables no longer ship to satellites: measured 480 of 5,000 rows carrying one, two distinct values, one repeated 479 times, 88% of every catalog page — a 240x cut (`1161cf23`). **(2)** Reconcile stopped re-reading a disk that has not changed: a directory whose mtime is unchanged cannot have gained or lost a photo, so the catalog answers for it. On the real archive that is 1,714 directory stats in ~2s against ~11 minutes to stat every file; on the test archive a second pass reads **0 of 19** directories (`6bebe3b3`). **(3)** It now runs by itself, so a folder renamed outside the app is simply followed and a photo dropped into one of the three roots simply joins the library — no prompt either way. A renamed root is **one operation**: the source row moves and its photos' paths are re-prefixed in the same transaction, and a candidate only wins if this source's own photos are found underneath it, so a genuinely unplugged drive is left alone. Acceptance with nothing clicked: renamed a root and dropped in three files -> 35 live, **0 missing** (`17f376cb`). **(4)** Embeddings now reach the satellite — the laptop held 154,941 photos and **zero** vectors, so semantic search away from home had nothing to search; proven live against the real hub, model key matched, 0 skipped (`a0bb8d72`). Also corrected: the laptop's thumb budget was pinned at 8GB by a stored setting against a 3,706GB drive — now auto (33GB). | 1.4 | **live** |
| 0c | **DONE 07-31 19:50 — the mirror follows a hub rename instead of stopping dead.** `images(filepath)` carries a unique index, and the mirror wrote the hub's path onto existing rows unguarded: the first renamed photo whose new path another row still held raised `IntegrityError` inside the page loop, the cursor never advanced, and the satellite stopped applying **anything** from that point on. That is why root renames never reached the laptop. Now the hub's path applies per row with the holder keeping a contested path, one bad row costs one row, and the export carries `missing_at` so a retirement can actually reach a satellite. Merged `25dd486e`, 56 tests re-run independently. Also found and fixed the reason none of the day's taxonomy work was visible: the hub's serving checkout was **56 commits behind main** — the whole taxonomy wave was written, merged and never deployed. | 1.4 | **live** |
| 1 | `is_satellite_mode()` treats `standalone` as satellite — installed desktop app silently runs with AI/faces/captions/pregen off | 1.3 | **merged 07-31 f6573cd7** |
| 2 | Three-root readiness: `Edits/ Raws/ Snapshots/` constants, tolerate legacy names during Sean's manual rename, never create roots, kill the stale "canonical" comment in `taxonomy.py:45` Census 07-31: of 2,411 stuck rows, 1,821 = Takeout Trash deliberately quarantined at `.pending-delete/takeout-trash/` (retire rows, never rewrite into library); 573 relocated into `Exported Edits/` (unique basename+size match; check existing-row duplicates first); 17 collision pairs need flag surgery; **0 truly lost** (sample n=80, zero unresolved). | 1.4 | **merged 07-31 14b5ce4b; catalog APPLY run still pending (dry-run first, server quiet, before any trash-empty); re-review follow-ups: the tool cannot see the Takeout quarantine dir (`.pending-delete/takeout-trash/`) so the 1,821 rows need their own retirement pass; CLI report caps at 100 samples — expose full listing before --apply; Android BackupWorker still sends `Personal Photos` (tolerated, retire later)** |
| 3 | Offline-storage circuit breaker: a scan that would mark >5% of a root missing halts as "storage unavailable" — never mass-trash (the guard Immich refused) | 1.4 | **merged 07-31 444b5aeb** |
| 4 | Multi-vendor RAW phase 1: scanner+catalog+embedded-preview thumbs for ARW/NEF/ORF/RAF/RW2; import stops silently filing what the catalog rejects | 1.12 | **merged 07-31 4af4d7f0** |
| 5 | Stars as Elo's face: real computed `rating`, filter/sort on desktop, retire the dead mobile write | 1.12 | **merged 07-31 8a72bc77** |
| 6 | Refine: diverse mode spread fix + aspect-ratio pairing in Dual (same files, one lane) | 1.6 | **merged 07-31** |
| 7 | Responsive: ≤880px dead right-panel button; `.develop-layout` narrow breakpoint | 1.9 | **merged 07-31** |
| 8 | Develop right rail + filmstrip whole-panel collapse | 1.9 | **merged 07-31** |
| 9 | Video custody affordance replacing the grey 320×180 rectangle | 1.14 | **merged 07-31** |
| 10 | Trash hardening: empty/restore/count provably correct on large selections — the gate before Reject ships | 1.14 | **lane built+reviewed; NOT merged — one satellite test regressed vs main, under investigation** |
| 11 | Perf gates wired into CI: the three existing `--check` harnesses actually run; history logged into commits | 1.2 | **merged 07-31** |
| 12 | LrC migration UI: wire the orphaned `/api/develop/lrcat` importer into Connect Lightroom | 1.4 | **merged 07-31** |
| 13 | Suggestions: distinct kind badges, grouped review rail | 1.9 | **merged 07-31** |
| 14 | Import one-action collapse over existing detection; minimal UI, morning review | 1.4 | **merged 07-31, morning review pending** |
| 15 | Vocabulary pass (Refine/Filters/Pick/Reject/Stars/Places/Galleries/Deliveries) incl. `publish`→`publishing` split kill — repo-wide, runs alone AFTER fleet-1 merges to avoid conflicts | 1.15 | next |
| 16 | Develop open p50 ≤400ms (today 1,377) | 1.13 | next |
| 17 | Automatic laptop reclamation per the age-only cache policy | 1.15 | next |
| 18 | Two storage modes at onboarding; first-run fork (library vs card) | 1.3/1.14 | next |
| 19 | One-box search across people/places/semantics/facets | 1.14 | next |
| 20 | Smart collections: boolean operators, ranges, multi-value facets | 1.9 | next |
| 21 | Overlay stragglers: system drawer + Deliver still scrim the app | 1.9 | next |
| 22 | Hub pairing without URL/port thinking | 1.3 | next |
| 27 | Satellite cold-start pull: hub takes minutes to build a full catalog export and the rc.1 puller aborts at its short timeout, so the laptop mirror never completes cursor-0; chunk the export server-side and ship a desktop build with the 600s timeout already on main | 1.3 | next |
| 28 | Deploy updated hub code BEFORE Sean renames RAWS/Exported Edits/Film Scans, then run the relocation tool immediately after -- renaming first repeats the dead-rows event at 4x size | 1.4 | **sequenced, Sean's call** |
| 23 | Retire `C:\Pictures` — everything hash-proven except the newer 2025 LrC catalog (16.9 GB) + 2 loose files; archive those first, deletion stays Sean's | 1.4 | Sean+agent |
| 24 | Reject verb ships once #10 is proven | 1.14 | after 10 |
| 25 | Gallery/website shared styling | 1.8 | next |
| 26 | Integrity Checks as a first-class scheduled report (untracked files, missing refs, checksum mismatches) | Immich study | next |

## 3. Keeping this file honest

- **Every Sean statement about this product gets logged here, verbatim, same
  session it was said.** Not paraphrased into a task title — the exact words, with
  the date. Paraphrase is where intent leaks out.
- **A multi-item message gets one row per item.** The 07-19 message carried four
  asks; the first shipped and the fourth was never recorded. One row each, or the
  tail is lost.
- **Never delete a row.** Mark it `live` when it ships, or `declined` with Sean's
  own words declining it. A removed row reads as done.
- **`unverified` is a debt, not a status quo.** It means the claim has never been
  checked. Do not report an `unverified` item as working.
- **Ask when a new statement contradicts an old one.** Before building, check §1
  for a row that the new ask fights with. If one exists, stop and ask Sean which
  wins — do not silently pick the newer, the older, or a blend. Log the answer as
  a new dated row and mark the loser `superseded` with the date it lost. The same
  applies when a statement is too ambiguous to build from: ask, then log the
  answer verbatim. An unresolved contradiction goes in §4 and stays there until
  Sean settles it. Guessing between two things he said is how a want gets built
  wrong and then rebuilt.
- This file is exempt from the "no plans or dated status files" rule in
  [AGENTS.md](AGENTS.md) documentation hygiene. It is an owner document, not
  coordination scaffolding. The retired 1,580-line lane-era plan remains readable
  at `git show 937dee04:MASTER_PLAN.md`.

## 4. Open contradictions

Statements in section 1 that fight with each other. Do not build the affected
area until Sean settles the row. Resolving one means: log his answer as a new
dated section 1 row, mark the losing row `superseded`, and delete the entry here.

**None open.** Four were found when this file was assembled and all four were put
to Sean on 07-31:

- **C1 import** — one smart import wins; the two 07-29 buttons are the mechanism,
  not the destination. Detection must carry it.
- **C2 phone scope** — full Google Photos replacement. Section 1.7 is a real
  workstream, not a someday list.
- **C3 private vs product** — both. Public product, one actual user, prod-first
  development survives.
- **C4 storage** — resolved into new architecture: two modes, local-only and
  desktop+server, with canonical as a role rather than a fixed drive.
