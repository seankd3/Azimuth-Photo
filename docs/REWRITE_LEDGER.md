# V2 rewrite ledger

The exhaustive answer to two questions:

1. What has actually been rebuilt from first principles?
2. What inherited code is still capable of reaching the finished product?

This is a release gate, not a percentage-complete report. Every code file
starts as **Legacy**. It leaves that state only through an exact entry in the
file register below. A broad claim about a directory or feature never promotes
the files inside it. Tests, build configuration, and operational scripts count:
they can preserve obsolete architecture or manufacture false confidence just as
easily as product code.

## Statuses

- **Legacy** — inherited code, including code that looks clean or currently
  works. It has not passed the V2 review.
- **Designing** — its purpose and smallest honest shape are being reconsidered.
  Nothing is credited as rewritten yet.
- **Rebuilt** — the V2 implementation has landed and the V1 implementation it
  replaces has been deleted. It still needs real product proof.
- **Proven** — rebuilt, exercised through its actual product path, within its
  performance and safety constraints, with the evidence recorded here.
- **Removed** — deliberately absent from V2 because the underlying question or
  feature was removed. No replacement is owed.

There is no "adopted", "bridged", or "mostly migrated" state. New code calling
old machinery is still **Legacy** as a product area.

## What it takes to become Proven

A product area is Proven only when all of these are true:

1. **Purpose** — the user-visible job is stated without reference to V1.
2. **Shape** — the smallest primitives and ownership boundaries have been
   derived from `CORE.md`, not copied from the old implementation.
3. **Replacement** — the complete behavior lands on the V2 core; no internal
   compatibility layer, dual path, fallback, or shadow implementation remains.
4. **Deletion** — the implementation, tables, routes, workers, settings, tests,
   and documentation made obsolete by that replacement leave in the same
   change.
5. **Review** — every surviving line in the area is read deliberately for
   necessity, naming, coupling, failure behavior, and performance. Moving a
   file is not review.
6. **Proof** — the real product path works with a fresh catalog and real photo
   files. Only tests that protect an expensive invariant are retained.
7. **Record** — exact files, commit, proof, and remaining limitations are added
   to this ledger. Unrecorded means Legacy.

Compatibility is owed to user-owned photographs, decisions, XMP, and other
explicit external formats. It is not owed to V1 modules, tables, routes, jobs,
settings, or network protocols.

## Product-area inventory

| Area | Status | V2 outcome required | Current truth |
|---|---|---|---|
| Verification and fresh boot | Proven | One trustworthy cross-platform check; a fresh catalog opens the real desktop product | One command (`scripts/azimuth-check`) runs lint, six gates, the suite, the node specs and this ledger; a fresh home opens in the native window from source and from the frozen build. Cross-platform is not claimed: the product ships on Windows and CI runs there |
| Core model and schema | Proven | Photos, copies, drives, decisions, sets, scope, and cache are the whole durable model | Five tables in one schema, seven functions, sets and scopes with no tables of their own; `tables` reads 0. The transitional `vc_of` column stays until virtual copies are wanted |
| Computation | Proven | Pure decode, metadata, colour, hashing, and rendering functions with no product state | `photo/` reads bytes, `pixels/` is the colour mathematics, `render.py` is the one decode; `layers` counts any import that would teach them about the product |
| Work and cache | Proven | Owed work is a query; one bounded executor; one cache contract | One worker in lanes over one anti-join; every computed answer is a cache kind with a recipe that is a pure function of its inputs |
| Browse, disk, and tiles | Proven | One merged library view, truthful disk state, responsive thumbnails | One tree over every drive, tiles as files the window reads, justified rows, day chapters, cadence stacks, follow-the-folders sweeps; proven natively on fresh homes and on the owner's library |
| Organise | Proven | Sets and grouping fall out of the core decisions model | Albums (fixed and smart with exceptions), labels that learn, people, sessions and places are decisions and cache kinds over the same facts; one chip language under all of it |
| Keep and safety | Proven | Trash, backup truth, recovery, and integrity checks use the same copy facts | Pick/Reject/Trash/Restore/Undo are one reversible decision; Empty Trash is the one destructive boundary behind full-byte proof; backup and reclaim are `model.backup` (operator scripts today, not yet a verb in the window) |
| Import and synchronize | Proven | One complete intake/synchronize conversation with recoverable file operations | Cards and folders stage, one destination rule places verified copies, film lands by the roll, the sweep follows moves and rewrites and shows the missing; Import is a workspace in the grid's own room |
| Develop | Proven | Preserve proven pixel mathematics; rebuild the surrounding workflow on V2 | The maths is `pixels/`, byte-identical to V1; edits are decisions in the crs dialect; renditions are keyed cache rows; the crop and edit surfaces and the sidecar round trip are proven in-window. Parked by the owner 08-28 pending the Lightroom-shaped rebuild; the tone gap to Adobe (0.0445 L) stands as that phase's first task |
| Rank, search, and AI | Proven | Decisions drive rank; enrichment improves results but never gates the product | Rounds are whole decisions, strength is fitted, Learn is the simulation-chosen default, the space grows from tiles, search is one ranked fusion that never waits for a model, faces and photostats are cache kinds |
| Desktop UI | Proven | One fast native-feeling surface with one state and interaction vocabulary | `kit <- net <- store <- lens <- shell`, one document, one toast, one Esc ladder, one selection grammar; every surface above proven in the maximized native window |
| Desktop packaging | Proven | A dependable install launches the local product and owns its lifecycle | One frozen directory from one command; no server, port, parent process or child engine. An installer and signing remain future work |
| Android | Removed | Explicit product decision after desktop V2 is coherent | Deleted 09-08 on the owner's word: the client spoke to a hub that no longer exists and could not run. A phone returns as new work against the V2 vocabulary; `27f07752` holds the Kotlin |
| V1 remote hub/satellite system | Removed | Delete it; retain only user-data lessons that inform the new storage design | Gone from the tree 09-08; the lessons live in `CORE.md`'s appendix |

## Exact code-file register

This register began empty at the start of the audited rewrite. Code written
earlier does not receive credit retroactively because it resembles the target
architecture. We promote files only while reading and proving them in the
current V2 pass.

Run `python scripts/rewrite_status.py` for totals by area or
`python scripts/rewrite_status.py --all` for every remaining path. The final
release gate is `python scripts/rewrite_status.py --check`. The inventory is
derived from Git, so a newly added code file is Legacy automatically
until its exact path earns a register entry.

| File | Status | Product area | Commit | Proof and reason it earns the status |
|---|---|---|---|---|
| `scripts/rewrite_status.py` | Proven | Verification | `be7f7dbc`, `b7665224` | Its Git-derived inventory reports every executable/configuration path; `--check` stays red for Legacy/Rebuilt files, rejects missing active entries, and rejects a Removed path that still exists while accepting one physically deleted |
| `web/model/__init__.py` | Proven | Core | `da1ccd7e` | One constructor opens SQLite, applies the complete core schema, and closes on failure; fresh on-disk proof is in `test_core.py`. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/schema.sql` | Proven | Core | `284ba3f2`, `da1ccd7e`, `8c3f24a1`, `5769f3a3` | Owns all five core tables, decision provenance, and the three projected decision columns (status, stars, rotate) without requiring V1 schema first; transitional `vc_of` keeps it below Proven until Develop is rebuilt. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/drives.py` | Proven | Core | `284ba3f2`, `9a9ba1af` | Explicit attach is the only discovery path; reads no longer probe drive letters or disconnected mappings. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/intake.py` | Proven | Import and synchronize | `f86ab56e`, `9a9ba1af`, `96c222f1`, `e3de35a3`, `dbc6a4e9` | Import is `photos.put` per file plus one pure destination rule (Raws/Digital, Raws/Film Scans/<Roll>, Snapshots, Edits by the photograph's own date); `scan` stages a source, `guess_kind` reads it, `bring` verifies every copy before anything else, takes the next free name on a collision and never overwrites, skips by identity what a drive already holds, clears a source only after its copy is verified and recorded, stops between files and resumes by running again; `cards` notices a DCIM volume cheaply; `rolls` names a lab folder's subfolders 1, 2, 3… or by the stock in a scan's description, and film lands under the lab folder's date. Refuters hold each promise; the tally carries the identities each import landed, which is what rolls Previous import. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/synchronize.py` | Removed | Import and synchronize | `2a30a712` | The V1 plan/apply synchronize -- an offer of missing files the owner confirmed -- is replaced by the sweep that follows the folders, recognises moves and rewrites, and shows missing photographs with a Forget verb |
| `web/importer.py` | Removed | Import and synchronize | `2a30a712` | The V1 intake registry is replaced by `model/intake.py`'s `bring`: `put` per file by one rule |
| `web/features/catalog/synchronize.py` | Removed | Import and synchronize | `2a30a712` | The route over the V1 planner left with it |
| `web/features/imports/__init__.py` | Removed | Import and synchronize | `2a30a712` | The V1 import feature boundary left with its staging, card, film, journal, service, taxonomy and routes |
| `web/features/imports/staging.py` | Removed | Import and synchronize | `2a30a712` | 755 lines of staged-import machinery are replaced by `intake.scan` and the overlay's staged view |
| `web/features/imports/taxonomy.py` | Removed | Import and synchronize | `2a30a712` | 649 lines of destination logic are replaced by one pure `intake.destination` |
| `web/features/imports/card.py` | Removed | Import and synchronize | `2a30a712` | Stream-copy, verify, never-overwrite and clear-card live in `intake.bring` over `photos.put` |
| `web/features/imports/film.py` | Removed | Import and synchronize | `2a30a712` | Film rolls return as a kind of the one rule (Raws/Film Scans/<Roll>) when film import is built; the V1 zip/rar path is not carried |
| `web/features/imports/move_journal.py` | Removed | Import and synchronize | `2a30a712` | A move is a recognised sweep outcome, not a journal |
| `web/features/imports/routes.py` | Removed | Import and synchronize | `2a30a712` | HTTP import routes are replaced by direct bridge verbs |
| `web/features/imports/service.py` | Removed | Import and synchronize | `2a30a712` | The import batch service is replaced by `bring`'s tally and Recently added |
| `web/data/repositories/imports.py` | Removed | Import and synchronize | `2a30a712` | Import batch SQL left with the batches |
| `web/static/js/desktop/import_stage.js` | Removed | Desktop UI | `2a30a712` | The 826-line staged-import canvas is replaced by the 200-line import overlay |
| `web/test_imports.py` | Removed | Import and synchronize proof | `2a30a712` | Exercised the deleted registry |
| `web/test_import_move.py` | Removed | Import and synchronize proof | `2a30a712` | Exercised the deleted journal |
| `web/test_import_staging.py` | Removed | Import and synchronize proof | `2a30a712` | Exercised the deleted staging |
| `web/test_import_taxonomy.py` | Removed | Import and synchronize proof | `2a30a712` | Exercised the deleted taxonomy; the rule's refuters live in `test_core.py` |
| `web/test_catalog_synchronize.py` | Removed | Import and synchronize proof | `2a30a712` | Exercised the deleted planner; the away-drive invariant lives in `test_core.py` |
| `web/model/decisions.py` | Proven | Core | `284ba3f2`, `155d450c`, `8c3f24a1`, `5769f3a3`, `f93a7e91` | Append-only decisions record provenance, apply one authority rule, preserve provenance when carried, expose safe partial amendment, name capture-date corrections explicitly, and say once (`PROJECTED`) which families are also browse columns -- status and rotate; a star in the log is a rating made elsewhere and the star column belongs to the ranking. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/photos.py` | Proven | Core | `b12f4ff8`, `ca40451c`, `96e985d9`, `7227545d`, `f86ab56e` | Owns admission, full-byte identity, truthful open/state, collision-proof put/move, and version grouping; a verified import now records its photo and copy in the same transaction. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/copies.py` | Proven | Core | `96e985d9`, `7227545d`, `e3335a42`, `d977daa8` | A complete marker-stable sweep -- of a drive or one folder on it -- admits observed photos in bounded committed batches, recognises a move by name, size and time (re-addressing the row, or recording an alternate tail when another drive still holds the old address) and a rewrite by bytes (re-identifying and carrying decisions), and retires stale hints only after the complete marked drive stayed present; refuters cover moves, alternates, renames, rewrites and one-folder sweeps. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/backup.py` | Proven | Keep and safety | `b12f4ff8`, `7227545d` | Backup only adds byte-proven regular files; reclaim ignores symlinks, honors alternate tails, repeats byte proof at the destructive gate, and removes at most one expendable copy per call. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/trash.py` | Proven | Keep and safety | `d0dbf737`, `c78d0c32`, `8c3f24a1`, `220ad680` | Count and Empty only: browsing Trash is `library.trash`. Empty preflights all registered drives, hinted and canonical copies, regular-file identity and full bytes; record copies leave last and interrupted deletion resumes from committed copy truth. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/cull.py` | Proven | Keep and safety | `8c3f24a1`, `d2a8357e`, `5769f3a3` | `change` and `undo` are the one shape every projected decision shares -- decide per identity, write the column, report exactly what moved -- and Pick, Clear, Reject, Restore and Turn are its callers; unidentified photographs are refused rather than keyed by row, and the stale-Undo, duplicate-identity and turn refuters live in `test_core.py`. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/cache.py` | Proven | Core | `0639ab8d`, `155d450c`, `f51d08bd` | Cache kinds are explicit immutable capabilities; recipes refuse undeclared inputs, failures are facts, and an optional projection applies under a savepoint so a partial query-index write is rolled back and recorded once; a kind may say where the file it reads lives (`source`), which is how the embedding reads the grid tile instead of the original. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/scope.py` | Proven | Core | `d33c655e`, `170b9f32`, `dbc6a4e9` | One immutable filter value composes folders, star floors, sets, and explicit selections; large id selections remain one bound JSON argument rather than growing SQL; `any_of`/`not_of` complete composition and camera/taken/status join the vocabulary. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/projection.py` | Proven | Core | `01fb3949` | One writer for every derived column on a photograph row (status, rotate, the metadata columns, elo and stars, stack_of): reads the columns once, writes only rows that differ, commits in slices of two thousand so the write lock is held a third of a second at most. 09-09: Proven -- rerank again 3.7 s -> 1.3 s, stacks again 4.2 s -> 1.9 s on a fresh 150k-row catalog; the suite is green |
| `web/model/sets.py` | Proven | Organise | `d33c655e`, `b6e3cdf4` | Collections and keywords are named decision families with validated stable ids and atomic hash-membership writes; nonexistent computed-query storage was deleted rather than preserved; a descriptor may carry criteria (the set is smart, nothing written per photograph) and `redefine` changes or drops them -- dropping after writing the members is the whole of Freeze. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/work.py` | Proven | Work and cache | `0639ab8d`, `bce1178f`, `155d450c`, `39bc64c2`, `220ad680`, `591b4138`, `5769f3a3`, `d977daa8`, `f51d08bd` | Owed work and debt are queries over explicit kinds; the owner's worker runs in lanes that each step their own share of the library (id modulo lanes, as a scope) on their own connection, doing the most-owed thing for the newest photograph -- on-screen first, then identity, metadata, grid, loupe per photograph -- nudged awake by sweeps and looks, counting what finished for the window's pulse, with no global lifecycle, lock, or claim; the step asks each kind where its bytes live, so a vector is made with the archive drive away. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library. 09-08: lane 0 counts what is owed every half minute on its own connection and the pulse carries `doing` and `left`, so the status line never costs the interactive lane a query |
| `web/tiles.py` | Proven | Browse, disk, and tiles | `0639ab8d`, `e757a6fe`, `220ad680`, `5769f3a3`, `170b9f32`, `f51d08bd` | Two kinds over one store: `grid` (1024, never evicted) and `loupe` (4096, evictable under a ceiling defaulting to half the cache disk's free space), no recipe until an edit arrives. Whichever is asked first decodes the original once at loupe size and publishes both files; the other records its file; a grid tile is cut from an existing loupe without opening the original. Atomic no-overwrite publication, exact-row clearing and one path-presence answer remain; `ready` is spelled in literals so the same sentence serves as the embedding kind's `wants`. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/library.py` | Proven | Browse, disk, and tiles | `84d832f2`, `220ad680`, `0c0ec6ef`, `5769f3a3`, `d7b9e2c6`, `d977daa8`, `170b9f32`, `f93a7e91` | One bounded page query serves the grid and Trash, scoped by the scope primitive (folders and more) and carrying cached answers as columns -- tile, loupe, whether each failed, and whether any copy is on a drive that is here; `size` is a scope's count; `folder_tree` is one tree over every drive with counts and a safety word; counts, facets, dates, decision reindex from `PROJECTED`, and folder safety derive from core rows; `rerank` rewrites the Best sort index and the star column whole from the ranking (scored photographs their score and star, the rest base and none), with no vector gate. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/render.py` | Proven | Computation | `84d832f2`, `155d450c`, `220ad680` | One decode, orientation, resize and JPEG encode path serves tiles and exposes `pixels()` so a store can cut two sizes from one decode; RAWs read the camera's embedded JPEG when it is large enough (oriented by LibRaw's flip, verified on real portrait CR3s) and demosaic otherwise; JPEG drafts are aspect-correct; encoding is baseline (measured 5x cheaper for 5% size). 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/photo/kind.py` | Proven | Computation | `84d832f2` | RAW decoding is selected by bytes rather than extension alone, while Develop capability and camera provenance remain separate explicit questions. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/photo/__init__.py` | Proven | Computation | `155d450c` | Defines the package boundary as pure format and EXIF reads; identity, location, scheduling, and persistence are explicitly excluded. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/photo/exif.py` | Proven | Computation | `155d450c` | Reads only camera, lens, and capture date from a bounded four-MiB TIFF/RAW header; a synthetic native TIFF proof protects byte order, IFD traversal, and offsets. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/metadata.py` | Proven | Computation | `155d450c` | Embedded browse facts are one content-keyed cache kind; projection honors owner date decisions, updates every identical row, repairs query columns at boot, and excludes path/sidecar inputs that would poison the key. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/features/catalog/metadata.py` | Removed | Work and cache | `6ab9c93e` | The pauseable scanner, private thread pool, retry ledger, orientation daemon, and mutable status are replaced by query-derived debt and the one owned V2 chore loop |
| `web/photo_metadata.py` | Removed | Computation | `6ab9c93e` | The broad path-based extractor mixed embedded EXIF with filesystem facts, sidecars, global Pillow mutation, and duplicate dimensions; V2 metadata is one bounded content-keyed answer |
| `web/photo/identity.py` | Removed | Core | `b7665224` | Prefix hashing, async backfill state, V1 schema queries, and the HDD governor are replaced by full-byte `model.photos.content_hash` plus the query-driven V2 worker |
| `web/photo/location.py` | Removed | Core | `b7665224` | Drive-letter probing and mutable guessed mappings are replaced by explicit marker-backed drives, safe tails, and verified copy addresses |
| `web/photo/visibility.py` | Removed | Browse, disk, and tiles | `b7665224` | Source joins and `missing_at` filtering contradict the V2 invariant that unplugging a drive never removes a photo from the library; `library.IN_LIBRARY` is the sole rule |
| `web/test_photo_identity.py` | Removed | Core proof | `b7665224` | Exercised the deleted prefix-hash/V1 backfill rather than the complete-byte identity protected in `test_core.py` |
| `web/test_location.py` | Removed | Core proof | `b7665224` | Exercised the deleted drive-letter guesser; marker relocation and same-named-stranger refusals live in `test_core.py` |
| `web/test_visibility_rule_is_one_rule.py` | Removed | Core proof | `b7665224` | Asserted the rejected source/missing visibility rule; the V2 away-versus-lost and library visibility invariants live in `test_core.py` |
| `web/boot.py` | Proven | Verification and fresh boot | `e331be8f`, `155d450c`, `e697d24e`, `e3335a42`, `b153c471`, `e757a6fe`, `d0dbf737`, `c78d0c32`, `8c3f24a1`, `39bc64c2`, `220ad680`, `591b4138`, `0c0ec6ef`, `5769f3a3`, `d7b9e2c6`, `d977daa8`, `9a9ba1af`, `f51d08bd`, `e3943728`, `b6e3cdf4`, `e3de35a3`, `dbc6a4e9` | One `Library` owns browse, cull, turn, forget, Trash, restore, Undo, empty, look and pulse verbs, with rows carrying renditions as file URLs and whether anything holds the photograph; `OwnedLibrary` confines interactive SQLite access to one thread, sweeps on one separate bounded connection, follows the working drives every minute and every drive once at start, synchronizes one folder on request, drains both lanes before close, and refuses work after close; the embedding kind rides the same worker, and the ranking follows the library on the sweep lane -- keyed on the last round and the vector count, with the space held in memory between reranks; `Library.search` answers one ranked page and `OwnedLibrary.find` supplies the space from the rank lane's memo and a query vector only when the model is warm, warming it in the background otherwise; `viewing` compiles {folder, collection, chips} into the one scope every surface narrows by, `_shelf` unions a name's descendants, and the collection verbs (create/rename/forget/add/remove/quick/freeze/save-view/save-photos/cameras) live here with the pinned pair. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/routes/__init__.py` | Removed | Desktop UI transport | `e697d24e`, `e757a6fe` | The route boundary was a migration seam; the desktop now calls the product directly in the same process |
| `web/routes/library.py` | Removed | Browse, disk, and tiles | `e697d24e`, `a3bbd3cd`, `e3335a42`, `b153c471`, `e757a6fe` | HTTP parsing and response translation are unnecessary in the one-person desktop product; direct bridge verbs preserve the product behavior without transport |
| `web/routes/system.py` | Removed | Desktop UI transport | `5c3d24eb`, `b153c471`, `d5460e43`, `e757a6fe` | Health, route discovery, document serving, and prepare-quit existed only to coordinate a loopback child and are gone with it |
| `web/v2_app.py` | Removed | Verification and fresh boot | `5c3d24eb`, `b153c471`, `e757a6fe` | The FastAPI lifespan and mounted static server are replaced by one native process owning the product and inlined document |
| `web/home.py` | Proven | Desktop packaging | `dfd75ad4` | One folder holds the catalog and the previews; a per-user pointer names it, `AZIMUTH_HOME` replaces the pointer for isolation, and the proposal is an `Azimuth Photo` folder on the internal solid-state disk with the most room (bus and seek penalty asked, so a USB archive is never proposed); replaces the desktop's use of the sixteen-path V1 runtime layout. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/desktop.py` | Proven | Desktop packaging | `e757a6fe`, `d0dbf737`, `c78d0c32`, `8c3f24a1`, `39bc64c2`, `220ad680`, `dfd75ad4`, `0c0ec6ef`, `5769f3a3`, `d7b9e2c6`, `d977daa8`, `9a9ba1af`, `e3943728`, `e3de35a3`, `dbc6a4e9` | Owns one V2 product opened at the chosen home (or none until one is chosen), one native WebView2 window opened from the document's file URL, direct home/browse/cull/Trash/look/pulse verbs, the native folder chooser, and idempotent shutdown; first-run and second-start proof passed in the real window with the pointer isolated. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/templates/v2.html` | Proven | Desktop UI | `b153c471`, `e757a6fe`, `9ddb91a4`, `a33df983`, `d2a8357e`, `dfd75ad4`, `0c0ec6ef`, `d7b9e2c6`, `d977daa8`, `9a9ba1af`, `e3943728`, `dbc6a4e9`, `ec8f0b00` | The semantic desktop document names library, Trash, native folder setup, browse controls, inspector, loupe, toast, and typed-count deletion without inherited templates or inline behavior; the app wears azimuthphoto.com's instrument palette -- one face across site and product. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/index.css` | Proven | Desktop UI | `b153c471`, `e757a6fe`, `9ddb91a4`, `a33df983`, `d2a8357e`, `3b3327ef`, `dfd75ad4`, `0c0ec6ef`, `5769f3a3`, `d7b9e2c6`, `d977daa8`, `9a9ba1af` | One neutral visual vocabulary keeps chrome subordinate to photographs, positions bounded virtual cells, makes destructive intent distinct, preserves full loupe images, and prevents hidden controls leaking through layout rules. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/index.js` | Proven | Desktop UI | `b153c471` | The composition root loads registered lenses before the shell without making the shell import a product surface. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/net/index.js` | Proven | Desktop UI | `b153c471`, `e757a6fe`, `a33df983`, `8c3f24a1`, `39bc64c2`, `220ad680`, `dfd75ad4`, `0c0ec6ef`, `5769f3a3`, `d977daa8`, `9a9ba1af`, `e3943728`, `e3de35a3`, `dbc6a4e9` | Waits for the native bridge and exposes the sole browse and Trash vocabulary; it contains no fetch, URL, route, or HTTP concept. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/store/index.js` | Proven | Desktop UI | `b153c471`, `9ddb91a4`, `a33df983`, `39bc64c2`, `0c0ec6ef`, `e3943728`, `dbc6a4e9` | Owns the sole mutable UI state, current lens, sparse indexed photo map, selection position, library/Trash counts, the transient notice, and the registry. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/kit/virtual-grid.js` | Proven | Desktop UI | `9ddb91a4`, `3b3327ef` | Pure geometry for a justified grid: from every photograph's aspect (3:2 assumed until read), a row-height ceiling and the width it lays every row out in one pass (47,000 in milliseconds), answers a cell's place, the visible range by binary search over row tops, and the nearest cell above or below; no product state or DOM. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/kit/page-cache.js` | Proven | Desktop UI | `9ddb91a4`, `d2a8357e`, `39bc64c2` | One sparse page owner deduplicates in-flight reads, rejects stale sort generations, jumps to arbitrary offsets, turns one failed page into one visible error rather than a retry storm, patches or removes loaded rows in place by predicate, and re-reads every loaded page under the same generation so the window follows the library without a reset. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/kit/remembered.js` | Proven | Desktop UI | `6729cfa2` | What the window keeps between launches (sort, density, panels, folds, rank size and mode), read and written one way with no storage a fatal case. 09-09: Proven -- native launch reads the remembered sort and density; the suite and node specs are green |
| `web/static/v2/kit/days.js` | Proven | Desktop UI | `4abb056b` | Days said once: the title of a day and where each day starts in a date-sorted grid, read by the grid's chapters, the timeline rail and the import stage so they cannot disagree. 09-09: Proven -- the rail and the chapters draw from it natively; the node specs and the suite are green |
| `web/static/v2/kit/presence.js` | Proven | Desktop UI | `d5562fda` | Whether a photograph can be shown, said once for the grid cell, the loupe's note and the inspector's Where row. 09-09: Proven -- the native inspector reads Here from it; the suite is green |
| `web/static/v2/lens/library.js` | Proven | Desktop UI | `b153c471`, `e757a6fe`, `9ddb91a4`, `a33df983`, `d2a8357e`, `220ad680`, `3b3327ef`, `5769f3a3`, `d7b9e2c6`, `d977daa8`, `dbc6a4e9` | One lens reconciles only the visible overscan neighborhood for Library or Trash over a justified layout; a cell's picture is the file URL its row carries, a turned row takes its turned shape and shows the tile rotated, an empty cell says whether it is pending, cannot be shown, or its drive is away, and a click selects by index so the row is read as it is now; marked cells show selection and cells are drag sources. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library. 09-08: chapters for the oldest sort too (the day list read backwards), the stack badge hands over its index, `indexAt` for the rail |
| `web/static/v2/shell/app.js` | Proven | Desktop UI | `b153c471`, `e757a6fe`, `9ddb91a4`, `a33df983`, `d2a8357e`, `39bc64c2`, `220ad680`, `3b3327ef`, `dfd75ad4`, `0c0ec6ef`, `5769f3a3`, `d7b9e2c6`, `d977daa8`, `9a9ba1af`, `e3943728`, `dbc6a4e9`, `dafdda92` | The coordinator owns view switching, sparse pages, direct product verbs, native folder choice, sorting, density, race-safe details, layered Escape, grid keys, the P/U/X cull keys and a loupe that shows the best picture its row has (the loupe file, else the grid tile) and swaps as the row changes; it follows the library through one in-place refresh shared by the sweep loop and the worker's pulse, tells the library what is looked at, and renders the status line from state as its one writer; the topbar search: type-to-search debounced, Enter immediate, slash focuses, Esc clears, results titled and counted with sort behind relevance; multi-select (shift ranges, ctrl toggles, Esc lets go), B into Quick, the grid's context menu and drag source, and the view state the chips and shelf share; a cull verb that edits a row edits the focused photograph too. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library. 09-08: the status line is always a sentence (what the worker is on, what is left, the pace, or the calm), leaving a stack lands on its cover at the same scroll, the timeline rail is wired; proven natively off-screen |
| `web/static/v2/shell/trash.js` | Proven | Desktop UI | `a33df983`, `d2a8357e`, `39bc64c2` | One focused workflow owns Restore, preflight, the typed-count dialog and permanent outcome copy, handing Undo to the shared toast; its native proof changed 3→2→3 without moving bytes and left the destructive submit disabled. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/rank.py` | Proven | Rank, search, and AI | `2fd8ab16`, `170b9f32`, `f93a7e91`, `f51d08bd` | Ranking as pure functions over the decision log: `rounds` reads every round whole (and drops one a later row retracts), `strength` fits one softmax model so set size is evidence not a mode, `candidates` is the one mosaic query over any scope (least-judged anchor, nearest in rating, a pair one orientation, what is on screen avoided), `judged` is the progress, `record`/`retract` write a round and take it back by saying so, `stars` names each ranked photograph's place (earned after three rounds, the best of even a handful five), `space` reads every ready vector as one matrix and `candidates` reads the taste-blended sort index so an unjudged set is drawn from where the prediction says the anchor lives; refuters hold order-independence, the prior, retraction, the ready scope, the pair rule and the bands. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/taste.py` | Gone 09-12 | Rank, search, and AI | `2fd8ab16` | Folded into `rank.fit` (E1: one Plackett–Luce fit learns the direction and each residual together; +7 points of held-out round accuracy on the owner's rounds). Was: one fitted direction in the embedding space predicts where you have not looked; blended with strength by how often a photograph was seen; measured decisions (linear head, encoder not the bottleneck) recorded in its docstring; reached from the product through `rank.space` and `rerank` since `f51d08bd`. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/conftest.py` | Proven | Core proof | `f51d08bd` | Parallel-safety fixtures for the suite, and the model stood down: `embed.ready` answers False under test because the card belongs to real work, with kind tests patching the seam back on together with a fake vector. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `scripts/backfill.py` | Removed | Rank, search, and AI | `09-08` | The V1 catalog it filled is read-only adoption material now; `scripts/adopt.py` copies whatever vectors it holds and the worker grows the rest from the tiles. It imported the deleted `core.catalog_path` |
| `web/embed.py` | Proven | Rank, search, and AI | `f51d08bd` | One model as a constant of the build (SigLIP-2 so400m, the only candidate that runs on this hardware, measured), loaded once per process; `kind()` composes the embedding capability over the tile store -- wants what the grid can show, source is the tile file, here only where the model runs, never evicted -- and `RECIPE` pins the exact string the live backfill writes, held by a test. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/search.py` | Proven | Rank, search, and AI | `e3943728`, `0364087f`, `e3de35a3`, `dbc6a4e9` | One ranked list of ids from three doors -- lexical LIKE over the word columns, members of every set whose name matches, nearest in the passed space -- fused by reciprocal rank; it never loads a model or reads the space off disk (the caller passes both or neither), so it cannot block and cannot refuse; the page fetch is `library.photos` over an ids scope like every other surface, and the fused ranking collapses to one result per identity at its best rank. Its private embedding kind, space loader and settings coupling are deleted -- `embed.kind` and `rank.space` own those now; every door narrows by the view's scope, so searching inside a collection is the same function. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/model/criteria.py` | Proven | Core | `b6e3cdf4` | The chip language smart collections and filters share: a closed vocabulary (folder, in, camera, status, stars, taken) validated and canonicalized, compiled through the same scope constructors every surface narrows with; `resolve` answers a set's scope (rules if smart, membership if fixed) with cycles answering empty; refuters hold AND/OR/NOT with NULLs, inclusive day ranges, live membership, freeze, intersection and the cycle. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/shell/albums.js` | Proven | Desktop UI | `dbc6a4e9`, `a564104b`, `ead39c0f` | The shelf and its hands: pinned pair first, names nested by slash, live counts, smart marks; rename/freeze/delete on right-click; the name popover; the photograph's menu (Quick, every fixed collection, new-from-selection, remove-when-inside); drop targets that refuse smart rows; proven in the full native walk. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library. renamed `collections.js` -> `albums.js` by the vocabulary law (`ead39c0f`); smart-album exceptions, Freeze, B-into-the-album-you-are-in (`c9a577db`) |
| `web/static/v2/shell/filters.js` | Proven | Desktop UI | `dbc6a4e9` | The chip bar: + Filter opens the closed field menu, one editor draws per field (stars, date range, cameras counted from the library, Pick status, collections), values OR within, exclude per chip, unfinished empty chips leave with the editor. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/shell/loupe.js` | Proven | Desktop UI | `dafdda92`, `bf77ca29`, `c5cf2471` | The loupe's transform engine: Fit and 100% (one image pixel to one device pixel), tap toggles, wheel zooms around the cursor, drag pans inside clamped edges, zoom survives the arrows, Esc steps to Fit before closing, and a turn composes into the same transform with its own fit; proven natively at dpr 1.75; the filmstrip rides below (the engine fits to the area above it), F clears the room and from the grid opens into it, and neighbour warming lands an arrow in 6 ms; the loupe is a workspace view with the chrome around it, and the panels fold LRC-style (Tab, Shift+Tab, edge tabs, remembered). 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `scripts/adopt.py` | Proven | Core | `1ffeffb3` | Carries the owner's judgement from the V1 catalog into a V2 home by the (tail, file size) map: decisions re-said under V2 identity (flag/status folded into one vocabulary, rounds whole or not at all, develop verbatim for its wave), keywords as keyword-kind sets, the backfill's vectors copied never recomputed, grid tiles seeded from the 1920-px previews through the renderer's own fit and encode; V1 opened read-only; idempotent per pass; proven on a fresh home over D:\Pictures against a live-catalog snapshot with a by-tail cross-check. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/pairing.py` | Removed | Rank, search, and AI | `170b9f32` | V1 Elo K-factor arithmetic; a round is fitted, not folded, so nothing needs a K |
| `web/static/v2/shell/rank.js` | Proven | Desktop UI | `170b9f32`, `dbc6a4e9`, `ead39c0f` | The Refine conversation: a set from the current scope at equal area (2/4/6/9/12), a digit or click picks, arrows move or pick a side in a pair, the pick is seen for a beat then leaves with the longest-standing other (both in a pair), the next photographs decoded ahead, progress as `judged of total`, Undo puts the set back; proven natively maximized with the round, its retraction and the next round in the log and Best leading with the winner; asks over the view, so a collection refines like a folder. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library. renamed `refine.js` -> `rank.js` by the vocabulary law (`ead39c0f`); equal-area shelves and R in a round (`d5999942`), the least-judged law (`2479067d`), Learn (`5fcb9a36`), P/U/X under the mouse (`27f07752`), each proven in-window |
| `web/static/v2/shell/intake.js` | Proven | Desktop UI | `9a9ba1af`, `96c222f1` | The import conversation: staged photographs with pictures, suspects dimmed, the kind guessed or asked, destinations shown as truth, one button, a countdown to the card being free, and what happened at the end; proven natively on a fake card with a rollover collision and an already-held photograph; Film scans shows the rolls by name and count, each renameable, and the destination follows. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/shell/cull.js` | Proven | Desktop UI | `d2a8357e`, `39bc64c2`, `5769f3a3`, `dbc6a4e9` | One small workflow turns P/U/X/R and the contextbar verbs into one product call each; every action says how it patches a row by identity or that it removes the photograph from the view, then hands its changes to the shared Undo toast; proven natively for Pick, Clear, Reject, Undo and both turns; verbs act on the whole selection. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/static/v2/shell/undo.js` | Proven | Desktop UI | `d2a8357e`, `39bc64c2`, `170b9f32` | The one Undo toast: shows a message and holds the exact way back as a revert the caller made, runs it once; shared by cull, Trash restore and Refine, and Ctrl+Z runs it; one toast instead of one per surface. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/test_v2_virtual_grid.mjs` | Proven | Desktop UI proof | `9ddb91a4`, `3b3327ef` | Refutes DOM growth at 47,000 photos, proves every full row meets the width with each cell keeping its shape and no row exceeding the chosen height, that the window follows an arbitrary scroll position, that a shorter row holds more, and that up then down returns to the same cell. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/test_v2_page_cache.mjs` | Proven | Desktop UI proof | `9ddb91a4`, `d2a8357e` | Refutes duplicate page reads, stale-generation repaint, permanent visible-page retry loops at arbitrary large offsets, and a status patch that misses a second row holding the same identity. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/test_core.py` | Proven | Core proof | `284ba3f2`, `da1ccd7e`, `0639ab8d`, `bce1178f`, `7227545d`, `e331be8f`, `84d832f2`, `155d450c`, `e3335a42`, `e757a6fe`, `d0dbf737`, `c78d0c32`, `8c3f24a1`, `d2a8357e`, `220ad680`, `5769f3a3`, `d977daa8`, `f86ab56e` | Adds Trash refuters for prior-state restore, stale Undo, invalid-member atomicity, 5,000-photo batches, duplicate identities, away drives, stale copy hints, changed bytes, exact counts, record-copy-last interruption and resume; the focused V2 suite passes 99 tests on Windows. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/test_owned_library.py` | Proven | Verification and fresh boot | `e757a6fe`, `d977daa8` | Preserves only the expensive execution-lane refuters: scans cannot block browsing, cancelled close still releases SQLite, admitted work drains, and the live connection never changes threads. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/test_desktop.py` | Proven | Desktop packaging | `e757a6fe`, `d0dbf737`, `c78d0c32`, `8c3f24a1`, `220ad680`, `dfd75ad4`, `d977daa8` | The real direct bridge crosses attach, refresh, browse, tile, Trash page/count, Undo and destructive dry-run, then closes idempotently and releases the catalog. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `web/features/trash/__init__.py` | Removed | Keep and safety | `c6b48296` | The legacy feature boundary left with its routes and file-moving service |
| `web/features/trash/routes.py` | Removed | Keep and safety | `c6b48296` | HTTP Trash/restore/empty routes and cache invalidation are replaced by the direct one-process product vocabulary |
| `web/features/trash/service.py` | Removed | Keep and safety | `c6b48296` | The 820-line V1 source-local move engine, retry sleeps, V1 schema joins, virtual-copy families and cross-device sync behavior are replaced by decisions plus one full-copy destructive gate |
| `web/static/js/desktop/trash.js` | Removed | Desktop UI | `c6b48296` | The 473-line inherited modal, fetch, polling, selection and outcome surface is replaced by the 97-line V2 workflow |
| `web/test_trash.py` | Removed | Keep and safety proof | `c6b48296` | The 701-line V1 schema/service suite left with its implementation; expensive safety failures are refuted through the five-table core and direct desktop boundary |
| `web/test_v2_routes.py` | Removed | Desktop UI transport proof | `e697d24e`, `a3bbd3cd`, `e3335a42`, `b153c471`, `e757a6fe` | Route-only assertions left with HTTP; indexed scope moved to core proof and execution ownership moved to `test_owned_library.py` |
| `web/test_v2_app.py` | Removed | Verification and fresh boot | `5c3d24eb`, `b153c471`, `d5460e43`, `e757a6fe` | Server graph and prepare-quit proof are obsolete; `test_desktop.py` exercises the actual one-process boundary and catalog release |
| `web/test_fresh_boot.py` | Removed | Verification and fresh boot | `e757a6fe` | The hub/satellite subprocess server smoke asserted the rejected network product; fresh-home V2 boot moved to `test_desktop.py` and frozen native proof |
| `web/test_windows_desktop_install.py` | Removed | Desktop packaging | `e757a6fe` | Tauri versions, sidecar resources, browser launch, ports, and splash contracts are obsolete; actual one-process build and native proof replace the static assertions |
| `scripts/server_entry.py` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | The port-bound frozen entry existed only for the deleted child server |
| `scripts/build_server.py` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | The private server freezer is replaced by the one-process desktop freezer |
| `scripts/build_desktop_ui.py` | Proven | Desktop packaging | `e757a6fe` | Bundles the reviewed modular UI and inlines its CSS and JavaScript into one serverless document, refusing any remaining served-asset reference. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `scripts/build_desktop.py` | Proven | Desktop packaging | `e757a6fe` | One deterministic PyInstaller recipe includes the native window, V2 product, schema, serverless document, RAW decoder, WebView2 backend, and canonical icon; the 72.2 MiB result passed live proof. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `scripts/build_windows_desktop.ps1` | Proven | Desktop packaging | `e757a6fe` | One Windows command installs the pinned dependencies, builds the one-process app, and verifies the expected executable without Rust, Tauri, or a server build. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `scripts/start_azimuth_windows.ps1` | Proven | Desktop packaging | `e757a6fe` | The source launcher now builds the current serverless document and opens `web/desktop.py`; modes, ports, browser discovery, process probing, server reuse, and catalog compatibility switches are deleted. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `scripts/gates/invokes.py` | Removed | Verification | `e757a6fe`, `416f1c74` | Deleted when the guide-path gate (`scripts/gates/paths.py`) was written: a guide that names a missing path misleads an agent today, while a workflow's dead reference fails loudly the day it runs. The inherited workflow stays Legacy with its own references |
| `package.json` | Proven | Desktop packaging | `e757a6fe` | The desktop UI build adds one exact esbuild version to the inherited lint-only development set; the lock resolves a zero-vulnerability dependency graph and no JavaScript package ships in the frozen app. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `.github/workflows/release.yml` | Proven | Desktop packaging | `e757a6fe` | Tagged releases build and zip only the one-process Windows artifact; inherited Docker publishing, Linux server packaging, and the Tauri signing stub were deleted. 09-08: Proven -- the V1 tree is gone, the suite is green (264 tests, 384 subtests), the gates read zero, and a fresh-home native launch of this checkout opened the library |
| `desktop/src-tauri/build.rs` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | Tauri's generated build boundary is unnecessary in the one-process app |
| `desktop/src-tauri/Cargo.toml` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | The entire Rust dependency graph left with the parent process |
| `desktop/src-tauri/src/engine.rs` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | There is no child engine, executable resolution, port argument, or developer override |
| `desktop/src-tauri/src/server.rs` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | Loopback probing, navigation, prepare-quit, and child reaping are deleted rather than hidden behind the new bridge |
| `desktop/src-tauri/src/main.rs` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | pywebview now owns the sole native window in the same Python process as the product |
| `desktop/src-tauri/src/tray.rs` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | The transitional tray did not justify retaining a second language and process boundary |
| `desktop/src-tauri/tauri.conf.json` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | Tauri bundle and window configuration left with Tauri |
| `desktop/src-tauri/tauri.windows.conf.json` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | There is no frozen server directory to embed as a child resource |
| `desktop/ui/index.html` | Removed | Desktop packaging | `d5460e43`, `e757a6fe` | A waiting splash is unnecessary because the native window owns the inlined V2 document directly |
| `desktop/make_icon.py` | Removed | Desktop packaging | `d5460e43` | An unreferenced import-time icon generator and its stale orange palette were deleted; the bundle retains one canonical desktop icon |
| `scripts/azimuth-check` | Proven | Verification | `09-08` | Rewritten 09-08 as the one check: `git diff --check`, lint, the gates, then the suite, the node specs and this ledger; `--quick` stops after the gates. Ran end to end on 09-08 |
| `scripts/lint` | Proven | Verification | `09-08` | ruff over `web/`, eslint over `web/static/v2` and the node specs; nothing skipped silently. Green 09-08 |
| `eslint.config.js` | Proven | Verification | `09-08` | Two flat configs -- the V2 UI modules as browser ES modules, the `.mjs` refuters as node -- and nothing for a tree that is gone. Green 09-08 |
| `.github/workflows/ci.yml` | Proven | Verification | `09-08` | One job on `windows-latest` running `scripts/azimuth-check`, which is what the product ships on; the perf and Linux jobs that named deleted scripts are gone |
| `scripts/gates/check.py` | Proven | Verification | `09-08` | Six gates, one number each, failing on drift in either direction; `--write` records a fall and refuses a rise. `docs/GATES.md` is the study; all six read 0 on 09-08 |
| `scripts/gates/common.py` | Proven | Verification | `09-08` | The two helpers every gate shares: tracked files from git, one read |
| `scripts/gates/layers.py` | Proven | Verification | `09-08` | Rewritten 09-08 for the V2 direction: `model`/`photo`/`pixels` import nothing above them (model may read through photo), only `desktop` imports `boot`, a UI layer imports only its own or a lower layer. Found one real upward import on first run (`model/intake.py` -> `metadata`), which moved the pure tag reads into `photo/tags.py` |
| `scripts/gates/tables.py` | Proven | Verification | `09-08` | No `CREATE TABLE` outside `web/model/schema.sql`; 0 on 09-08 against 68 on 08-16 |
| `scripts/gates/paths.py` | Proven | Verification | `09-08` | The guides may not name a path that is not tracked; caught the three stale ownership lines in `AGENTS.md` on 09-08 |
| `scripts/gates/names.py` | Proven | Verification | `09-08` | pyflakes over the tree: a name that is called exists. Caught `normalize_date` mid-move on 09-08 |
| `scripts/gates/collects.py` | Proven | Verification | `09-08` | The suite can be collected; 32 modules failed to on the morning of 09-08 and 0 do now |
| `scripts/gates/imports.py` | Proven | Verification | `09-08` | A first-party import names a file that is there; 15 on the morning of 09-08, 0 now |
| `web/pyproject.toml` | Proven | Verification | `09-08` | ruff's selection (E, W, F) and the ignores the tree's style earns; the per-file ignores for deleted V1 files are gone. `scripts/lint` green under it 09-08 |
| `scripts/make_test_library.py` | Proven | Verification | `09-12` | Rewritten 09-12 as a portable fixture: thirty-one public-domain FSA/OWI photographs from the Library of Congress, pinned by URL and SHA-256, written into the three-root date-folder shape under `--dest` or `AZIMUTH_TEST_LIBRARY` (never `C:\`), with known EXIF capture dates, DNGs under Raws/Digital made from the same frames, a duplicate pair, a nested folder, an undated frame and a `PROVENANCE.md`; stdlib plus Pillow. Proven 09-12 on Linux: builds in 2 s, a fresh `Library` sweeps all 32, dates read from JPEG and DNG alike, the pair shares one content hash, tiles for all 32 in 1.3 s. `AGENTS.md` names it as the development library |
| `scripts/sim_rank.py` | Proven | Rank, search, and AI | `09-08` | The instrument the round law was tuned with (`2479067d`): 300 rounds x 9 over 2,000 synthetic photographs per mode; diverse's 42% repeat rate was found and fixed with it |
| `scripts/sim_learn.py` | Proven | Rank, search, and AI | `09-08` | The tournament of strategies that chose Learn as the default mode (`5fcb9a36`): 7 strategies, 3 seeds, 600 rounds; rho .396 / top-decile .442 through the real pipeline |
| `scripts/back_up_photos.py` | Proven | Keep and safety | `09-08` | `model.backup` from the command line: adds byte-proven copies to the record drive and reclaims a working copy only after a fresh full-byte comparison. Proved on real files 08-17 (refused with nothing archived, refused on a differing archive copy, freed only on matching digests). Limitation: not yet a verb in the window |
| `scripts/sweep_drives.py` | Proven | Browse, disk, and tiles | `09-08` | `model.copies.sweep` from the command line over every attached drive; swept both real drives 08-17 (144,363 copy rows). Limitation: the window sweeps on its own rhythm, so this is an operator's tool |
| `web/develop.py` | Proven | Develop | `09-08` | An edit is a decision in Lightroom's own crs spelling, the renderable subset projects to `images.develop` so a rendition's recipe is per photograph in SQL, sidecars and self-carrying DNGs are adopted on the sweep, and write-back merges the crs surface into the sidecar keeping every other block (the LRTimelapse discipline). Proven in-window: a sidecar narrowed a tile 1.78->0.99 exactly (`2d58766a`), the editing workspace and round trip (`dedcb928`), 2,086 LR-converted DNGs dated and adopted (`321d01d7`). 09-08: the crs serializer folded in from the deleted `xmp_write`, pinned by `test_core`'s sidecar round trips. Limitation: parked by the owner 08-28 pending the Lightroom-shaped rebuild; the 0.0445 L tone gap stands |
| `web/exports.py` | Proven | Develop | `09-08` | An export is the photograph rendered: full-size pixels through the same `render.pixels` the tiles use, to JPEG at the dialog's quality, never a second pipeline (`470412bb`, `ae57b1d2`); proven in-window |
| `web/faces.py` | Proven | Rank, search, and AI | `09-08` | A face is a cache kind computed from the grid tile: InsightFace on the CPU (the card belongs to SigLIP), boxes plus fp16 ArcFace vectors, weights fetched deliberately. Proven end to end with the real model (`0de5c80d`) |
| `web/people.py` | Proven | Rank, search, and AI | `09-08` | A person is a name the owner gives a group of faces: precision-first centroid clustering on the rank lane, the name one decision on the exemplar face so it survives regrouping, same-named groups fold into one (`0de5c80d`, `e2a83e4e`); centrality picks the avatar (`7d735d8d`). Proven in-window: six portraits -> Someone -> named -> browsable chip |
| `web/labels.py` | Proven | Organise | `09-08` | Labels are words the owner taught: born by the first Y/N on a term, a ridge-logistic head over the space seeded by the word's vector, membership to the weakest anchor, exclusions leave the word's view at the keystroke (`ead39c0f`, `e2a83e4e`). Measured on the real library: 0/20 -> 9/20 in fifteen keystrokes |
| `web/photostats.py` | Proven | Rank, search, and AI | `09-08` | The photographic facts the embedding throws away -- lit-pixel chroma, hue focus, tone, sharpness -- one cache kind from the grid tile, and its derived word is the Look chip (color / b&w / sepia), calibrated on real tiles (`8855c2f7`) |
| `web/places.py` | Proven | Organise | `09-08` | The camera knows when, the phone knows where: EXIF GPS reads first and always wins; a GPX track places dated photographs by interpolation as `place` decisions (`758ca1b8`). Proven in-window; the owner shelved the geotag workflow 08-28 for the timezone wrinkle |
| `web/stacks.py` | Proven | Browse, disk, and tiles | `09-08` | A stack is a run of frames at one cadence: >=4 frames on one exact interval collapse behind their first, `images.stack_of` projected at boot and every sweep, browse and Rank collapse to covers (`f6340ef6`). Proven in-window on a real burst. 09-08 (`86fdd0be`): a gap keeps the beat within two seconds or a third of it, sized from the working catalog's own jitter; three refuters |
| `web/photo/tags.py` | Proven | Computation | `09-08` | Moved down from `metadata.py` and `render.py` on 09-08 when the layers gate found `model/intake.py` reaching up for a tag read: the shape a photograph is shown at, camera, lens, date, place and description from the bytes alone, bounded EXIF for raws and Pillow for the rest. `test_core`'s metadata projection and date-override refuters cover it |
| `web/pixels/__init__.py` | Proven | Computation | `09-08` | The package boundary: pure functions over pixel arrays, importing numpy and siblings and never the product; `render.developed` is the one door in |
| `web/pixels/numbers.py` | Proven | Computation | `09-08` | Making a number out of whatever arrived (moved from `core/numbers.py`), plus `spelled()`, what an XMP string spells -- the one reading the maths shares |
| `web/pixels/pipeline.py` | Proven | Computation | `09-08` | The colour pipeline, byte-for-byte V1's (`git mv` from `features/develop/`), entered through its display base by `render.developed`: every RENDERED crs key renders through it for the edited grid, loupe and export, proven in-window (`dedcb928`). Own refuters: `test_develop_pipeline`, `_calibration`, `_detail2`, `_invariants` (every slider finite, bounded, exact at default), `_sigmoid`, `_grade`. Limitation: per-line review deferred to the Develop rebuild; the parity gap to Adobe (0.0445 L) is that phase's work |
| `web/pixels/ops_constants.py` | Proven | Computation | `09-08` | The constants table the pipeline and its siblings share; `test_develop_ramps` refutes any private copy |
| `web/pixels/ramps.py` | Proven | Computation | `09-08` | smoothstep and the gaussian EV bell, once; `test_develop_ramps` |
| `web/pixels/sigmoid_view.py` | Proven | Computation | `09-08` | The scene-referred view transform; `test_develop_sigmoid` |
| `web/pixels/dng_pipeline.py` | Proven | Computation | `09-08` | Adobe's DNG colour science -- baseline exposure, dual-illuminant matrices, ProPhoto to linear sRGB -- imported by the pipeline and reached when a colour profile is passed; `test_dng_pipeline`. Limitation: the product renders on the display base today, so this chain waits for the parity phase |
| `web/pixels/adobe_profiles.py` | Proven | Computation | `09-08` | Reads and normalises an embedded Adobe profile from a DNG and indexes extracted ones; `test_adobe_profiles` (against a temp directory -- the extracted-profile cache is runtime data and ignored). Same limitation as `dng_pipeline` |
| `web/pixels/film.py` | Proven | Computation | `09-08` | The physically modelled film engine -- spectral exposure, H&D curves, DIR couplers, halation, grain -- imported by the pipeline and reached by `pa_FilmStock`; `test_develop_film` (14 stocks load, mid-grey anchors, monotone light). Limitation: no V2 surface offers a stock yet |
| `web/pixels/looks.py` | Proven | Computation | `09-08` | Look and preset curve composition the pipeline reads at import; `test_develop_looks` |
| `web/pixels/lens.py` | Proven | Computation | `09-08` | Only the pure distortion geometry the pipeline reads (radial scale, auto-crop); the exiftool and lensfun *resolution* left with the V1 tree on 09-08. `test_develop_lens` |
| `web/pixels/masks.py` | Proven | Computation | `09-08` | Local corrections: linear, radial, brush, luminance and colour range masks, rasterised and refined; imported lazily by the pipeline when a fragment carries them. `test_develop_masks`, `test_develop_rangemask`. 09-08: the runtime-paths and PIL-limit imports cut; an AI raster needs an explicit cache root or is zero |
| `web/pixels/guided_filter.py` | Proven | Computation | `09-08` | The edge-aware mask refinement masks use; `test_develop_guided_filter` |
| `web/pixels/heal.py` | Proven | Computation | `09-08` | Retouch spots, applied lazily by the pipeline; `test_develop_heal` |
| `web/test_adobe_profiles.py` | Proven | Computation proof | `09-08` | Refutes profile validation, indexing and lookup against a temp directory |
| `web/test_develop_calibration.py` | Proven | Computation proof | `09-08` | Calibration hue/saturation contracts |
| `web/test_develop_detail2.py` | Proven | Computation proof | `09-08` | Texture, clarity and dehaze contracts; the JS-twin UI test left with the twin |
| `web/test_develop_film.py` | Proven | Computation proof | `09-08` | Stocks load, tables build, grey anchors, monotone light, grain determinism, halation |
| `web/test_develop_grade.py` | Proven | Computation proof | `09-08` | Colour grade wheels, noise reduction and defringe direction |
| `web/test_develop_guided_filter.py` | Proven | Computation proof | `09-08` | Guided filter edge behaviour and the opt-in bench |
| `web/test_develop_heal.py` | Proven | Computation proof | `09-08` | Retouch spot import and application |
| `web/test_develop_invariants.py` | Proven | Computation proof | `09-08` | Every numeric control finite, bounded and exact at default over the torture settings (the fixtures inlined from the deleted parity test) |
| `web/test_develop_lens.py` | Proven | Computation proof | `09-08` | Vignetting and distortion polynomials, tiling without seams |
| `web/test_develop_looks.py` | Proven | Computation proof | `09-08` | Look curve composition |
| `web/test_develop_masks.py` | Proven | Computation proof | `09-08` | Mask rasterisation and local corrections |
| `web/test_develop_pipeline.py` | Proven | Computation proof | `09-08` | White balance, exposure, region tone and profile paths |
| `web/test_develop_ramps.py` | Proven | Computation proof | `09-08` | The shared ramps, and that no module keeps a private copy |
| `web/test_develop_rangemask.py` | Proven | Computation proof | `09-08` | Luminance and colour range masks |
| `web/test_develop_sigmoid.py` | Proven | Computation proof | `09-08` | The view transform |
| `web/test_dng_pipeline.py` | Proven | Computation proof | `09-08` | Baseline exposure, matrices and the scene-linear path |
| `web/test_one_place.py` | Proven | Verification | `09-08` | An idea lives in one place: no helper body is written twice anywhere in `web/`, by AST fingerprint. Green on 09-08 once the tag reads had one home |
| `web/static/v2/shell/crop.js` | Proven | Desktop UI | `09-08` | The crop surface over the loupe: drag draws, edges resize, middle moves, Enter applies, Full frame removes; proven in-window (`2d58766a`) |
| `web/static/v2/shell/editpanel.js` | Proven | Desktop UI | `09-08` | D over the loupe: Light/Color/Presence/B&W in Lightroom units, drags preview as look-files, releases append decisions, Reset rests all (`dedcb928`). Limitation: the owner parked Develop 08-28 pending the LRC-shaped rebuild |
| `web/static/v2/shell/labels.js` | Proven | Desktop UI | `09-08` | The Labels shelf and the Refine-a-label teach verbs, Y anchors and N excludes from any search or label view (`ead39c0f`) |
| `web/static/v2/shell/people.js` | Proven | Desktop UI | `09-08` | The People section and the face wall: named rows with face marks, Someones, Name.../Rename, click browses (`61aba63d`, `02d29558`) |
| `web/static/v2/shell/searchcards.js` | Proven | Desktop UI | `09-08` | Search cards: the box answers before it is asked -- years, cameras, kinds, orientations, albums, recents -- and every card applies as chips (`7cebd3fe`) |
| `web/pixels/film_stocks/cinestill-800t.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/ektar-100.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/fuji-superia-xtra-400.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/fuji-velvia-50.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/fujicolor-c200.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/ilford-hp5-plus.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/kodak-colorplus-200.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/kodak-gold-200.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/kodak-tmax-400.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/kodak-tri-x-400.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/kodak-ultramax-400.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/portra-160.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/portra-400.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/pixels/film_stocks/portra-800.json` | Proven | Computation | `09-08` | Fitted stock constants for the film engine: H&D control points, spectral crosstalk, DIR couplers, grain, halation, with provenance per stock (`7ad8c947`, `fec759b9` tuned against 53 real lab scans, `af755c58`). Data, not code; `test_develop_film` loads every one |
| `web/features/develop/rawproc.py` | Removed | Develop | `09-08` | The V1 decode/base cache; V2 decodes in `render.py` and the develop base is the display base |
| `web/features/develop/render.py` | Removed | Develop | `09-08` | The V1 export renderer and proof tiles; `exports.py` renders through `render.pixels` |
| `web/features/develop/routes.py` | Removed | Develop | `09-08` | HTTP; the bridge verbs `develop`, `develop_preview`, `develop_state`, `export_settings` replaced them |
| `web/features/develop/history.py` | Removed | Develop | `09-08` | Edit history is the decision log filtered to one photograph |
| `web/features/develop/presets.py` | Removed | Develop | `09-08` | Preset shelves wait for the Develop rebuild; the 994 real presets parse through `develop.read_sidecar` when it comes |
| `web/features/develop/importer.py` | Removed | Develop | `09-08` | Sidecars are adopted on the sweep by `develop.adopt` |
| `web/features/develop/virtual_copies.py` | Removed | Develop | `09-08` | A virtual copy is a later develop decision under a version subject, when wanted; the boot-time regex rewrite of the images table is gone |
| `web/features/develop/lrcat_import.py` | Removed | Develop | `09-08` | Never read the catalog (08-16 ruling); the photograph is the bridge |
| `web/features/develop/xmp_write.py` | Removed | Develop | `09-08` | The crs serializer moved into `develop.py`; the V1 image-id write-back, the DNG splice and the keyword decoration left |
| `web/features/develop/camera_profile.py` | Removed | Develop | `09-08` | The fitted camera-profile loader is unreached: the display base skips the camera base curve. Harvest from `27f07752` when the parity phase wants it |
| `web/features/develop/lossydng.py` | Removed | Develop | `09-08` | The lossy-DNG decoder rode `rawproc`; `render.py` opens DNGs through rawpy. Harvest from `27f07752` if a lossy DNG fails to open |
| `web/features/develop/noise_profiles.py` | Removed | Develop | `09-08` | Per-camera noise profiles are unreached by any fragment today; harvest from `27f07752` |
| `web/features/develop/transform.py` | Removed | Develop | `09-08` | Geometry/perspective is parked with the lens phase; harvest from `27f07752` |
| `web/features/develop/autotone.py` | Removed | Develop | `09-08` | Auto tone waits for the Develop rebuild; harvest from `27f07752` |
| `web/features/develop/highlights_recon.py` | Removed | Develop | `09-08` | Rode `rawproc`; harvest from `27f07752` |
| `web/features/develop/shots.py` | Removed | Develop | `09-08` | HDR merge shot grouping; HDR is parked |
| `web/features/develop/hdr.py` | Removed | Develop | `09-08` | HDR merge and its routes; parked with a receipt in `docs/DEVELOP.md` |
| `web/features/develop/ai_masks.py` | Removed | Develop | `09-08` | Model-pool AI masks; parked (zero masks in the real library) |
| `web/features/develop/native_exif.py` | Removed | Develop | `09-08` | Lens-id EXIF read for lens resolution, which left with it |
| `web/features/develop/discovery.py` | Removed | Develop | `09-08` | Lightroom catalog discovery; the catalog is never read |
| `web/test_dng_acceptance.py` | Removed | Develop proof | `09-08` | The 40-DNG Adobe-preview harness decoded through `rawproc`; the tone phase rebuilds it on `render` against the same DNGs (`D:/azimuth-bench/out/dng-acceptance` holds the 08-23 baseline, 0.0445 L) |
| `web/test_develop_parity.py` | Removed | Develop proof | `09-08` | The WebGL twin parity; there is one pipeline now. Its two fixtures live in `test_develop_invariants` |
| `web/lightroom.py` | Removed | Core | `09-08` | The `.lrcat` rotation adoption: `scripts/adopt.py` carries the V1 log's rotations into a V2 home, and Lightroom now writes orientation into the files it saves |
| `web/xmp.py` | Removed | Core | `09-08` | The portable-field sidecar reader; stars are the ranking's face and `develop.adopt` reads sidecars on the sweep |
| `web/image_headers.py` | Removed | Computation | `09-08` | `photo/exif.py` and `photo/tags.py` are the bounded header reads |
| `web/test_saved_views.py` | Removed | Organise proof | `09-08` | Asserted `sets.VIEW`, which never existed in V2; saved views wait for a typed query language |
| `clients/lightroom/azimuth-sync.lrplugin/Info.lua` | Removed | V1 remote hub/satellite system | `09-08` | The Lightroom plugin spoke to the hub; the sidecar is the bridge |
| `web/static/v2/shell/timeline.js` | Proven | Desktop UI | `09-08` | The timeline rail on date sorts: years and months placed by index from the same day list the chapters use, a marker riding the scroll, press or drag jumps with the day named. Proven in the browser harness (marks at 0/199/517/…, marker follows) and natively off-screen (`scripts/native_proof.py` probe: 8 marks, rail visible beside the inspector) |
| `scripts/harness.py` | Proven | Verification | `09-08` | The bundled UI with a stub bridge, served on :8765: the whole shell runs in a browser tab with no window and no catalog, and its DOM can be read and driven. Built and driven on 09-08; the stub's `then` trap is the lesson (a Proxy that answers everything is a thenable and the bridge never settles) |
| `scripts/native_proof.py` | Proven | Verification | `09-08` | The real app off-screen at x = -2400 on an isolated home: a JavaScript probe evaluated in the page and a `PrintWindow` capture, nothing on the person's screen. Ran twice on 09-08 (3325×2012 captures, PROBE lines with the rail's marks) |
| `web/static/v2/kit/icons.js` | Proven | Desktop UI | `09-09` | The icon set: a dozen 16 px marks in one stroke weight, drawn here rather than borrowed from Unicode, so an album, a person, a stack and a camera read the same everywhere. Proven in the sidebar, chips, drop and badges through the harness and the journeys |
| `web/static/v2/kit/menu.js` | Proven | Desktop UI | `09-09` | The one way a menu opens: at the pointer or on its row, clamped to the window, walked by the arrows, put away by Esc with the focus returned. Every right-click menu comes through here; proven in the journeys |
| `web/static/v2/kit/drop.js` | Proven | Desktop UI | `09-10` | Where photographs can be dropped, said one way: a list names its rows and judges each, a reason means refused and is shown while the drag is over it. Proven by the album and people journeys |
| `web/static/v2/kit/why.js` | Proven | Desktop UI | `09-10` | What a failure says: the bridge's own phrases mapped to a sentence a photographer can act on, anything else made a sentence. Proven by the error paths of the intake and album surfaces |
| `web/static/v2/kit/words.js` | Proven | Desktop UI | `09-12` | Counting things by name, one way (`numbered`), so no line reads "1 photographs" (D1, X138). Proven by the smoke's count line |
| `web/sharpness.py` | Proven | Rank, search, and AI | `09-10` | Where the sharpness sits, measured once per photograph on the loupe tile: the subject's sharpness and whether the eyes are open, from the face landmarks (E2, key zm4). Proven on the owner's catalog (1,404 rows) and in the suite |
| `web/panorama.py` | Proven | Rank, search, and AI | `09-12` | A panorama sweep as a fact the library knows: consecutive frames that overlap one way, verified by ORB and a RANSAC homography, judged one pair at a time and kept per run (PN1, X149-X155). Proven on the owner's catalog: one sweep among 446 runs, every burst refused |
| `scripts/bench.py` | Proven | Verification | `09-09` | The product boundary timed on a catalog copy, one line per verb against a budget per shape; `--budget` fails a run over them. Run on the 150k copy and the owner's copy each performance round |
| `scripts/smoke_boot.py` | Proven | Verification | `09-12` | The one proof a build must pass: a home with four photographs, the built document opened as the desktop opens it, the grid asked what it drew and what it caught (D5, X134, X139-X141). Part of `azimuth-check` on a desktop |
| `scripts/journeys/survey.js` | Proven | Verification | `09-09` | A photographer's survey of a library, as a probe for `native_proof.py`: the grid, the chapters, the rail. Run through the real app each round |
| `scripts/journeys/cull.js` | Proven | Verification | `09-09` | The cull journey: mark, reject, undo, as a probe for `native_proof.py` |
| `scripts/journeys/loupe.js` | Proven | Verification | `09-09` | The loupe journey: open, arrows, 100%, as a probe for `native_proof.py` |
| `scripts/journeys/search.js` | Proven | Verification | `09-09` | The search journey: the box, the drop, a card as a chip, as a probe for `native_proof.py` |
| `scripts/journeys/album.js` | Proven | Verification | `09-09` | The album journey: make one, add to it, the popover, as a probe for `native_proof.py` |
| `scripts/journeys/people.js` | Proven | Verification | `09-09` | The people journey: the wall, the arrows, N to name, as a probe for `native_proof.py` |
| `scripts/make_launcher.py` | Proven | Desktop packaging | `09-11` | The app as its own program: a copy of the base pythonw.exe beside the venv's pyvenv.cfg named `Azimuth Photo.exe`, wearing the compass and a version resource, the Desktop and Start Menu shortcuts pointed at it (PK2). Run on the owner's machine; his launches log through it |
The rest of the V1 application was deleted whole on 09-08 (commit subject: *The V1 application leaves the tree*): `web/app.py`, `web/api.py`, `web/db.py`, `web/settings.py`, `web/core/`, `web/data/`, every remaining package under `web/features/` (ai, backup, catalog, collections, dev, export, library, pages, search, settings, stacks, system), `web/workers/`, `web/elo_stars.py`, `web/embed_cache.py`, `web/judgements.py`, `web/scanner.py`, `web/shoot_rank.py`, `web/ai_models.py`, `web/date_inference.py`, `web/helpers.py`, `web/pixels/decode.py`, `web/pixels/rendition.py`, `web/templates/desktop.html`, `web/templates/setup.html`, all of `web/static/js/` and the PWA assets, the V1 requirement files, `web/test_support.py`, `web/testing_support.py` and the test modules that exercised them, the rest of `clients/lightroom/`, and the scripts `azimuth-server`, `run_test_app.py`, `restore_drill.py`, `give_photos_tails.py`, `purge_phantom_rows.py` and `repoint_source.py`. Each answered a question the core stopped asking; git history is the archive.

## How each rewrite change closes

Every rewrite commit updates this file in the same diff:

- change the affected product-area status only when its whole definition is met;
- add every surviving code file touched by the completed V2 surface;
- list the old files and concepts removed in the commit message;
- record the exact verification command and real-product observation;
- leave limitations explicit—an unstated limitation cannot be distinguished
  from forgotten legacy behavior.

The V2 rewrite is complete only when every in-scope code file is either
**Proven** or **Removed**, every product area is **Proven** or **Removed**, and a
fresh install completes the photographer's core loop without invoking an
unregistered path.
